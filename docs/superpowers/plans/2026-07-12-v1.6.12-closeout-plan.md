# v1.6.12 Compatibility and Release Closeout Implementation Plan

> **For agentic workers:** Execute this plan task-by-task with tests and review checkpoints.

**Goal:** Close the remaining v1.6.12 compatibility requirement and prepare the branch for release without changing the shipped AI feature scope.

**Architecture:** The `/graph` endpoint is a read-only project-forest route, so it must be able to import and serve legacy projects without initializing run-only plotting dependencies. Keep plotting available through the existing orchestrator namespace and load it only when a run reaches diagnostics. Legacy project directories without Workbench manifests remain an empty/degraded forest rather than being interpreted as modern runs.

**Tech Stack:** FastAPI, Python 3.14, pytest, Matplotlib (run-time only), existing project-forest scanner, Git worktree release flow.

---

### Task 1: Establish baseline and preserve the main checkout

**Files:**
- Read only: `/Users/jiayuanren/项目规划/.worktrees/workbench-v1.6.12/docs/superpowers/README.md`
- Read only: `/Users/jiayuanren/项目规划/.worktrees/workbench-v1.6.12/docs/superpowers/followups/v1.6.9-followups.md`
- Read only: `/Users/jiayuanren/项目规划/.worktrees/workbench-v1.6.12/docs/superpowers/roadmap/architecture-debt.md`
- Read only: `/Users/jiayuanren/项目规划/.worktrees/workbench-v1.6.12/docs/superpowers/roadmap/2026-07-01-unified-graph-workbench-roadmap.md`
- Read only: `/Users/jiayuanren/项目规划/.worktrees/workbench-v1.6.12/docs/superpowers/roadmap/2026-05-13-statistical-methods-roadmap.md`
- Read only: `/Users/jiayuanren/项目规划/.worktrees/workbench-v1.6.12/docs/superpowers/specs/2026-07-12-v1.6.12-ai-closeout-design.md`

- [x] Confirm the branch is `workbench-v1.6.12` and the starting commit is `60e63db`.
- [x] Confirm the main checkout's pre-existing untracked roadmap is outside scope.
- [x] Run the full baseline gate with `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 ./scripts/gate.sh`.

### Task 2: Add the failing V4 regression test

**Files:**
- Create: `tests/test_graph_legacy_compat.py`

- [x] Add a subprocess regression test that creates a legacy-style `runs/` directory, imports the FastAPI app, calls `GET /graph`, asserts the empty/degraded response, and fails if plotting is imported during app startup.
- [x] Run `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 .venv/bin/python -m pytest tests/test_graph_legacy_compat.py -q`.
- [x] Confirm the test fails because `workbench.api` eagerly imports Matplotlib through the orchestrator.

### Task 3: Make `/graph` import-safe for legacy projects

**Files:**
- Modify: `backend/workbench/orchestrator/__init__.py:88` and the orchestrator module's public helper area.
- Test: `tests/test_graph_legacy_compat.py`

- [x] Replace the top-level `from ..visualization import create_figures` import with a lazy wrapper named `create_figures` that imports the real implementation only when called.
- [x] Keep the wrapper in `workbench.orchestrator` so `engine/stages/diagnostics.py` and existing monkeypatch-based tests retain the same seam.
- [x] Re-run the focused regression test and confirm it passes.
- [x] Run the existing visualization/diagnostics tests to confirm real run execution still imports and calls Matplotlib correctly.

### Task 4: Reconcile the live backlog and release documentation

**Files:**
- Create: `docs/v1.6.12-release-notes.md`
- Modify: `docs/superpowers/followups/v1.6.9-followups.md`
- Modify: `task_plan.md`, `findings.md`, `progress.md`

- [x] Record the root cause and the degrade behavior in release notes, including that legacy project directories without `run_manifest.json` are ignored by the project forest and return an empty forest.
- [x] Remove V4 from the live backlog after the regression test and real sample smoke both pass.
- [x] Remove W3 only if the release notes explicitly document the existing `/runs/:id?project_root=` deep-link behavior.
- [x] Do not pull V7/V11, D2/D6, S1-S6, or the informational statistical-method roadmap into v1.6.12.

### Task 5: Full verification and release boundary

**Files:**
- Review all branch changes; no main-checkout edits.

- [x] Run `git diff --check`.
- [x] Run `LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 ./scripts/gate.sh` from the v1.6.12 worktree with preview servers stopped.
- [x] Inspect `git diff --stat`, `git status`, and the exact staged file list.
- [x] Request a pre-merge review and resolve all Critical/Important findings. Review returned no Critical/Important findings; one Minor note says the 5-second subprocess timeout may be tight on unusually slow CI.
- [x] Prepare the local branch commit (`474e031`). Push/PR/merge/tag remain separate because explicit release authorization is not yet present.
