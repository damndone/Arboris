# v1.8.8 P2：自然语言能力可达性守卫实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 把“自然语言可调用全部能力”变成一条从统一能力清单逐项判定、能暴露真实缺口的可达性守卫。

**Architecture:** CapabilityContract 保留声明的 operation/step 路径，但可达性不再由路径字符串非空决定。直接路径必须落到 OperationRegistry 中 natural_language_enabled=True 的 operation；组合路径必须落到现存的 WORKFLOW_STEP_SPEC_CONTRACTS，并且承载入口 operation.multi_step 本身必须自然语言开启。能力清单只把真实直接入口写入 proposed_by，把组合入口写入 composable_as；code.execute 与已被批量版取代的 data.column.cast 作为显式、带理由的两个关闭项，其余缺口不豁免。

**Tech Stack:** Python 3.14、pytest、现有 OperationRegistry / WORKFLOW_STEP_SPEC_CONTRACTS / CapabilityContract。

---

## 设计选择与当前验收基线

选择目标书的方案 1：守卫报告并钉死当前值。P2 不把 P5 的实现偷偷提前，也不使用 xfail；当前缺口会作为守卫报告中的真实 gaps 保存，并在修复某项能力的同一变更中更新记录。

已用 worktree 自己的解释器核实：

~~~text
inventory = 54
natural-language operation ids = data.columns.cast, graph.fork, model.rerun, operation.multi_step
model.genesis.natural_language_enabled = False
P1 的旧判定可达 = 39/54
~~~

P2 修正后要钉住：

~~~text
directly reachable = 4
composition reachable = 30  (20 admitted model families + 10 workflow-step capabilities)
reachable = 34/54 = 63%
explicitly closed = 2  (code.execute, data.column.cast)
real gaps = 18
~~~

18 条真实缺口：

~~~text
model.time_series.arma_garch
model.time_series.ets
model.auto
test.anova
test.chi_square
test.correlations
test.evidence
test.fisher_exact
test.nonparametric
test.rank_correlations
test.t_tests
prediction.prediction_lasso
prediction.prediction_ridge
prediction.prediction_random_forest
imputation.mice
resample.smote
resample.oversample
resample.undersample
~~~

model.genesis 是顶层 operation，但当前关闭自然语言顶层提议；它仍是 workflow step，所以 20 个已准入模型族通过 operation.multi_step 组合可达。model.auto、ARMA-GARCH、ETS 没有对应 workflow step，不能把表单可运行冒充为自然语言可达。

## File Structure

| 文件 | 职责 | 本计划的改动 |
|---|---|---|
| backend/workbench/agent/capability_contract.py | 能力契约、清单派生、可达性分类 | 从 live operation/step 注册表判定直接/组合路径；修正 proposed_by；增加两个显式豁免；增加可注入守卫报告 |
| tests/test_capability_inventory.py | P1 清单测试与 P2 可达性守卫 | 更新旧的乐观路径断言；增加直接/组合区分、54/4/30/34/18 钉死、显式豁免、假能力分母元测试 |
| docs/superpowers/plans/2026-08-08-v1.8.8-p2-reachability-guard.md | 本阶段计划 | 先写入，再按任务 TDD |

不改 workflow_contracts.py、operations.py、workflow_runtime.py、orchestrator、P0 seam 或统计执行器。natural_language_enabled 只从 live registry 读取；本阶段不开放新能力。

## TDD 与验活规则

每个生产断言都按顺序执行：先用 apply_patch 写测试并跑预期红测；再写最小实现变绿；最后把实现临时改成断言要拦的错误，先断言锚点存在、再断言文件确实改变，记录真实报错，恢复后重跑。

验活脚本的固定形状：

~~~python
assert anchor in original_text
mutated = original_text.replace(anchor, replacement, 1)
assert mutated != original_text
~~~

临时突变恢复后必须跑 git diff --check 和 git status --short。

---

### Task 1：让 CapabilityContract 判定真实直接路径与组合路径

**Files:**

- Modify: tests/test_capability_inventory.py
- Modify: backend/workbench/agent/capability_contract.py

- [ ] **Step 1: 写失败测试**

把原来用 model.genesis 证明直接可达的测试改用真实开放入口，并追加：

~~~python
def test_a_capability_exposes_only_a_live_direct_proposer() -> None:
    contract = CapabilityContract(
        capability_id="data.columns.cast",
        kind="data_operation",
        summary="Cast several columns in the current dataset node.",
        proposed_by=("data.columns.cast",),
    )
    assert (
        contract.directly_reachable_by,
        contract.composition_reachable_by,
        contract.is_reachable,
    ) == (("data.columns.cast",), (), True)


def test_a_capability_distinguishes_composition_from_direct_proposal() -> None:
    contract = CapabilityContract(
        capability_id="model.ols",
        kind="model_family",
        summary="Estimate an ordinary least squares model.",
        proposed_by=("model.genesis",),
        composable_as=("model.genesis",),
    )
    assert (
        contract.directly_reachable_by,
        contract.composition_reachable_by,
        contract.is_reachable,
    ) == ((), ("model.genesis",), True)


def test_a_nonexistent_declared_path_is_not_reachable() -> None:
    contract = CapabilityContract(
        capability_id="pack.missing",
        kind="pack",
        summary="A deliberately fake capability for the reachability guard.",
        proposed_by=("model.genesis",),
        composable_as=("workflow.not_registered",),
    )
    assert (
        contract.directly_reachable_by,
        contract.composition_reachable_by,
        contract.is_reachable,
    ) == ((), (), False)
~~~

保留边界测试；原 test_a_capability_declares_how_it_can_be_reached 改用 data.columns.cast。

- [ ] **Step 2: 跑红测**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8/backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_capability_inventory.py -q -k "live_direct_proposer or composition_from_direct or nonexistent_declared_path"
~~~

预期真实报错是 AttributeError: CapabilityContract 没有 directly_reachable_by。若是导入或路径错误，先修正命令，不得把非目标报错当红测证据。

- [ ] **Step 3: 最小实现**

在 CapabilityContract 前加入惰性 helper：

~~~python
def _live_natural_language_operation_ids() -> frozenset[str]:
    from .operations import OperationRegistry
    return frozenset(OperationRegistry().natural_language_operation_ids())


def _live_workflow_step_ids() -> frozenset[str]:
    from .workflow_contracts import WORKFLOW_STEP_SPEC_CONTRACTS
    return frozenset(WORKFLOW_STEP_SPEC_CONTRACTS)
~~~

在 CapabilityContract 中加入：

~~~python
    @property
    def directly_reachable_by(self) -> tuple[str, ...]:
        live = _live_natural_language_operation_ids()
        return tuple(operation_id for operation_id in self.proposed_by if operation_id in live)

    @property
    def composition_reachable_by(self) -> tuple[str, ...]:
        live = _live_natural_language_operation_ids()
        if "operation.multi_step" not in live:
            return ()
        steps = _live_workflow_step_ids()
        return tuple(step_id for step_id in self.composable_as if step_id in steps)

    @property
    def is_reachable(self) -> bool:
        return bool(self.directly_reachable_by or self.composition_reachable_by)
~~~

不要把 natural_language_enabled 复制进 CapabilityContract，也不要复制 workflow step 清单。

- [ ] **Step 4: 跑绿测**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8/backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_capability_inventory.py -q -k "live_direct_proposer or composition_from_direct or nonexistent_declared_path"
~~~

预期 3 passed。

- [ ] **Step 5: 验活**

每次突变前 assert 锚点存在且文件改变：

1. 直接路径 return 改成 return self.proposed_by；运行目标测试，预期关闭的 model.genesis 被错误当成直接路径。
2. 组合路径 return 改成 return ()；预期断言显示组合路径从 model.genesis 变成空。
3. is_reachable 改回 bool(self.proposed_by or self.composable_as)；只运行 nonexistent_declared_path，预期 True 与 False 的断言失败。

恢复后重跑 Task 1 目标测试并记录 PASS。

- [ ] **Step 6: 提交**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8 && git add backend/workbench/agent/capability_contract.py tests/test_capability_inventory.py && git commit -m "fix(capability): distinguish live direct and composed reachability"
~~~

### Task 2：让清单的 proposed_by 与显式关闭项诚实

**Files:**

- Modify: tests/test_capability_inventory.py
- Modify: backend/workbench/agent/capability_contract.py

- [ ] **Step 1: 写失败测试**

~~~python
def test_inventory_records_only_live_natural_language_proposers() -> None:
    from workbench.agent.capability_contract import capability_inventory
    by_id = {item.capability_id: item for item in capability_inventory()}
    direct_ids = {
        item.capability_id for item in by_id.values() if item.directly_reachable_by
    }
    assert direct_ids == {
        "data.columns.cast", "graph.fork", "model.rerun", "operation.multi_step",
    }
    assert by_id["model.ols"].proposed_by == ()
    assert by_id["model.ols"].composition_reachable_by == ("model.genesis",)


def test_the_two_closed_operations_are_explicitly_exempt() -> None:
    from workbench.agent.capability_contract import unreachable_capabilities
    result = unreachable_capabilities()
    assert {item.capability_id for item in result.exempt} == {
        "code.execute", "data.column.cast",
    }
    assert all(item.reachability_exempt_reason for item in result.exempt)


def test_unwired_model_selector_and_model_families_remain_real_gaps() -> None:
    from workbench.agent.capability_contract import unreachable_capabilities
    _exempt, gaps = unreachable_capabilities()
    assert {"model.time_series.arma_garch", "model.time_series.ets", "model.auto"} <= {
        item.capability_id for item in gaps
    }
~~~

同步更新 P1 旧断言：未 admission 的模型族必须 proposed_by=()、composable_as=()、not is_reachable；standalone operation 测试按 live registry 判断 proposed_by；当前 gap 测试更新为 18 条并包含 model_family 与 selector。

- [ ] **Step 2: 跑红测**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8/backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_capability_inventory.py -q -k "inventory_records_only_live or two_closed_operations or unwired_model_selector"
~~~

预期失败包含 direct ids 出现未开启 operation，或 result.exempt 为空；这必须是 proposed_by 高估，而不是 fixture 错误。

- [ ] **Step 3: 最小实现**

加入唯一关闭白名单：

~~~python
CAPABILITY_REACHABILITY_EXEMPTIONS: dict[str, str] = {
    "code.execute": (
        "Arbitrary code execution remains closed to the natural-language Agent "
        "by the product safety decision; use the explicit user-authorized code path instead."
    ),
    "data.column.cast": (
        "The single-column natural-language surface was superseded by the batch "
        "data.columns.cast operation; the legacy identity remains for manual typed-operation compatibility."
    ),
}
~~~

在 _model_capabilities 中只在 entry 存在且 model.genesis 是 live natural-language operation 时写 proposed_by。

在 _operation_capabilities 中按 operation_id 索引 registry.capabilities()，使用：

~~~python
proposed_by=(
    (operation_id,)
    if entries[operation_id]["natural_language_enabled"]
    else ()
),
composable_as=(
    (operation_id,) if operation_id in WORKFLOW_STEP_SPEC_CONTRACTS else ()
),
reachability_exempt_reason=CAPABILITY_REACHABILITY_EXEMPTIONS.get(operation_id),
~~~

并拒绝白名单中的未注册 operation：

~~~python
stale_exemptions = set(CAPABILITY_REACHABILITY_EXEMPTIONS) - set(entries)
if stale_exemptions:
    raise ValueError(
        "reachability exemption names unregistered operation(s): "
        + ", ".join(sorted(stale_exemptions))
    )
~~~

保留 composable_as 从 WORKFLOW_STEP_SPEC_CONTRACTS 派生，不手写十个 step id。

- [ ] **Step 4: 跑全清单测试**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8/backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_capability_inventory.py -q
~~~

预期全文件 PASS；gap=18、exempt=2。

- [ ] **Step 5: 验活**

1. 删除 model.genesis 的 live 条件；运行 inventory_records_only_live，预期 model.ols 重新出现在 direct ids 并失败。
2. 把 natural_language_enabled 条件改成 True；运行 inventory_records_only_live 与 two_closed_operations，预期 code.execute/data.column.cast 被错误算作 direct。
3. 把 reachability_exempt_reason 临时改成 None；运行 two_closed_operations，预期 result.exempt 缺少两个 id。
4. 删除白名单一个 key；运行 two_closed_operations，预期 exempt 集合缺少对应 operation id。

每次突变前 assert 锚点存在且文件改变；恢复后重跑全 test_capability_inventory.py。

- [ ] **Step 6: 提交**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8 && git add backend/workbench/agent/capability_contract.py tests/test_capability_inventory.py && git commit -m "fix(capability): stop counting disabled proposal paths as reachable"
~~~

### Task 3：从统一清单生成守卫报告并钉住 34/54

**Files:**

- Modify: tests/test_capability_inventory.py
- Modify: backend/workbench/agent/capability_contract.py

- [ ] **Step 1: 写守卫与分母元测试**

~~~python
def test_natural_language_reachability_guard_pins_current_truth() -> None:
    from workbench.agent.capability_contract import capability_reachability_guard
    report = capability_reachability_guard()
    assert (
        len(report.inventory), len(report.directly_reachable),
        len(report.composition_reachable), len(report.reachable),
        len(report.exempt), len(report.gaps),
    ) == (54, 4, 30, 34, 2, 18)
    assert {item.capability_id for item in report.gaps} == {
        "model.time_series.arma_garch", "model.time_series.ets", "model.auto",
        "test.anova", "test.chi_square", "test.correlations",
        "test.evidence", "test.fisher_exact", "test.nonparametric",
        "test.rank_correlations", "test.t_tests",
        "prediction.prediction_lasso", "prediction.prediction_ridge",
        "prediction.prediction_random_forest", "imputation.mice",
        "resample.smote", "resample.oversample", "resample.undersample",
    }
    assert {item.capability_id for item in report.exempt} == {
        "code.execute", "data.column.cast"
    }


def test_reachability_guard_derives_its_denominator_from_the_supplied_inventory() -> None:
    from workbench.agent.capability_contract import (
        CapabilityContract, capability_inventory, capability_reachability_guard,
    )
    fake = CapabilityContract(
        capability_id="pack.injected_for_guard",
        kind="pack",
        summary="An injected capability used to test the denominator.",
    )
    inventory = (*capability_inventory(), fake)
    report = capability_reachability_guard(inventory)
    assert (
        len(report.inventory),
        {item.capability_id for item in report.gaps},
    ) == (
        len(inventory),
        {"pack.injected_for_guard", "model.time_series.arma_garch", "model.time_series.ets", "model.auto",
         "test.anova", "test.chi_square", "test.correlations", "test.evidence",
         "test.fisher_exact", "test.nonparametric", "test.rank_correlations",
         "test.t_tests", "prediction.prediction_lasso", "prediction.prediction_ridge",
         "prediction.prediction_random_forest", "imputation.mice", "resample.smote",
         "resample.oversample", "resample.undersample"},
    )
~~~

gap id 集合是当前红线，不是 xfail 白名单：它断言“这些仍然缺失”，而不是让它们伪装成可达。修好一项时，集合和计数必须同一变更更新；新增能力先由分母元测试暴露。

- [ ] **Step 2: 跑红测**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8/backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_capability_inventory.py -q -k "reachability_guard_pins or guard_derives_its_denominator"
~~~

预期 ImportError: cannot import name capability_reachability_guard。

- [ ] **Step 3: 最小实现**

在 UnreachableCapabilities 之前新增：

~~~python
class CapabilityReachabilityGuard(NamedTuple):
    inventory: tuple[CapabilityContract, ...]
    directly_reachable: tuple[CapabilityContract, ...]
    composition_reachable: tuple[CapabilityContract, ...]
    reachable: tuple[CapabilityContract, ...]
    exempt: tuple[CapabilityContract, ...]
    gaps: tuple[CapabilityContract, ...]


def capability_reachability_guard(
    inventory: Iterable[CapabilityContract] | None = None,
) -> CapabilityReachabilityGuard:
    items = tuple(capability_inventory() if inventory is None else inventory)
    direct = tuple(item for item in items if item.directly_reachable_by)
    composed = tuple(item for item in items if item.composition_reachable_by)
    reachable_ids = {item.capability_id for item in direct} | {
        item.capability_id for item in composed
    }
    reachable = tuple(item for item in items if item.capability_id in reachable_ids)
    exempt = tuple(
        item for item in items
        if item.capability_id not in reachable_ids
        and item.reachability_exempt_reason is not None
    )
    gaps = tuple(
        item for item in items
        if item.capability_id not in reachable_ids
        and item.reachability_exempt_reason is None
    )
    return CapabilityReachabilityGuard(
        inventory=items, directly_reachable=direct,
        composition_reachable=composed, reachable=reachable,
        exempt=exempt, gaps=gaps,
    )
~~~

改造 unreachable_capabilities 接受同一可选 inventory 并委托守卫：

~~~python
def unreachable_capabilities(
    inventory: Iterable[CapabilityContract] | None = None,
) -> UnreachableCapabilities:
    report = capability_reachability_guard(inventory)
    return UnreachableCapabilities(exempt=report.exempt, gaps=report.gaps)
~~~

在 typing 增加 Iterable，并在 __all__ 导出 CapabilityReachabilityGuard、capability_reachability_guard、CAPABILITY_REACHABILITY_EXEMPTIONS。

- [ ] **Step 4: 跑守卫测试**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8/backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_capability_inventory.py -q
~~~

预期全文件 PASS，并保存 54/4/30/34/2/18。18 条仍在 report.gaps，不使用 xfail；严格全可达断言应在 P5 完成前失败。

- [ ] **Step 5: 验活**

1. 把 items 的三元条件改成只读 capability_inventory；运行 guard_derives_its_denominator，预期 55 变 54 并失败。
2. 把 direct 推导改成空 tuple；运行 reachability_guard_pins，预期 4/34 变为 0/30 并失败。
3. 把 composed 推导改成空 tuple；预期 30/34 变为 0/4 并失败。
4. 把 gaps 的 reason is None 改成 reason is not None；预期 gap 集合错误地变成两个关闭项并失败。

每次突变前 assert 锚点存在且文件改变；恢复后重跑全 test_capability_inventory.py 与 git diff --check。

- [ ] **Step 6: 提交**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8 && git add backend/workbench/agent/capability_contract.py tests/test_capability_inventory.py && git commit -m "test(capability): pin honest natural-language reachability"
~~~

### Task 4：回归、范围核验与收口

**Files:** 无新增生产文件；只做验证。

- [ ] **Step 1: 相关消费者回归**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8/backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_capability_inventory.py ../tests/test_agent_capabilities.py ../tests/test_notebook_planning_agent.py -q
~~~

若旧消费者把 proposed_by 当成注册 operation 而不是自然语言入口，修测试/解释到真实语义；不恢复乐观路径。

- [ ] **Step 2: backend 全量（从仓库根）**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8 && PYTHONPATH=$PWD/backend ./.venv/bin/python -m pytest tests -q \
  --ignore=tests/test_cs_did_oracle.py \
  --ignore=tests/test_sa_did_oracle.py \
  --ignore=tests/test_cs_did_clustering.py
~~~

验收：退出码 0，计数不低于 BE 4824；三个被 ignore 的 DID oracle 缺 fixture 是协议明确的环境例外。

- [ ] **Step 3: 范围与突变残留**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8 && git diff --check && git status --short && git diff --stat a6a8c31..HEAD
~~~

只允许出现本计划列出的三个文件：计划文档、capability_contract.py、test_capability_inventory.py；不应有临时脚本、突变残留或 P0/P1 地基修改。

- [ ] **Step 4: full gate 可用性**

Full gate 只能在宿主终端运行；不要在 agent 沙箱内把 containment 假失败报告成产品失败。若宿主终端可用：

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8 && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 bash scripts/gate.sh --full
~~~

若当前会话不能提供宿主终端，只报告 NOT VERIFIED，不声称 gate 通过，也不改 gate 代码。

- [ ] **Step 5: 最终检查与本地 commit**

~~~bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8 && git status --short --branch && git diff --check && git diff --stat
git add docs/superpowers/plans/2026-08-08-v1.8.8-p2-reachability-guard.md backend/workbench/agent/capability_contract.py tests/test_capability_inventory.py
git commit -m "feat(capability): make natural-language reachability fail honestly"
~~~

不执行 push、PR、merge 或 tag。提交后再次确认 git status --short --branch 干净，并保存 commit SHA 给 Claude 核验。

## 计划自审

- [x] 覆盖 P2 前置：natural_language_enabled、直接/组合区分、能力层枚举、显式豁免、分母元测试、18 条缺口记录。
- [x] 选择已明确：方案 1；没有 xfail，没有把 P5 缺口实现偷放进 P2。
- [x] 当前事实已核实：54 总能力、4 个自然语言 operation、model.genesis=False、34/54 目标可达、2 个有意关闭、18 个真实缺口。
- [x] 没有手写第二份能力或 workflow step 清单；唯一手写集合是两个显式关闭理由，以及用于钉住当前结果的测试快照。
- [x] 验活脚本要求锚点存在且文件确实改变，避免 no such file 被误报为突变证据。
- [x] 没有生产回退：无效 operation/step 路径不计为可达；source、orchestrator、统计结果、前端均不在范围内。

## 留给后续

- P2 不修 18 条真实缺口；P5 负责补齐并在同一变更更新 18 条记录。
- P2 不接入 P7 的 29 个 pack；P6 需按 P7 adoption spec 重新核验 operation 粒度、power analysis、非 DataFrame 输入适配。
- P2 不做真机 Agent 端到端、前端 changeSummary 或 full gate 的宿主执行；P7 负责真机验收。
- P2 不把 model.genesis 改成顶层自然语言 operation；它只修正清单不再把它冒充成直接入口。
