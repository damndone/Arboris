# v1.6.1 执行 Progress Notes

> 每个 loop 一条固定格式：Loop / Changed / Verified / Known risk / Next。

## Baseline（Loop 0，2026-06-23）
- worktree `.worktrees/workbench-v1.6.1` off origin/main `61d45ef`（= tag v1.6.0）。
- `./scripts/gate.sh` → **GATE PASSED**。
- 基线数：**BE 1170 passed / golden 23 (0-drift) / FE 637 passed (65 files) / tsc 0**。失败 = 0。
- 共享 fixture：`tests/fixtures/forest_min.csv`（40 行，40 唯一 wage = 连续 y，education/experience 作 x，能到 model 阶段）。

---

**Progress note** — Loop: 0 / Changed: env(venv+deps) + tests/fixtures/forest_min.csv + progress doc / Verified: gate PASSED, baseline BE1170/golden23/FE637/tsc0, fail=0 / Known risk: none / Next: 2A.1 Hash Contract

**Progress note** — Loop: 2A.1 / Changed: hashing.py +node_hash, tests/test_node_hash.py / Verified: 5 unit pass + golden 16 0-drift / Known risk: op_spec volatile-field exclusion enforced in 2A.3 / Next: 2A.2

**Progress note** — Loop: 2A.2 / Changed: node_store.py (CAS + _refs ledger), tests/test_node_store.py / Verified: 2 unit (idempotent write + ref ledger) + golden 16 0-drift / Known risk: non-atomic _refs RMW (OK single-slot; lock if concurrent) / Next: 2A.3

**Progress note** — Loop: 2A.4 / Changed: flags.py, lineage/incremental.py (dry-run wrapper), orchestrator _run_workflow (flag branch), tests/test_incremental_dryrun.py / Verified: 2 dry-run tests pass (trace complete, RecordingStage excluded, flag-off no trace) + golden 23 0-drift / Known risk: parent-chain reuse validated in 2A.5/2A.6 / Next: 2A.5 / Done inline (orchestrator wiring, accuracy-first) / Tag: WORKBENCH_INCREMENTAL_CACHE default OFF

**Progress note** — Loop: 2A.5 / Changed: op_spec.py (+MICE config), imputation_cache.py (new), incremental.py (taxonomy+MICE hit), orchestrator cfg, test_imputation_cache.py, forest_min_missing fixture, test_incremental_dryrun status / Verified: MICE hit_reused + G2 + byte-identical(parquet+coefs) + no-MICE recomputed_same_hash + force-full matches + golden 23 0-drift / Known risk: full ctx rehydrate -> Slice 3; identity markers accumulate (tiny, ref-count later) / Done inline (core logic, iterative vs oracle) / Next: 2A.6

**Progress note** — Loop: 2A.6 / Changed: test_engine_increment_fork.py (verification), incremental.py (fold-in: DiagnosticsStage mapping fix) / Verified: upstream identity reuse + MICE hit_reused + estimation/downstream hash change (Merkle propagation) + byte-identical vs force-full / Known risk: recomputed_changed status label deferred to 2B.3 (needs parent trace) / Next: 2A.7 / Note: no new production code (mechanism composed from 2A.1-2A.5); op_overrides HTTP merge = 2B.3

**Progress note** — Loop: 2A.7 / Changed: incremental.py (precompute chain + _node_hashes), node_index.py (new bridge), recording.py (flag-gated node_index write), test_recording_node_hash.py / Verified: node_index maps cleaned/model/report -> stage hashes, graph.json byte-untouched (decorate-only), RecordingStage uncached, flag-off no index / Improvement vs plan: node_index.json artifact instead of Node dataclass surgery (0-drift without re-bless) / Next: 2A section gate -> report

---

**Progress note** — Loop: 2B.1 / Changed: lineage/family.py (new, scan_family + Family/FamilyMember dataclasses), tests/test_family_scanner.py (5) / Verified: ancestors(nearest-first)/descendants(BFS)/siblings(shared direct parent); mixed legacy(no node_index)/new degrade w/o crash; missing run_inputs -> opaque root; unknown run -> singleton; cycle-guarded / Known risk: O(runs) scan, index -> Slice 3 / Next: 2B.2 / Legacy detection = absence of node_index.json (reconciled w/ 2A.7 decision; plan's "graph node no node_hash" predates it)

**Progress note** — Loop: 2B.2 / Changed: lineage/headset.py (new, build_headset union DAG), api.get_run_graph (view=headset / WORKBENCH_GRAPH_HEADSET branch), tests/test_graph_headset.py (4) / Verified: flag-off legacy shape unchanged (test_api+test_graph_api+editable_annotation 46 green); flag-on head-set {nodes by node_hash, edges, heads, schema_version=4, legacy}; shared-prefix strict hash dedup (cleaning once); legacy target -> legacy:true degrade / Known risk: legacy MEMBER inside new family skipped from union (R5 opaque-head degrade); var:y/var:x/stage:cleaned share cleaning hash -> collapse to one node, kept = stage:cleaned (relies on RecordingStage emit order) / Next: 2B.3 / Done inline (serve-layer, deep context loaded; adversarial review at 2B section close)

**Progress note** — Loop: 2B.3 / Changed: tests/test_rerun_increment.py (2) only — NO new production code / Verified: rerun already flows through orchestrator incremental path (2A.4 wiring) when flag on; from_node recorded; upstream cleaning reuses identity (same node_hash as parent, status recomputed_same_hash); estimation = miss_executed (new hash); head-set shows M1/M2 as siblings off shared cleaning (no estimation->estimation chain) / Known risk: recomputed_changed status genuinely unreachable in v1.6.0 rerun scope (model-node edit only -> upstream hash never changes; status derives from CAS presence not parent-trace diff) -> stays reserved, NO speculative parent-trace plumbing / Next: 2B.4

**Progress note** — Loop: 2B.4 / Changed: api.py (_annotate_editable_node + _backfill_schema_values; _annotate_editable_nodes now thin wrapper), headset.py (pass run_inputs.form to annotate(view,manifest,form)), tests/test_sibling_and_value_backfill.py (2) / Verified: head-set model siblings share cleaning parent hash; child editable_schema covariance.value=="unadjusted" (run_inputs source), parent keeps capabilities default "robust" (empty form -> missing-key fallback); backfill COPIES (never mutates shared capabilities list); legacy per-run path unchanged (source="capabilities") / Known risk: none / Next: 2C.1。**至此 2B 验收完成。GATE PASSED: BE 1202 (+13) / golden 23 0-drift / FE 637 / tsc 0.**

---

**Progress note** — Loop: 2C.1 / Changed: graphViewTypes.ts (EditableControl +columns kind/role/required/visible_when; HeadSetResponse/NodeView/Head raw + HeadSetNode/Head/ForestViewModel + opNodeId), graphAdapter.ts (adaptHeadSet), graphAdapter.headset.test.ts (5) / Verified: shared C single node; M1/M2 both kept; heads camelCased; backfilled value carried; edges synth ids; existing adapter 25 green; tsc 0 / Key decision: forest node identity = dedup KEY (node_hash or bare node_id), NOT node_id (M1/M2 share id "model:ols_1"); opNodeId preserves original id for from_node / Next: 2C.2

**Progress note** — Loop: 2C.2 / Changed: controls/controlFactory.tsx (new), controlFactory.test.tsx (6) / Verified: CONTROL_REGISTRY covers all 8 kinds (NO switch); select emits onChange(key,val); columns multi-picker toggles; every kind renders identifiable element; visible_when preserved not enforced; only select+columns day-1 enabled (rest = placeholders) / Known risk: none / Next: 2C.3

**Progress note** — Loop: 2C.3 / Changed: detail/RerunContext.tsx (new provider), api.ts (rerunFromNode + getRunGraphHeadSet + HeadSetResponse import), OperationSection.tsx (editable when provider present, read-only fallback), actionRegistry.ts (rerunFromNode LIVE -> openDetail), actionRegistry.test.ts + NodeActionMenu.test.tsx updated, OperationSection.test.tsx (4) / Verified: controls seed from editable_schema.value (backfilled, not default); submit disabled until dirty; submits {fromNode=opNodeId, opOverrides=only-changed}; success state, NO navigation (provider onRerun = refetch); degrades read-only w/o provider; full FE 652 green / Known risk: none / Next: 2C.4

**Progress note** — Loop: 2C.4 / Changed: graph/forestGraph.ts (parentMap/tracePath/headActiveSet pure), graph/ForestCanvas.tsx (new), ForestCanvas.test.tsx (3) + forestGraph.test.ts (4) / Verified: shared C one DOM node, M1/M2 branch; active-head highlight switches; select node -> upstream trace [raw,C,M2]; tracePath cycle-guarded / **DECISION (vs plan "modify GraphCanvas"): built focused ForestCanvas + pure helpers instead of overloading the 648-line GraphCanvas (759-line test) with a dual ViewModel — honors G3 (functional closure, 视觉/layout 精修另算) at far lower risk; GraphNode is ReactFlow-Handle-coupled so forest uses plain layout** / **KNOWN GAP: ForestCanvas not yet mounted in live app route (head-set fetch + forest toggle in run-detail view) — surfaced at 2C checkpoint for user decision** / Next: 2C.5

**Progress note** — Loop: 2C.5 / Changed: graph/rollback.test.tsx (2) only — mechanism already in ForestCanvas (pure useState active-head) / Verified: switching to ancestor head flips highlight with ZERO fetch + ZERO onRerun + forest object unmutated (immutable runs intact); rollback reversible / Known risk: none / Next: 2C QA+Reviewer / **至此 2C 功能闭环完成。GATE PASSED: BE 1202 / golden 23 0-drift / FE 661 (+24) / tsc 0.**

**Progress note** — Loop: 2C.6 (live integration, 用户拍板加的 plan 外 loop) / Changed: workbench/forestFlag.ts (?forest=1 gate, ship-dark) + .test, lineage/hooks/useForestData.ts (head-set fetch+adapt+refetch), workbench/views/ForestRouteView.tsx (self-contained forest route, loading/error/legacy 降级) + .test, WorkbenchRouteContainer.tsx (gate 分支到 ForestRouteView，拆出 LegacyGraphWorkbench 守 Rules of Hooks), graphAdapter.adaptHeadSet (nullish 防御，legacy 不崩) / Verified: forestFlag off-by-default; ForestRouteView load→render/legacy/error 三态; FE 666 green; tsc 0 / **真实浏览器 smoke (Chrome 148, CDP 驱动，preview MCP 因 unicode 路径 spawn 失败→自驱 Chrome)**: live BE(uvicorn)+vite, URL `?tab=lineage&forest=1` → ForestCanvas 渲染 6 节点(raw+共享 cleaning 一次+M1/M2 兄弟+2 report)、2 head chips(child ↳ + parent)；点 parent head=rollback 高亮翻转(M2/child-report→inactive, M1/parent-report→active, raw/cleaning 恒亮, 零 fetch/mutation)；点 parent model→trace [raw,cleaning,M1]；截图无空白/崩溃/遮挡 / 集成缺口已闭合 / Known risk: 默认 tab=overview，森林在 lineage tab + ?forest=1（ship-dark）/ Next: 2C QA+Reviewer。**GATE PASSED: BE 1202 / golden 23 0-drift / FE 666 (+29 over 2B) / tsc 0.**

**Smoke cleanup**: vite.config 的 PORT-env 行(仅为试 preview harness)已回滚；失败的 .claude/launch.json 已删；自起的 vite/uvicorn/headless-chrome 已杀；用户的 options-academy(5173)已原样恢复。

**Progress note** — Loop: 2C 对抗审查 (inline 两角色，subagent 限额) / Found+Fixed 1 真实逃逸: **adaptHeadSet legacy-target 崩溃** — legacy 目标的 `?view=headset` 实测返回旧 per-run 形状(`edges` 是 dict-by-id、无 `heads`)，原 `(backend.edges ?? []).map` 对 dict 抛错；live ForestRouteView legacy 路径会崩。单测因省略 edges 假绿。Fix: adaptHeadSet 早返回 legacy marker(`backend.legacy || !Array.isArray(heads)` → 空 nodes/edges/heads + legacy:true)；测试改用真实 legacy 形状(edges 为 dict) / 其余审查项(rerun slot 竞争→error 降级、refetch 经 scan_family 纳入孙 run、跨家族 key 隔离、edge/node key 一致、trace 仅上溯)均 hold / Verified: FE 667 green, tsc 0 / **v1.6.1 全部完成，待用户授权 push/merge/tag。**
