# Workbench v1.8 Browser Debug Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the raw VIXCLS browser workflow complete, recoverable, graph-auditable, comparable, reportable, and fully visible without broadening the v1.8 estimation scope.

**Architecture:** Repair existing seams in four independently testable waves: form/Genesis input,
truthful failure recovery and Agent context, graph/Compare/Report integrity, then chart accounting
and export. Runtime contract hashes retain their execution meaning; new behavior is additive or
uses compare-only derived identities.

**Tech Stack:** FastAPI/Pydantic, Python lineage and Agent services, React/TypeScript/Vitest, the
existing ARMA–GARCH pack and inline SVG chart components.

---

### Task 1: Explicit missing policy and canonical transform preflight

**Files:**
- Modify: `frontend/src/runForm/ArmaGarchControls.tsx`
- Modify: `frontend/src/runForm/ArmaGarchControls.roundtrip.test.tsx`
- Modify: `frontend/src/runForm/ArmaGarchControls.test.tsx`
- Modify: `frontend/src/runForm/RunForm.test.tsx`
- Modify: `backend/workbench/api.py` or the existing model-preflight router selected after inspection
- Reuse: `backend/workbench/engine/packs/arma_garch/input.py`
- Reuse: `backend/workbench/engine/packs/arma_garch/transforms.py`
- Test: `tests/models/arma_garch/test_missing_value_policy.py`
- Test: a focused API preflight test beside existing run-form API tests

- [ ] Add a frontend regression test proving `drop_missing_confirmed` is explicitly selected,
  serialized, and restored; run it and observe the missing control failure.
- [ ] Add an API test sending raw VIX-like rows with blanks and asserting that preflight uses the
  existing complete-case analysis view and full-data transform profiles; run it red.
- [ ] Implement the smallest form wiring and read-only preflight adapter over existing pack code.
- [ ] Keep transform confirmation separate from recommendation and show the evidence source.
- [ ] Run focused frontend/backend tests green, then run the existing missing-value suite.

### Task 2: Genesis uses the ARMA–GARCH univariate contract

**Files:**
- Modify: `frontend/src/lineage/drafts/GenesisWizard.tsx`
- Modify: `frontend/src/lineage/drafts/GenesisWizard.test.tsx`
- Modify: `backend/workbench/lineage/pipeline_drafts.py`
- Modify: `tests/test_pipeline_drafts_genesis.py`

- [ ] Write a frontend test selecting ARMA–GARCH, one time column, and one value column and proving
  save becomes available with `x=[]` and the same `model_options` produced by the run form; run red.
- [ ] Write a backend validator test accepting that univariate shape while retaining x requirements
  for multivariate model families; run red.
- [ ] Reuse `ArmaGarchControls` and its contract builder; add only the model-specific save guard and
  backend exemption.
- [ ] Run both focused suites green and verify LMM/OLS Genesis tests are unchanged.

### Task 3: Truthful failed model identity and bounded Agent recovery

**Files:**
- Modify: `backend/workbench/lineage/node_index.py`
- Modify: the ARMA–GARCH failure/recording boundary in `backend/workbench/orchestrator/` or pack runner
- Modify: `backend/workbench/agent/context_tools.py`
- Modify: `backend/workbench/agent/orchestrator.py`
- Modify: failed-node UI context/Run-code section identified by focused search
- Test: `tests/test_recording_node_hash.py`
- Test: `tests/test_agent_arma_garch_proposal.py`
- Test: `tests/test_agent_arma_garch_vocabulary.py`
- Test: relevant frontend node-action test

- [ ] Write a failure-injection test proving materialized nodes and an explicitly failed configured
  model receive hashes, while no fitted/result fields are present; run red.
- [ ] Write an Agent test proving the failed model can accept the existing typed `model.rerun`
  missing-policy patch and that unsupported probes return one stable visible error; run red.
- [ ] Implement partial index/tombstone recording at the structured failure boundary and expose
  immediate pack capability context. Do not add a generic row-filter operation.
- [ ] Add a frontend assertion that disabled Run-code actions display the backend reason.
- [ ] Run failure-manifest, cancellation, Graph-write-failure, Agent, rerun, and code-execute tests.

### Task 4: Generated stage identity and action gating

**Files:**
- Modify: `backend/workbench/lineage/node_index.py`
- Modify: `backend/workbench/engine/packs/arma_garch/runner.py`
- Modify: `frontend/src/lineage/detail/sectionRegistry.tsx`
- Modify: `frontend/src/lineage/detail/LineageChainSection.tsx`
- Test: `tests/test_recording_node_hash.py`
- Test: ARMA–GARCH graph runtime tests
- Test: frontend lineage/detail section tests

- [ ] Write a backend test asserting every generated `stage:ts-*` node has a stable hash and only
  stage-local artifact references; run red.
- [ ] Write frontend tests proving stage nodes show lineage/Ask AI but never terminal result,
  rerun, or Compare actions; run red.
- [ ] Extend the pack-owned node-index bridge without changing `graph.json` or artifact ownership.
- [ ] Restrict result/operation registration to canonical `kind==model` and add graph-edge fallback.
- [ ] Run graph/index, drawer, rerun, Compare-selection, and golden-focused tests green.

### Task 5: Compare membership identity and time-series Report facts

**Files:**
- Modify: `backend/workbench/services/compare_node_service.py`
- Modify: the ARMA–GARCH compare packet builder under `backend/workbench/`
- Modify: Report fact extraction/validation under `backend/workbench/report/` and
  `frontend/src/report/factTable.ts`
- Test: `tests/test_compare_node_api.py`
- Test: `tests/agent/test_arma_garch_agent_compare.py`
- Test: Report fact tests in backend/frontend

- [ ] Add a Compare regression fixture copied from the real manual/auto split receipts, proving
  equal membership is comparable despite different model contracts; run red.
- [ ] Add lineage-relation tests: ancestor mismatch blocks, unrelated project peers with equal
  evidence pass, and Compare still never writes either `graph.json`; run red.
- [ ] Preserve runtime `split_hash`; derive a compare-only membership identity and pass the actual
  forest relation into validation.
- [ ] Add Report tests for bounded scalar time-series facts and for skipping/rejecting structured
  values so `[object Object]` cannot render; run red.
- [ ] Implement the whitelist and validation without composite IC or VaR semantics.
- [ ] Run Compare integrity, project-forest, report-citation, and frontend Report suites green.

### Task 6: Complete artifact states and native SVG export

**Files:**
- Modify: `frontend/src/runResult/ArmaGarchChartGallery.tsx`
- Modify: `frontend/src/runResult/ArmaGarchCharts.tsx`
- Modify: related chart component tests

- [ ] Write tests requiring a visible state for all 17 artifact keys, including zero exceptions,
  object-form model comparison, unavailable payloads, and one SVG download; run red.
- [ ] Introduce a small payload-state helper that distinguishes unavailable, malformed, valid empty,
  row-series, and summary payloads.
- [ ] Render zero-exception and model-comparison panels and serialize the existing displayed SVG on
  download. Label displayed SVG versus full structured JSON evidence.
- [ ] Run the complete ARMA–GARCH frontend result/chart suite and typecheck green.

### Task 7: Integrated gate and real browser acceptance

**Files:**
- Modify: `docs/superpowers/handoffs/2026-07-21-v1.8-vix-browser-debug-closure.md`
- Update: `findings.md`, `progress.md`, `task_plan.md`
- Update: existing Failure Memory System artifact selected by repository instructions

- [ ] Run the focused backend suites for ARMA–GARCH, Agent, lineage, Compare, Report, Genesis, and
  failure injection with no development server active.
- [ ] Run frontend Vitest suites and `npm run typecheck`.
- [ ] Run `bash scripts/gate.sh --full` alone; record exact test totals, golden drift, duration, and
  peak memory from the gate output.
- [ ] Start Workbench once, use the committed raw VIXCLS file, and perform the approved manual and
  automatic browser journeys with mouse interactions.
- [ ] Use DeepSeek V4 Pro for one real failed-run recovery Agent session; preserve prompt, events,
  proposed typed operation, confirmation, and child run result.
- [ ] Verify successful Compare, Report generation, six stage drawers, all 17 artifact states, and
  an SVG download. Save screenshots and immutable evidence paths.
- [ ] Stop servers, rerun any browser-sensitive focused tests, and write the final handoff/FMS entry
  with unresolved limitations stated explicitly.

## Self-review

- Every observed browser defect maps to exactly one task and a red/green test.
- The plan reuses existing missing-policy, rerun, graph-index, report, and SVG seams.
- Runtime split integrity and Compare sample identity remain separate.
- Failed model identity is explicitly failed and cannot masquerade as a completed fit.
- Joint estimation, generic row filtering, a new chart framework, and release publication remain
  outside scope.
