# V1.5.1 — Visual Alignment & Live Run Implementation Plan

| | |
|---|---|
| **Plan ID** | `v1.5.1-visual-alignment-plan` |
| **Date** | 2026-05-25 |
| **Status** | Draft for three-party review |
| **Spec** | [`2026-05-25-v1.5.1-visual-alignment-design.md`](../specs/2026-05-25-v1.5.1-visual-alignment-design.md) |
| **Worktree** | `.worktrees/workbench-v1.5.1` (created from `workbench-v1.5.0` `6a7bc5e`) |
| **Branch** | `workbench-v1.5.1` |
| **Rebase plan** | Rebase onto `main` after V1.5.0 PR [#6](https://github.com/damndone/econometrics-Workbench/pull/6) merges |
| **Baseline** | FE 333 / BE 618 passing as of V1.5.0 sign-off (commit `6a7bc5e`) |
| **Wall-clock budget** | 14–17 working days (~3 weeks) |

---

## 1. How to read this plan

### 1.1 Task ID convention

`T<track>.<n>` where `<track>` ∈ {1, 2, 3, 4, 5}. T0 is pre-flight.

- T1.x → SSE + Progress
- T2.x → Auto-select first review
- T3.x → LeftRail
- T4.x → Topbar
- T5.x → Polish bundle

### 1.2 Effort sizing

| Size | Wall-clock budget |
|---|---|
| **S** | < 30 min |
| **M** | 30–90 min |
| **L** | 90–180 min |
| **XL** | half a day (use sparingly) |

If a task overruns 2× its budget, **stop** and escalate. Don't push through with thrashing.

### 1.3 Per-task fields

- **Owner**: FE-A / FE-B / BE / shared
- **Effort**: S / M / L / XL
- **Touched files**: exact paths
- **Steps**: sequential, no decision points (those are in the spec)
- **DoD**: observable test or behaviour; no "looks good"
- **Depends on**: upstream task IDs that must be green first

### 1.4 Per-track gate (three-level review)

Per memory `feedback_review_workflow.md`:

1. **Implementer** completes all tasks in track; full FE + BE suites green locally
2. **Test & QA** independently runs full suites + browser smoke for visual tracks
3. **Reviewer** reads full diff, runs smoke independently, signs off
4. **Push** branch only after reviewer signs off

Each track ships one push. T<n+1> starts only after T<n> push.

### 1.5 Execution order

```
T0 pre-flight  →  T5 polish  →  T1 SSE  →  T2 auto-select  →  T3 LeftRail  →  T4 Topbar
   (0.5d)        (2d)          (3d)        (0.5d)            (5-7d)          (3-4d)
```

T5 first because: smallest blast radius, lowest risk, builds team rhythm for the sprint. T1 second because: closes the highest-priority vulnerability and unblocks confidence in the polling code path before we add UI on top. T3 and T4 are the visual heavy lifts; running them last means they benefit from any infrastructure stabilisation T1/T5 produced.

### 1.6 Test count tracking

| Track | FE delta | BE delta | Running FE | Running BE |
|---|---|---|---|---|
| Baseline | — | — | 333 | 618 |
| T5 | +1 (lastRun persistence) | +3 (UI origin resolver) | 334 | 621 |
| T1 | +3 (SSE happy + fallback + abort) | 0 | 337 | 621 |
| T2 | +5 (review priority, all-caution, all-ok, explicit-wins, no-re-select) | 0 | 342 | 621 |
| T3 | +9 (LeftRail 5 + useStageCounts 4) | 0 | 351 | 621 |
| T4 | +3 (Topbar comp 5 minus 2 already counted under App.test) | 0 | 354 | 621 |
| **End** | **+21** | **+3** | **354** | **621** |

---

## 2. T0 — Pre-flight (done before this plan started)

### T0.1 — Worktree creation (DONE)

- Worktree `.worktrees/workbench-v1.5.1` created from `origin/workbench-v1.5.0` (commit `6a7bc5e`).
- Branch `workbench-v1.5.1` exists locally; tracks `origin/workbench-v1.5.0` for now (will retarget to `origin/workbench-v1.5.1` on first push).
- Baseline verified: FE 333, BE 618.

### T0.2 — Push branch + open draft PR

- **Owner**: shared
- **Effort**: S
- **Steps**:
  1. After this plan + spec land as the first commit on the branch, `git push -u origin workbench-v1.5.1`
  2. `gh pr create --draft --base main --head workbench-v1.5.1 --title "V1.5.1 — Visual Alignment & Live Run [WIP]"` with body linking to spec + plan
- **DoD**: Draft PR open
- **Depends on**: spec + plan committed

### T0.3 — V1.5.0 PR merge → rebase

- **Owner**: shared
- **Effort**: S
- **Steps**:
  1. Wait for V1.5.0 PR [#6](https://github.com/damndone/Econometrics-Workbench/pull/6) to merge
  2. `git fetch origin && git rebase origin/main`
  3. Push with `--force-with-lease`
- **DoD**: V1.5.1 branch sits on top of V1.5.0-merged main; no conflicts
- **Depends on**: V1.5.0 PR merge

---

## 3. Track 5 — Polish bundle (2 days)

Done first because lowest risk and unblocks `/runs` dark chrome (which Tracks 3 and 4 depend on visually).

### T5.1 — `lastRun` persistence via AppShellContext

- **Owner**: FE-A
- **Effort**: M
- **Touched files**:
  - `frontend/src/AppShellContext.tsx` (NEW if missing; otherwise edit existing)
  - `frontend/src/AppShell.tsx`
  - `frontend/src/App.tsx`
  - `frontend/src/App.test.tsx`
- **Steps**:
  1. Grep for existing AppShell context. If missing, create `AppShellContext` with shape `{ lastRun: RunSummary | null; setLastRun: (r) => void }`.
  2. On Provider mount, read `sessionStorage["workbench:lastRun"]` → seed state.
  3. On `setLastRun` call, JSON-stringify into sessionStorage.
  4. Replace App.tsx local `useState` with `useAppShellContext().lastRun`.
  5. Add test: render → submit run → navigate `/runs/x` → navigate `/` → assert hint card still visible.
- **DoD**: 1 new test passes; manual: hint card survives round-trip navigation
- **Depends on**: T0.2

### T5.2 — `/runs` history dark chrome

- **Owner**: FE-A
- **Effort**: S
- **Touched files**:
  - `frontend/src/App.tsx` (regex update)
  - `frontend/src/App.test.tsx`
  - `frontend/src/styles.css` (verify nothing breaks)
- **Steps**:
  1. Locate the `workbench-shell--lineage` modifier toggle (currently regex `/^\/runs\/[^/?#]+$/`).
  2. Change to `/^\/runs(?:\/[^/?#]+)?$/` so both `/runs` and `/runs/<id>` match.
  3. Add code comment explaining the route scope.
  4. Add App.test.tsx assertion that `/runs` route has dark modifier class.
  5. Manual verify: history list rows readable on dark; no contrast regression.
- **DoD**: 1 new test passes; visual smoke L28 added
- **Depends on**: T5.1

### T5.3 — `WORKBENCH_UI_ORIGIN` smart fallback

- **Owner**: BE
- **Effort**: M
- **Touched files**:
  - `backend/workbench/cli.py`
  - `tests/test_cli.py`
- **Steps**:
  1. Add `_resolve_ui_origin()`:
     - If `os.environ.get("WORKBENCH_UI_ORIGIN")` set, return it.
     - Else check cache file `~/.workbench/ui_origin_cache.json` (1 h TTL).
     - Else TCP probe `localhost:5173` with 100 ms timeout. If reachable, return `http://localhost:5173`, write cache.
     - Else return `http://localhost:8000`, write cache.
  2. Replace inline `WORKBENCH_UI_ORIGIN` reads in `_lineage_url(...)` to call resolver.
  3. Add 3 tests:
     - Env var set → that value returned (skip probe)
     - `:5173` mocked open → `:5173` returned
     - `:5173` mocked closed → `:8000` returned
  4. Mock `socket.create_connection` in tests; don't hit real network.
- **DoD**: 3 new BE tests pass; manual: with no dev server, CLI prints `http://localhost:8000/runs/...`
- **Depends on**: none (parallel-safe with T5.1/T5.2)

### T5.4 — Track 5 review gate

- **Owner**: shared (Implementer + QA + Reviewer)
- **Steps**:
  1. Implementer: full FE 334 + BE 621 green locally
  2. Test & QA: run full suites; walk smoke L27–L29
  3. Reviewer: diff review, run independent smoke
  4. Commit + push
- **Depends on**: T5.1, T5.2, T5.3

---

## 4. Track 1 — SSE + Progress (3 days)

### T1.1 — Refactor `waitForRunTerminal` internals

- **Owner**: FE-B
- **Effort**: L
- **Touched files**:
  - `frontend/src/api.ts`
- **Steps**:
  1. Extract polling loop into private `_pollUntilTerminal(...)`.
  2. Add private `_subscribeUntilTerminal(...)`:
     - Open `connectRunEvents(projectRoot, runId, callbacks)`.
     - On every event, build `lastEvent: RunProgressEvent` and call `opts.onTick?.(detail, lastEvent)`. (Need to fetch `detail` lazily — only on terminal — to avoid extra HTTP.)
     - On terminal event, `await fetchRunDetail(...)` once and resolve.
     - On EventSource error, close handle and reject with a marker error.
  3. New `waitForRunTerminal(...)` body:
     - If `typeof EventSource === "undefined"`, return `_pollUntilTerminal(...)`.
     - Else try `_subscribeUntilTerminal(...)`. On marker-error reject, fall through to `_pollUntilTerminal(...)` for remaining time.
  4. Update `onTick` JSDoc to mention optional `lastEvent` second arg. **Signature stays callable with single arg** (TS optional param) — no breaking change for callers that don't care about events.
  5. Bump default `maxMs` from 5 min → 30 min (rationale: SSE is cheap, polling fallback keeps 5 min).
- **DoD**: `api.ts` compiles; no test changes yet (T1.2)
- **Depends on**: T5.4

### T1.2 — Update + extend `api.test.ts`

- **Owner**: FE-B
- **Effort**: L
- **Touched files**:
  - `frontend/src/api.test.ts`
- **Steps**:
  1. Update 4 existing `waitForRunTerminal` tests: they currently rely on `typeof EventSource === "undefined"` in jsdom (since vitest.setup.ts may or may not provide it — verify). If jsdom doesn't ship EventSource (typical), tests should pass unchanged thanks to fallback. If a global stub exists, ensure tests opt out via `vi.stubGlobal("EventSource", undefined)`.
  2. Add **T1.2.a** — SSE happy path: stub EventSource (use the MockEventSource pattern already in `App.test.tsx:968`), fire `step_start` → `workflow_completed` events, mock `fetchRunDetail` for terminal lookup, assert promise resolves with terminal detail and `onTick` was called with `lastEvent` populated.
  3. Add **T1.2.b** — SSE error → polling fallback: open MockEventSource, fire `onerror` after 100 ms, assert polling kicks in and eventually resolves.
  4. Add **T1.2.c** — Abort signal: open MockEventSource, abort before terminal, assert promise rejects with `AbortError` and EventSource closed.
- **DoD**: 4 updated + 3 new tests pass; total api.test.ts: existing N → N+3
- **Depends on**: T1.1

### T1.3 — Submit page progress UI

- **Owner**: FE-B
- **Effort**: M
- **Touched files**:
  - `frontend/src/App.tsx`
  - `frontend/src/App.test.tsx`
- **Steps**:
  1. Add local state `currentStep: { step: string; message: string } | null`.
  2. Pass `onTick: (_detail, lastEvent) => setCurrentStep({ step: lastEvent.step, message: lastEvent.message })` to `waitForRunTerminal`.
  3. Render below "Running workflow…" line:
     - If `currentStep`: `└─ <step>: <message>` (with `--rail-text` colour for subtlety)
     - Else after 3 s: `└─ Connecting to event stream…`
  4. Test: trigger run, fire `step_start` event via stubbed EventSource, assert step text appears.
- **DoD**: 1 new App.test.tsx assertion passes; manual: real `workbench run` shows step within 1 s
- **Depends on**: T1.2

### T1.4 — Optional: in-progress lineage banner

- **Owner**: FE-B
- **Effort**: M
- **Touched files**:
  - `frontend/src/lineage/LineageRouteContainer.tsx`
  - `frontend/src/lineage/LineageRouteContainer.test.tsx`
- **Steps**:
  1. In `LineageRouteContainer`, on mount: if `runDetail.status === "running"`, open `connectRunEvents(...)` subscription.
  2. Track `lastEvent` in state.
  3. Render thin banner above GraphCanvas: "Run in progress · current step: <name>".
  4. On terminal event, refetch runDetail + close subscription.
  5. Cleanup on unmount.
  6. Test: mount with status=running, fire event, assert banner shows step name.
- **DoD**: 1 new test passes; manual: deep-link to `/runs/x?...` mid-run shows banner
- **Depends on**: T1.3

### T1.5 — Track 1 review gate

- **Owner**: shared
- **Steps**:
  1. Full FE 337 + BE 621 green
  2. Walk smoke L29 (and any earlier L's untouched)
  3. Reviewer signs off, push
- **Depends on**: T1.1–T1.4

---

## 5. Track 2 — Auto-select first review (0.5 day)

### T2.1 — `useSelectedNode` initial effect

- **Owner**: FE-A
- **Effort**: M
- **Touched files**:
  - `frontend/src/lineage/hooks/useSelectedNode.ts`
- **Steps**:
  1. In the existing `useSelectedNode`, identify where `?node=` is read from URL.
  2. Add a new `useEffect` that runs **once after first viewModel load** (not every render):
     - If `searchParams.get("node")` is null AND viewModel.nodes is non-empty:
       - First pass: find first node with `trust === "review"`
       - Second pass: find first node with `trust === "caution"`
       - If found: `setSearchParams({ node: pick.nodeKey }, { replace: true })`
     - If found nothing: do nothing
  3. Ensure the effect doesn't re-fire after user clears selection (use a `didAutoSelectRef = useRef(false)` guard).
- **DoD**: Existing useSelectedNode tests still pass
- **Depends on**: T1.5

### T2.2 — `useSelectedNode.test.ts` cases

- **Owner**: FE-A
- **Effort**: M
- **Touched files**:
  - `frontend/src/lineage/hooks/useSelectedNode.test.ts`
- **Steps**:
  1. Test **mixed-trust fixture**: 3 nodes (ok, caution, review). Mount without `?node=`. Assert URL contains `?node=<review-node-key>` after first render.
  2. Test **caution-only fixture**: 2 nodes (ok, caution). Assert URL contains `?node=<caution-node-key>`.
  3. Test **all-ok fixture**: 2 ok nodes. Assert URL has no `?node=`, selection is null.
  4. Test **explicit `?node=` wins**: mount with `?node=other`, assert no override even if review nodes exist.
  5. Test **back button doesn't re-select**: mount with `?node=review`, user clears (`setSelected(null)`), assert effect doesn't re-add (using `didAutoSelectRef` guard).
- **DoD**: 5 new tests pass
- **Depends on**: T2.1

### T2.3 — Track 2 review gate

- **Owner**: shared
- **Steps**: as standard pattern; FE target 342
- **Depends on**: T2.1, T2.2

---

## 6. Track 3 — LeftRail (5-7 days)

The biggest single piece. Three sub-deliverables: hook (T3.1), component (T3.2-T3.4), integration (T3.5-T3.6).

### T3.1 — `useStageCounts` hook + tests

- **Owner**: FE-A
- **Effort**: M
- **Touched files**:
  - `frontend/src/lineage/leftRail/useStageCounts.ts` (NEW)
  - `frontend/src/lineage/leftRail/useStageCounts.test.ts` (NEW)
- **Steps**:
  1. Create directory `frontend/src/lineage/leftRail/`.
  2. Type:
     ```ts
     export type StageCount = {
       total: number;
       needsReview: number;
       nodes: GraphViewNode[];
     };
     export type StageCounts = Record<Stage, StageCount>;
     ```
  3. `useStageCounts(vm)`:
     - `useMemo` over `vm.nodes`.
     - Initialize all 8 stages + "unknown" with `{ total: 0, needsReview: 0, nodes: [] }`.
     - For each node: bucket into `node.stage`; increment `total`; push to `nodes`; if `node.trust !== "ok"` OR any `node.decisions[*].reviewStatus === "needed"`, increment `needsReview`.
  4. Tests:
     - Empty viewModel → all stages zero
     - One node per stage → counts == 1 each, needsReview reflects trust
     - All in one stage → 8 total in that stage, others zero
     - Review-needed detection from `reviewStatus="needed"` even when `trust="ok"`
- **DoD**: 4 tests pass
- **Depends on**: T2.3

### T3.2 — LeftRail tokens

- **Owner**: FE-A
- **Effort**: S
- **Touched files**:
  - `frontend/src/lineage/tokens/lineage.css`
  - `frontend/src/styles.css` (mirror)
- **Steps**:
  1. Add `--rail-bg`, `--rail-active-bg`, `--rail-text`, `--rail-text-muted`, `--rail-border`, `--review-dot` to `lineage.css` `:root`.
  2. Mirror identical values inside the `.workbench-shell--lineage` block of `styles.css`.
  3. Add the same code-comment warning used for the existing token mirror.
- **DoD**: Both files updated; no test changes
- **Depends on**: T3.1

### T3.3 — `LeftRail.tsx` component

- **Owner**: FE-A
- **Effort**: L
- **Touched files**:
  - `frontend/src/lineage/leftRail/LeftRail.tsx` (NEW)
- **Steps**:
  1. Import `useLineage`, `useStageCounts`, `useReactFlow` from `reactflow`.
  2. Stage order constant: `["source","eda","clean","transform","model","diag","viz","report"]`.
  3. Render 8 `<button>` rows. Each row:
     - Coloured 8 px dot (`background: var(--<stage>)`)
     - Stage label (capitalised)
     - Count (right-aligned, `--rail-text-muted`)
     - Red dot suffix when `counts[stage].needsReview > 0`
  4. `disabled={counts[stage].total === 0}` for empty stages (50% opacity).
  5. Active state: if `selectedNode?.stage === stage`, add `data-active="true"` for CSS targeting.
  6. `onClick`: if disabled, no-op. Else:
     - `const nodes = counts[stage].nodes`
     - `rf.fitView({ nodes: nodes.map(n => ({ id: n.id })), padding: 0.2, duration: 400 })`
     - `setSelected(nodes[0])`
  7. Wrap in `<nav aria-label="Pipeline stages">`.
- **DoD**: Component renders without errors when given a viewModel; visual styling matches spec §6.1
- **Depends on**: T3.2

### T3.4 — `LeftRail.test.tsx`

- **Owner**: FE-A
- **Effort**: M
- **Touched files**:
  - `frontend/src/lineage/leftRail/LeftRail.test.tsx` (NEW)
- **Steps**:
  1. Mock `useReactFlow()` to return `{ fitView: vi.fn() }`.
  2. Wrap renders in a `LineageContext` provider with controllable viewModel.
  3. Tests:
     - **Renders 8 rows in spec order**: assert button labels match `["Source","EDA","Clean","Transform","Model","Diag","Viz","Report"]`.
     - **Count accuracy**: with a 5-node fixture spanning 3 stages, assert displayed counts match.
     - **Disabled state**: stage with `count === 0` has `disabled` attribute; click does not call fitView.
     - **Click → fitView + setSelected**: click "Model" row, assert `fitView` called with Model nodes, assert `selectedNode` updates.
     - **Review dot visibility**: fixture with one caution node in EDA → assert red dot present on EDA row, absent on others.
- **DoD**: 5 tests pass
- **Depends on**: T3.3

### T3.5 — `GraphWorkbench` layout integration

- **Owner**: FE-A
- **Effort**: M
- **Touched files**:
  - `frontend/src/lineage/GraphWorkbench.tsx`
  - `frontend/src/lineage/GraphWorkbench.test.tsx`
- **Steps**:
  1. Wrap current JSX in `<div className="workbench-layout">` with `<LeftRail />`, `<GraphCanvas />`, `<DetailDrawer />` in that order.
  2. CSS: in `lineage.css` (or `lineage/leftRail/leftRail.css` if preferred for isolation), define `.workbench-layout { display: grid; grid-template-columns: 200px 1fr 360px; height: 100%; }`.
  3. Update existing GraphWorkbench.test.tsx assertions: add "LeftRail nav present" check.
- **DoD**: Existing GraphWorkbench tests still pass + 1 new assertion
- **Depends on**: T3.4

### T3.6 — Browser smoke + cleanup

- **Owner**: shared
- **Effort**: M
- **Touched files**:
  - `docs/dev-browser-smoke.md`
- **Steps**:
  1. Add L21 (renders 8 rows with counts)
  2. Add L22 (click → fitView animates canvas)
  3. Add L23 (review dot visible)
  4. Walk smoke against fresh v3 run; capture any visual issues
  5. Cross-check against `uiux/screenshots/initial.png` left rail region
- **DoD**: Smoke L21–L23 pass; screenshot diff documented if any
- **Depends on**: T3.5

### T3.7 — Track 3 review gate

- **Owner**: shared
- **Steps**: standard pattern; FE target 351
- **Depends on**: T3.6

---

## 7. Track 4 — Topbar (3-4 days)

### T4.1 — `Topbar.tsx` component

- **Owner**: FE-B
- **Effort**: L
- **Touched files**:
  - `frontend/src/shell/Topbar.tsx` (NEW)
- **Steps**:
  1. Create `frontend/src/shell/` directory (new namespace for outer-shell concerns).
  2. Props: none (reads `runDetail` from `LineageContext`).
  3. Layout (CSS Grid 3 cols):
     ```
     [workspace › run pill]  [spacer]  [tabs]
     ```
  4. Workspace label: derive `basename(runDetail.project_root)`. If empty: render `(workspace)`. If `runDetail` null: render skeleton.
  5. Run pill: `Run · <runId.split("-").slice(-1)[0]>`. (Take last segment.)
  6. Tabs: 3 buttons. Graph: `aria-current="page"`, no click handler (already on Graph). Table & Pipeline: `aria-disabled="true"`, `title="Coming in V2.0"`, `disabled`.
  7. Style with `var(--rail-*)` tokens for consistency.
- **DoD**: Component renders cleanly for both populated and skeleton states
- **Depends on**: T3.7

### T4.2 — `Topbar.test.tsx`

- **Owner**: FE-B
- **Effort**: M
- **Touched files**:
  - `frontend/src/shell/Topbar.test.tsx` (NEW)
- **Steps**:
  1. **Workspace label**: mount with `project_root=/tmp/wb-smoke` → assert "wb-smoke" rendered.
  2. **Run pill format**: mount with `run_id=2026-05-25-abc123def` → assert "Run · abc123def" rendered.
  3. **Graph active**: assert Graph button has `aria-current="page"`.
  4. **Disabled tabs**: assert Table and Pipeline have `disabled` and `aria-disabled="true"`.
  5. **Tooltip presence**: assert Table and Pipeline have `title="Coming in V2.0"`.
- **DoD**: 5 tests pass
- **Depends on**: T4.1

### T4.3 — Route wiring in `App.tsx`

- **Owner**: FE-B
- **Effort**: M
- **Touched files**:
  - `frontend/src/App.tsx`
  - `frontend/src/App.test.tsx`
- **Steps**:
  1. Locate the `<Route path="/runs/:runId" ...>` declaration.
  2. Wrap element with fragment containing `<Topbar />` above `<LineageRouteContainer />`.
  3. Verify Topbar does **not** mount on `/runs` (history list) or `/` (Submit).
  4. Add 3 App.test.tsx assertions: Topbar present on `/runs/x`, absent on `/runs`, absent on `/`.
- **DoD**: 3 new App.test.tsx assertions pass
- **Depends on**: T4.2

### T4.4 — Styling polish + browser smoke

- **Owner**: FE-B
- **Effort**: M
- **Touched files**:
  - `frontend/src/styles.css`
  - `docs/dev-browser-smoke.md`
- **Steps**:
  1. Add `.workbench-topbar { … }` styles using `var(--rail-*)`.
  2. Verify spacing against `uiux/screenshots/initial.png` topbar region.
  3. Add smoke L24, L25, L26.
- **DoD**: Smoke L24–L26 pass; visual matches reference
- **Depends on**: T4.3

### T4.5 — Track 4 review gate

- **Owner**: shared
- **Steps**: standard pattern; FE target **354**
- **Depends on**: T4.4

---

## 8. Final integration

### TF.1 — Full smoke walkthrough

- **Owner**: shared
- **Effort**: M
- **Steps**:
  1. Walk `docs/dev-browser-smoke.md` L1–L29 against a fresh `workbench run` (use the smoke command from V1.5.0 memory).
  2. Document any regressions in a checklist; fix-forward before flipping PR draft → ready.
- **DoD**: All 29 smoke items pass
- **Depends on**: T4.5

### TF.2 — Release notes

- **Owner**: shared
- **Effort**: M
- **Touched files**:
  - `docs/v1.5.1-release-notes.md` (NEW)
- **Steps**: Mirror `docs/v1.5.0-release-notes.md` structure. Sections: What shipped (per track) · Deferred to V1.5.2 · Verification · Migration · Sign-off.
- **DoD**: File committed
- **Depends on**: TF.1

### TF.3 — Flip PR draft → ready, request review

- **Owner**: shared
- **Effort**: S
- **Steps**:
  1. `gh pr ready <PR#>`
  2. Update PR body with final test counts and smoke results
  3. Tag reviewer
- **DoD**: PR review requested
- **Depends on**: TF.2

---

## 9. Risk register

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| SSE EventSource polyfill needed for older browsers | Low | Medium | Polling fallback already in place; document supported browsers |
| `useReactFlow()` returns null before mount → LeftRail click no-op | Medium | Low | Guard click handler; disable rows until RF instance ready |
| LeftRail + Topbar combined add layout reflow → flicker | Medium | Low | Wrap both in `position: sticky` containers, validate with smoke |
| `_resolve_ui_origin` TCP probe hangs on firewalled `:5173` | Low | Medium | Hard 100 ms timeout + cache |
| Auto-select effect re-fires after user clears selection (UX bug) | Medium | High | `didAutoSelectRef = useRef(false)` guard + unit test for it |
| V1.5.0 PR merge brings conflicts during T0.3 rebase | Low | Medium | Force-with-lease; resolve in clean state on V1.5.1 worktree |
| Test count drift breaks gates | Low | Low | §1.6 tracks running counts; deviation → investigate before commit |

---

## 10. Out of scope (do not attempt in V1.5.1)

Reaffirming spec §1.2:

- Multi-tab DetailDrawer
- `useGraphKeyboard` singleton guard
- AskAI section
- Artifact / Coefficient sections
- Bottom terminal panel
- Trust banner action buttons
- Drawer breadcrumb chips
- React Flow Controls abstraction
- `getDPDisplay` → `getDecisionDisplay` rename
- 798 KB chunk code-split
- Real-world large-dataset perf smoke

If any of the above feels "while we're here" tempting during a Track, **stop and write a follow-up note** in `docs/superpowers/followups/` instead of expanding scope.

---

## 11. Plan amendment log

| Date | Change | Reason |
|---|---|---|
| 2026-05-25 | Initial draft (in V1.5.1 worktree from start) | — |
