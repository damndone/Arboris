# v1.8.8 P0：组合 seam 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让一个 workflow step 能消费上一个 step 产出的持久化数据集，而不是永远读 `draft.target`——在不削弱现有 `context_fingerprint` stale 拒绝语义的前提下。

**Architecture:** step 的 spec 可声明 `source: {from_step, output}`（来源承诺）。编译期校验引用可解析、被引用的 step 确实声明产出数据集、并把 `from_step` 并入 `depends_on` 以复用既有的 DAG 与 fingerprint 机制。执行期从上游 `WorkflowStepResult.payload["produced_dataset"]` 取出真实 `(run_id, node_ref, artifact_id, result_fingerprint)` 注入下游，并断言指纹与执行时记录一致。计划期的指纹传递性**已由 `_make_step` 的 `dependency_fingerprints` 提供**（`workflow.py:373`），本计划不重造。

**Tech Stack:** Python 3.14、pandas、pytest。全部改动在 `backend/workbench/agent/` 下的 `workflow_contracts.py` / `workflow.py` / `workflow_runtime.py`，测试在 worktree 根的 `tests/`。

**为什么用 `statistical.derive_numeric` 做验证载体：** 数据转换要到 P3 才注册，但 P0 必须能独立验收。`statistical.derive_numeric` 是当前**唯一**会持久化数据子节点的 step（`workflow_runtime.py:276` `_persist_numeric_derivation`），因此用它做 seam 的证明链。P3 接入转换时无需再改 seam。

**环境前提（每个任务都适用）：**

```bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.8
```

跑 Python 必须用 worktree 自己的解释器，裸 `python` 会解析到别的 worktree：

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py -v
```

**但跑全量必须从仓库根跑。** 在 `backend/` 下跑会让
`test_acceptance_templates.py::test_cross_section_template_runs` **假红**
（`FileNotFoundError: 'examples/datasets/cross_section.csv'`——它用的是 cwd 相对路径），
从根跑同一条是绿的。Task 8 的全量 gate 别被这条假红误导。

跑全量时还要 `--ignore` 掉 `test_cs_did_oracle.py` / `test_sa_did_oracle.py` /
`test_cs_did_clustering.py`——它们在 collection 阶段就 `FileNotFoundError`（缺 R oracle
fixture 文件），与本计划无关。

**未经用户明确授权，不 push / 不建 PR / 不 merge / 不 tag。**

---

## 每个任务都适用的一条硬规矩：断言必须验活

**每写一条新断言，都要证明它是活的**：把实现临时改成它声称要拦的那个错误，确认测试变红，再改回来。
只跑出绿色不算数。

这条不是洁癖。Task 3 的实现者加了一条自认为在防「binding 里放成 recipe artifact」的断言：

```python
assert produced["artifact_id"] in result.artifact_ids   # artifact_ids 里两个都有
```

审查时把实现改成 `recipe_artifact_id`，**全仓 158 个测试无一变红**。它断言的恰好是两者的共同点。

比没有断言更糟：它让后来读代码的人以为这里被保护着。这个仓库出过的事故正是这个形态——
一次名字不匹配静默 `continue`，标准误用了 0.7804 而不是正确的 1.5955，全套测试是绿的。

> 观察：Task 1 的实现者主动验活了，Task 3 的没有。差别在于 Task 1 的提示里明确要求了。
> 所以这是**流程缺陷不是人的问题**，故写在这里，对所有任务生效。

---

## File Structure

| 文件 | 职责 | 本计划的改动 |
|---|---|---|
| `backend/workbench/agent/workflow_contracts.py` | step 词表、spec 校验、拓扑排序 | 新增 `source` 字段的接受与校验；新增 `STEP_PRODUCES_DATASET` 声明表 |
| `backend/workbench/agent/workflow.py` | 编译、fingerprint、执行状态机 | `compile_workflow` 把 `from_step` 并入 `depends_on` |
| `backend/workbench/agent/workflow_runtime.py` | step 执行与持久化 | `_persist_numeric_derivation` 产出标准 binding；`_workflow_input_frame` 按 `source` 解析上游节点 |
| `tests/test_workflow_seam.py` | **新建**，P0 全部测试 | — |

`STEP_PRODUCES_DATASET` 与 `source` 校验放在 `workflow_contracts.py`，因为那里已经是 step 契约的唯一归属地；运行时解析放在 `workflow_runtime.py`，与 `_workflow_input_frame` 同处。不新建模块——P0 是给既有 seam 加一条路径，不是另起一层。

---

## Task 1: step spec 接受 `source` 来源承诺

**Files:**
- Modify: `backend/workbench/agent/workflow_contracts.py:2089`（`unknown` 字段集合）
- Test: `tests/test_workflow_seam.py`（新建）

- [ ] **Step 1: 写失败测试**

创建 `tests/test_workflow_seam.py`：

```python
"""P0 组合 seam：step 消费上游 step 产出的数据集。

这些不变量属于组合本身：来源承诺可解析、被引用的 step 真的产出数据集、
下游读到的是上游的输出而不是原始数据。
"""

from __future__ import annotations

import pytest

from workbench.agent.operations import OperationValidationError
from workbench.agent.workflow_contracts import validate_workflow_steps


def _numeric_step(step_id: str, output_name: str) -> dict:
    return {
        "step_id": step_id,
        "operation_id": "statistical.derive_numeric",
        "spec": {
            "recipes": [
                {
                    "output_name": output_name,
                    "operation": "multiply",
                    "source_column": "size",
                    "factor": 2.0,
                }
            ]
        },
    }


def test_step_spec_accepts_a_source_commitment() -> None:
    """A step may declare that its input is another step's output."""

    downstream = _numeric_step("second", "doubled_again")
    downstream["spec"]["source"] = {"from_step": "first", "output": "produced_dataset"}
    steps = [_numeric_step("first", "doubled"), downstream]

    ordered = validate_workflow_steps(steps)

    assert [item["step_id"] for item in ordered] == ["first", "second"]
    assert ordered[1]["spec"]["source"] == {
        "from_step": "first",
        "output": "produced_dataset",
    }
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py::test_step_spec_accepts_a_source_commitment -v
```

Expected: FAIL —— `_validate_step_spec` 会拒绝 spec 里未声明的 `source` 字段（具体报错取决于 `statistical.derive_numeric` 的 spec 契约，形如 "does not accept field(s): source"）。

- [ ] **Step 3: 让 `source` 成为所有 step spec 的通用字段**

在 `backend/workbench/agent/workflow_contracts.py` 里，`validate_workflow_steps` 中调用 `_validate_step_spec` 的位置之前，把 `source` 摘出来再校验剩余 spec。找到这一段（约 2105 行）：

```python
        _validate_step_spec(str(operation_id), spec)
        normalized.append(
            {
                "step_id": step_id,
                "operation_id": str(operation_id),
                "spec": dict(spec),
```

替换为：

```python
        # `source` is a composition-level field owned by the workflow, not by
        # any one operation's spec contract, so it is validated here and kept
        # out of the per-operation validator that would reject it as unknown.
        source_commitment = spec.get("source")
        operation_spec = {key: value for key, value in spec.items() if key != "source"}
        if source_commitment is not None:
            _validate_source_commitment(step_id, source_commitment)
        _validate_step_spec(str(operation_id), operation_spec)
        normalized_spec = dict(operation_spec)
        if source_commitment is not None:
            normalized_spec["source"] = {
                "from_step": str(source_commitment["from_step"]),
                "output": str(source_commitment["output"]),
            }
        normalized.append(
            {
                "step_id": step_id,
                "operation_id": str(operation_id),
                "spec": normalized_spec,
```

在同文件 `_topological_order` 定义之前，新增校验函数：

```python
# The only output binding a step may commit to consuming today. Keeping this a
# closed set means a typo is a compile error rather than a runtime KeyError.
SUPPORTED_STEP_OUTPUTS = frozenset({"produced_dataset"})


def _validate_source_commitment(step_id: str, source: Any) -> None:
    """Validate one step's declared upstream input reference."""

    if not isinstance(source, Mapping):
        raise OperationValidationError(f"workflow step {step_id} source must be an object")
    unknown = set(source) - {"from_step", "output"}
    if unknown:
        raise OperationValidationError(
            f"workflow step {step_id} source contains unknown field(s): "
            + ", ".join(sorted(unknown))
        )
    from_step = source.get("from_step")
    if not isinstance(from_step, str) or not from_step:
        raise OperationValidationError(
            f"workflow step {step_id} source.from_step must be a step id"
        )
    output = source.get("output")
    if output not in SUPPORTED_STEP_OUTPUTS:
        raise OperationValidationError(
            f"workflow step {step_id} source.output must be one of: "
            + ", ".join(sorted(SUPPORTED_STEP_OUTPUTS))
        )
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py::test_step_spec_accepts_a_source_commitment -v
```

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add tests/test_workflow_seam.py backend/workbench/agent/workflow_contracts.py
git commit -m "feat(workflow): let a step declare its input as another step's output

\`source\` is a composition-level field, so it is validated by the workflow
rather than by the per-operation spec contract that would reject it as an
unknown field. The output key is a closed set so a typo fails at compile time."
```

---

## Task 2: 引用必须可解析，且被引用的 step 必须真的产出数据集

**Files:**
- Modify: `backend/workbench/agent/workflow_contracts.py`（`validate_workflow_steps` 的 DAG 校验段，约 2126 行；以及 `_validate_source_commitment`）
- Test: `tests/test_workflow_seam.py`

- [ ] **Step 0: 先补 Task 1 遗留的格式校验**

Task 1 的 `_validate_source_commitment` 只要求 `from_step` 是非空字符串，于是
`{"from_step": "not an id!!"}` 能通过，而报错文本却写着 "must name another step in this plan"。
同一个函数在 `validate_workflow_steps` 里校验 `step_id` 用的是 `_STEP_ID_PATTERN.fullmatch`。
不对齐的后果是：一个**格式**错误会一路走到本任务的引用比对，被报成「unknown step」——
把格式问题说成引用问题，方向指错。

在 `_validate_source_commitment` 里把 `from_step` 的判断改为与 `step_id` 同一把尺子：

```python
    from_step = source.get("from_step")
    if not isinstance(from_step, str) or _STEP_ID_PATTERN.fullmatch(from_step) is None:
        raise OperationValidationError(
            f"workflow step {step_id} source.from_step must name another step in this plan"
        )
```

补一条测试：格式非法的 `from_step`（如 `"not an id!!"`）报的是格式错误，**不是** unknown step。

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_workflow_seam.py`：

```python
def test_source_commitment_to_an_unknown_step_is_rejected() -> None:
    """A dangling reference is a compile error, not a runtime surprise."""

    downstream = _numeric_step("second", "doubled_again")
    downstream["spec"]["source"] = {"from_step": "absent", "output": "produced_dataset"}

    with pytest.raises(OperationValidationError, match="unknown step"):
        validate_workflow_steps([_numeric_step("first", "doubled"), downstream])


def test_source_commitment_to_a_step_that_produces_no_dataset_is_rejected() -> None:
    """Only steps that persist a dataset can be consumed as an input."""

    downstream = _numeric_step("second", "doubled_again")
    downstream["spec"]["source"] = {"from_step": "explored", "output": "produced_dataset"}
    steps = [
        {
            "step_id": "explored",
            "operation_id": "statistical.explore",
            "spec": {"operation": "corr", "selected_columns": ["size", "outcome"]},
        },
        downstream,
    ]

    with pytest.raises(OperationValidationError, match="does not produce a dataset"):
        validate_workflow_steps(steps)


def test_source_commitment_implies_a_dependency() -> None:
    """Declaring an input is declaring an ordering; the DAG must reflect it."""

    downstream = _numeric_step("second", "doubled_again")
    downstream["spec"]["source"] = {"from_step": "first", "output": "produced_dataset"}

    ordered = validate_workflow_steps([downstream, _numeric_step("first", "doubled")])

    assert [item["step_id"] for item in ordered] == ["first", "second"]
    assert ordered[1]["depends_on"] == ["first"]
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py -v -k "unknown_step or produces_no_dataset or implies_a_dependency"
```

Expected: 三条全 FAIL —— 前两条不抛异常（`pytest.raises` 落空），第三条报拓扑序不对或 `depends_on` 为空。

- [ ] **Step 3: 实现引用校验与隐式依赖**

在 `backend/workbench/agent/workflow_contracts.py` 里，`SUPPORTED_STEP_OUTPUTS` 定义旁新增：

```python
# Which step kinds persist a dataset child that a later step can consume.
# `statistical.derive_numeric` is the only one today (workflow_runtime's
# `_persist_numeric_derivation`); the data transforms join it in P3.
STEP_PRODUCES_DATASET = frozenset({"statistical.derive_numeric"})
```

然后在 `validate_workflow_steps` 里，找到现有的引用完整性检查段：

```python
    known = {step["step_id"] for step in normalized}
    for step in normalized:
        missing = [dep for dep in step["depends_on"] if dep not in known]
```

在这段**之前**插入来源承诺的解析（它会写 `depends_on`，所以必须早于依赖完整性检查）：

```python
    by_id = {step["step_id"]: step for step in normalized}
    for step in normalized:
        source = step["spec"].get("source")
        if source is None:
            continue
        from_step = str(source["from_step"])
        if from_step == step["step_id"]:
            raise OperationValidationError(
                f"workflow step {step['step_id']} source refers to itself"
            )
        producer = by_id.get(from_step)
        if producer is None:
            raise OperationValidationError(
                f"workflow step {step['step_id']} source refers to unknown step: {from_step}"
            )
        if producer["operation_id"] not in STEP_PRODUCES_DATASET:
            raise OperationValidationError(
                f"workflow step {step['step_id']} source refers to {from_step}, which "
                f"does not produce a dataset: {producer['operation_id']}"
            )
        # Consuming a step's output is an ordering constraint, so the reference
        # becomes a real dependency. Reusing depends_on means the existing
        # topological sort, cycle refusal, fail-closed gating and
        # dependency_fingerprints all cover chained steps with no new machinery.
        if from_step not in step["depends_on"]:
            step["depends_on"] = [*step["depends_on"], from_step]
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py -v
```

Expected: 四条全 PASS

- [ ] **Step 5: 确认环路仍被拒（复用既有机制的回归）**

追加测试：

```python
def test_source_commitment_cannot_form_a_cycle() -> None:
    """A reference is a dependency, so the existing cycle refusal covers it."""

    first = _numeric_step("first", "doubled")
    first["spec"]["source"] = {"from_step": "second", "output": "produced_dataset"}
    second = _numeric_step("second", "doubled_again")
    second["spec"]["source"] = {"from_step": "first", "output": "produced_dataset"}

    with pytest.raises(OperationValidationError, match="cycle"):
        validate_workflow_steps([first, second])
```

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py -v
```

Expected: 五条全 PASS

- [ ] **Step 6: 提交**

```bash
git add tests/test_workflow_seam.py backend/workbench/agent/workflow_contracts.py
git commit -m "feat(workflow): resolve source commitments into real dependencies

A reference to another step's output is an ordering constraint, so it becomes
a depends_on entry. That makes the existing topological sort, cycle refusal,
fail-closed gating and dependency_fingerprints cover chained steps without any
new machinery. Referring to a step that persists no dataset is a compile error
rather than a runtime KeyError."
```

---

## Task 3: 产出数据集的 step 发布标准 binding

**Files:**
- Modify: `backend/workbench/agent/workflow_runtime.py:276-410`（`_persist_numeric_derivation`）
- Test: `tests/test_workflow_seam.py`

**背景**：`_persist_numeric_derivation` 目前的 payload 是
`{"output_columns": summaries, "child_node_id": child_node_id}`（`workflow_runtime.py:410`）——
有 node id，但没有 `run_id` / `artifact_id`，下游无法解析。本任务补齐成一个标准块。

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_workflow_seam.py`（顶部补充 import）：

```python
from pathlib import Path

import pandas as pd

from tests.test_data_column_cast import _source_project
from tests.workflow_fixtures import composed_plan  # noqa: F401  (fixture module import guard)
from workbench.agent.workflow import WorkflowExecutor, compile_workflow
from workbench.agent.workflow_runtime import build_workflow_step_executor


def _chain_frame() -> pd.DataFrame:
    return pd.DataFrame(
        {
            "size": [1.0, 2.0, 3.0, 4.0],
            "outcome": [2.0, 4.0, 6.0, 8.0],
            "wave": ["a", "a", "b", "b"],
        }
    )


def _compiled_chain(tmp_path: Path, steps: list[dict]):
    project, run_id, artifact_id = _source_project(tmp_path, _chain_frame())
    draft = compile_workflow(
        workflow_id="wf_seam",
        target={"run_id": run_id, "node_ref": "stage:source", "artifact_id": artifact_id},
        preconditions={"context_fingerprint": "sha256:fixture"},
        steps=steps,
        available_columns=list(_chain_frame().columns),
    )
    return project, draft


def test_a_dataset_producing_step_publishes_a_resolvable_binding(tmp_path: Path) -> None:
    """The output block carries everything a downstream step needs to resolve."""

    project, draft = _compiled_chain(tmp_path, [_numeric_step("first", "doubled")])
    executor = build_workflow_step_executor(project, draft)

    result = executor(draft.steps[0], {})

    produced = result.payload["produced_dataset"]
    assert set(produced) == {"run_id", "node_ref", "artifact_id", "result_fingerprint"}
    assert produced["run_id"] == draft.target["run_id"]
    assert produced["node_ref"].startswith("data-derive-numeric:")
    assert produced["result_fingerprint"] == result.result_fingerprint
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py::test_a_dataset_producing_step_publishes_a_resolvable_binding -v
```

Expected: FAIL with `KeyError: 'produced_dataset'`

- [ ] **Step 3: 发布 binding**

在 `backend/workbench/agent/workflow_runtime.py` 的 `_persist_numeric_derivation` 末尾，
把现有的返回语句（406-411 行）：

```python
    return WorkflowStepResult(
        artifact_ids=[data_artifact_id, recipe_artifact_id],
        row_counts={item["output_name"]: item["nonmissing_count"] for item in summaries},
        result_fingerprint=fingerprint,
        payload={"output_columns": summaries, "child_node_id": child_node_id},
    )
```

替换为：

```python
    return WorkflowStepResult(
        artifact_ids=[data_artifact_id, recipe_artifact_id],
        row_counts={item["output_name"]: item["nonmissing_count"] for item in summaries},
        result_fingerprint=fingerprint,
        payload={
            "output_columns": summaries,
            "child_node_id": child_node_id,
            # The standard binding a later step commits to consuming. Every
            # dataset-producing step publishes this exact shape, so resolution
            # never branches on which operation produced the input. The
            # fingerprint is the step's own, which is what makes the
            # execution-time check in _resolve_committed_source_frame a real
            # check rather than a tautology.
            "produced_dataset": {
                "run_id": source_run_id,
                "node_ref": child_node_id,
                "artifact_id": data_artifact_id,
                "result_fingerprint": fingerprint,
            },
        },
    )
```

`fingerprint`、`source_run_id`、`child_node_id`、`data_artifact_id` 都是该函数内已有的局部变量
（分别定义在 289、287、约 360、约 319 行），无需新增导入或辅助函数。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py -v
```

Expected: 六条全 PASS

- [ ] **Step 5: 确认既有 workflow 测试未被打破**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_runtime.py ../tests/test_workflow_resume.py ../tests/test_agent_generic_workflow.py -v
```

Expected: 全 PASS。若有失败，是既有断言在比对 payload 的完整内容——**改断言不改 payload**，
并在 commit message 里说明。

- [ ] **Step 6: 提交**

```bash
git add tests/test_workflow_seam.py backend/workbench/agent/workflow_runtime.py
git commit -m "feat(workflow): publish a standard binding from dataset-producing steps

The payload already carried child_node_id but neither run_id nor artifact_id,
so a downstream step could not resolve the input it was pointed at. Every
dataset-producing step now publishes one identically shaped block, which is
what lets resolution stay free of per-operation branching."
```

---

## Task 4: 执行期解析上游输出，而不是读 `draft.target`

**Files:**
- Modify: `backend/workbench/agent/workflow_runtime.py:243-254`（`_workflow_input_frame`）与 `:578-585`（`execute` 内的调用点）
- Modify: `backend/workbench/agent/workflow_contracts.py`（下面 Step 0 的编译期拒绝）
- Test: `tests/test_workflow_seam.py`

### ⚠️ Task 2 已经改变了执行期行为——动手前必读

Task 2 的审查发现：`from_step` 一旦并入 `depends_on`，**既有的重放路径就已经生效了**。链路是
`workflow_runtime.py:576` 由 `depends_on` 建 `dependency_graph` → `_upstream_numeric_steps`
（`:219-239`）沿图收集所有 `derive_numeric` 祖先 → `_workflow_input_frame`（`:242-252`）
逐个重放 `_apply_numeric_recipes`。

所以从 Task 2 起，一个声明了 `source: {from_step: X}` 的 step，其输入帧里**已经含有 X 的派生列**
——但机制是「从 `draft.target` 重放 X 的配方」，不是「读 X 落盘的数据集」。方向凑巧对了，
机制不是我们要的那个，而且它只对 `derive_numeric` 有效，对 P3 的数据转换完全无效。

**对本任务的两个后果：**

1. **不能只加不减。** 下面的实现用了提前 return，声明 `source` 的 step 不再走重放——这是对的。
   若写成「先重放再解析」，同一批派生列会被算两遍。好在那是 fail-loud 的
   （`_apply_numeric_recipes:172` 会抛 `numeric derivation output already exists`），不会静默出错。
2. **提前 return 带来一个新的静默风险**，见 Step 0。

- [ ] **Step 0: 先堵住「source 与其他派生祖先并存」的静默丢弃**

提前 return 意味着：如果一个 step **既**声明了 `source: {from_step: X}`，**又**通过 `depends_on`
挂着另一个不在 X 祖先链上的 `derive_numeric` step Y，那么 **Y 的派生列会被静默丢掉**——
计划作者以为 Y 的产出可用，实际不在输入帧里，而且不报错。

这正是本仓库最忌讳的形态（静默丢弃）。**不要靠「谁赢」的约定解决，要在编译期拒绝。**

在 `workflow_contracts.py` 的来源承诺解析段里补一条：若某个 step 声明了 `source`，
则它的祖先集合中除了 `from_step` 自身的祖先链之外，不得再有 `STEP_PRODUCES_DATASET` 里的
step。违反即报错，消息要说清「一个 step 只能有一个数据来源」。

补测试：`source` 指向 X、同时 `depends_on` 挂一个无关的 derive_numeric Y → 编译期拒绝。

> 若实现中发现这个约束过严（例如某种合法形状被误伤），**回来改这条规格，不要放宽成静默丢弃**。

### ⚠️ 本任务的校验必须比对内容，不能比对 step fingerprint

初稿写的是拿 `produced["result_fingerprint"]` 与 `upstream.result_fingerprint` 比对，
并称之为「传递性保证的执行期一半」。**那是假的**，Task 3 的审查实证：

```
SAME OBJECT? : True     ← produced["result_fingerprint"] is result.result_fingerprint
```

两者是同一个局部变量赋值的同一个对象，resume 路径下也由同一条 state 记录还原。
这条校验在任何输入下都不会触发，**包括 binding 指向错误 artifact 的情况**。

Task 3 因此在 binding 里加了 `content_sha256`（复用已算好的 `sha256_file(data_path)`）。
本任务改为把它与 `resolve_statistical_source` 返回的 `context["source_sha256"]` 比对——
那条能真正咬住：binding 指错 artifact、run 目录在两次 resume 之间被改写、
落盘路径被占用导致索引与文件脱节。`result_fingerprint` 降级为纯溯源字段，不作校验。

这是本会话第三次「看起来在守卫、实际什么都不守」，而且这次在**设计**里而不是实现里。
计划顶部那条「断言必须验活」的规矩同样适用于我写的设计。

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_a_chained_step_reads_the_upstream_output_not_the_original(tmp_path: Path) -> None:
    """The whole point of P0: step two sees step one's result.

    `first` doubles size into `doubled`. `second` doubles `doubled` again. If
    `second` were reading the original source, `doubled` would not exist and
    the step would fail; if it silently fell back, the values would be wrong.
    """

    second = {
        "step_id": "second",
        "operation_id": "statistical.derive_numeric",
        "spec": {
            "source": {"from_step": "first", "output": "produced_dataset"},
            "recipes": [
                {
                    "output_name": "quadrupled",
                    "operation": "multiply",
                    "source_column": "doubled",
                    "factor": 2.0,
                }
            ],
        },
    }
    project, draft = _compiled_chain(tmp_path, [_numeric_step("first", "doubled"), second])

    state = WorkflowExecutor(project).execute(
        draft, build_workflow_step_executor(project, draft)
    )

    assert state.steps["second"].status == "completed", state.steps["second"].error
    produced = state.steps["second"]
    assert produced.row_counts, "the chained step produced no rows"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py::test_a_chained_step_reads_the_upstream_output_not_the_original -v
```

Expected: FAIL —— `second` 状态为 `failed`，错误信息指向 `doubled` 列不存在
（因为 `_workflow_input_frame` 只重放数值配方、且下游读的是原始 frame）。

> 若这里意外 PASS，**停下来查清楚**：可能是 `_upstream_numeric_steps` 的重放恰好
> 造出了 `doubled` 列。那说明这个测试没有真正测到 seam，需换成一个重放机制
> 覆盖不到的形状（例如让 `first` 改变行数）后再继续。这正是交接书 §7
> 「守卫要点名真正的消费者」那条。

- [ ] **Step 3: 实现解析**

在 `backend/workbench/agent/workflow_runtime.py` 中，把 `_workflow_input_frame`（243 行）替换为：

```python
def _workflow_input_frame(
    source_frame: pd.DataFrame,
    draft: WorkflowDraft,
    step: Any,
    dependency_graph: Mapping[str, tuple[str, ...]],
    previous: Mapping[str, WorkflowStepResult],
    root: Path,
) -> pd.DataFrame:
    """Rebuild the exact input for a step from its declared DAG.

    A step that committed to another step's output resolves that persisted
    dataset. Everything else keeps the original behaviour: start from the
    workflow's own target and replay the declared numeric derivations.
    """

    commitment = step.spec.get("source")
    if commitment is not None:
        return _resolve_committed_source_frame(step, previous, root)
    frame = source_frame
    for transform_step in _upstream_numeric_steps(draft, str(step.step_id), dependency_graph):
        frame, _ = _apply_numeric_recipes(frame, transform_step.spec["recipes"])
    return frame


def _resolve_committed_source_frame(
    step: Any,
    previous: Mapping[str, WorkflowStepResult],
    root: Path,
) -> pd.DataFrame:
    """Load the dataset the upstream step actually persisted."""

    commitment = step.spec["source"]
    from_step = str(commitment["from_step"])
    upstream = previous.get(from_step)
    if upstream is None:
        raise WorkflowExecutionError(
            f"workflow step {step.step_id} source {from_step} has not completed"
        )
    produced = upstream.payload.get(str(commitment["output"]))
    if not isinstance(produced, Mapping):
        raise WorkflowExecutionError(
            f"workflow step {step.step_id} source {from_step} published no "
            f"{commitment['output']!r} binding"
        )
    context, frame = resolve_statistical_source(
        root,
        source_run_id=str(produced["run_id"]),
        source_node_id=str(produced["node_ref"]),
        source_artifact_id=str(produced["artifact_id"]),
    )
    # Execution-time half of the guarantee: compare the bytes that actually
    # arrived against the bytes the upstream step published. Comparing step
    # fingerprints here would look like a check and never fire -- the binding
    # carries the producing step's own fingerprint, so both sides are the same
    # value no matter which artifact the binding points at.
    if str(produced["content_sha256"]) != str(context["source_sha256"]):
        raise WorkflowExecutionError(
            f"workflow step {step.step_id} source {from_step} resolved to different "
            "content than the completed step published"
        )
    return frame
```

然后更新调用点（约 583 行）：

```python
        step_frame = _workflow_input_frame(
            source_frame, draft, step, dependency_graph, previous, root
        )
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py -v
```

Expected: 七条全 PASS

- [ ] **Step 5: 提交**

```bash
git add tests/test_workflow_seam.py backend/workbench/agent/workflow_runtime.py
git commit -m "feat(workflow): resolve a chained step's input from the upstream output

Until now every step resolved its data from draft.target, so a plan's steps all
read the same original artifact and a transform's result was discarded by
whatever followed it. A step that committed to an upstream output now loads
that persisted dataset instead, and refuses to run if the binding it receives
is not the one the completed step published."
```

---

## Task 4.5: 链式 step 的血缘必须指向真实上游【Task 4 审查中发现，新增】

**Files:**
- Modify: `backend/workbench/agent/workflow_runtime.py`（`_persist_numeric_derivation`）
- Test: `tests/test_workflow_seam.py`

**问题**：`_persist_numeric_derivation` 无条件把 `draft.target` 写进三处：

- `recipe.json` 的 `source` 块（artifact_id 与原始 target 的 sha256）
- `_ensure_runtime_artifact(inputs=[draft.target["artifact_id"]])`
- 图节点的 `parent_stage_id` 与 edge 的 source（`draft.target["node_ref"]`）

于是一个声明了 `source` 的 step，**计算上**正确地吃了上游产出，**声称上**父节点却是原始数据。
谱系图会画成 `stage:source → second`，而不是 `first → second`。

**为什么必须修**：这个仓库的北极星是「一张 lineage graph 完成编辑/rerun/对比/AI/报告」。
一条错误的血缘边**不报错、不变红、测试全绿**，但会让图说谎——正是本版要消灭的缺陷形态。
而且它会误导下游的 rerun 与对比：以为改原始数据会影响这个节点，实际不会。

**做法**：声明了 `source` 的 step，其血缘写入都改为指向 binding 里的
`(run_id, node_ref, artifact_id)` 与 `content_sha256`，而不是 `draft.target`。
未声明 `source` 的 step 行为**逐字不变**。

**四处，不是三处**（Task 4 审查补充定位）：

- `recipe["source"]`（`workflow_runtime.py:356-359`）—— **落盘的 recipe.json 里也有一份
  错误的来源声明**，不只是图上的边
- `_ensure_runtime_artifact(inputs=[draft.target["artifact_id"]])`，`:375` 与 `:384` 两处
- `parent_stage_id=source_node_id`，`:394` 与 `:966`

**验收（必须验活）**：跑一条 `first → second` 链，断言图里 `second` 的父节点是 `first`
产出的那个 data 节点，而**不是** `stage:source`。把实现改回 `draft.target`，这条必须变红。

---

## Task 5: 断点续跑时 binding 必须能重建

> **实测确认（Task 4 审查）**：一个两步链，第二步瞬时失败后 resume，得到
> `second: failed — workflow step second source first published no 'produced_dataset' binding`。
> **链式 plan 一旦中断就永远无法 resume。** 是 fail-loud 不是静默错数，但
> **这条报错文案会把人送错地方**——它说「上游没发布 binding」，真相是
> 「binding 没跨 resume 持久化」。本任务要把两种情况分开措辞。

**Files:**
- Modify: `backend/workbench/agent/workflow.py:92-123`（`WorkflowStepState`）与 `:222-231`（resume 时重建 `completed_results`）
- Test: `tests/test_workflow_seam.py`

**为什么需要这个任务**：`WorkflowExecutor.execute` 在 resume 时用持久化的
`WorkflowStepState` 重建 `completed_results`，而 `WorkflowStepState` **不含 payload**。
于是已完成的上游 step 在续跑时 payload 为空，Task 4 的解析会报
"published no produced_dataset binding"。resume 是既有功能（`tests/test_workflow_resume.py`），
不能因为组合而退化。

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_workflow_seam.py`：

```python
def test_a_resumed_chain_can_still_resolve_the_upstream_binding(tmp_path: Path) -> None:
    """Resume rebuilds completed results from state, so the binding must persist."""

    second = {
        "step_id": "second",
        "operation_id": "statistical.derive_numeric",
        "spec": {
            "source": {"from_step": "first", "output": "produced_dataset"},
            "recipes": [
                {
                    "output_name": "quadrupled",
                    "operation": "multiply",
                    "source_column": "doubled",
                    "factor": 2.0,
                }
            ],
        },
    }
    project, draft = _compiled_chain(tmp_path, [_numeric_step("first", "doubled"), second])
    real_executor = build_workflow_step_executor(project, draft)

    def stop_after_first(step, previous):
        if step.step_id == "second":
            raise RuntimeError("interrupted before the chained step")
        return real_executor(step, previous)

    first_pass = WorkflowExecutor(project).execute(draft, stop_after_first)
    assert first_pass.steps["first"].status == "completed"
    assert first_pass.steps["second"].status == "failed"

    resumed = WorkflowExecutor(project).execute(draft, real_executor)

    assert resumed.steps["second"].status == "completed", resumed.steps["second"].error
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py::test_a_resumed_chain_can_still_resolve_the_upstream_binding -v
```

Expected: FAIL —— `second` 状态为 `failed`，错误为 "published no 'produced_dataset' binding"。

- [ ] **Step 3: 让 binding 随状态持久化**

在 `backend/workbench/agent/workflow.py` 中，把 `WorkflowStepState`（92-123 行）改为携带该字段。

字段声明处，在 `result_fingerprint: str | None = None` 之后加一行：

```python
    produced_dataset: dict[str, Any] | None = None
```

`to_dict` 的返回字典中，在 `"result_fingerprint": self.result_fingerprint,` 之后加一行：

```python
            "produced_dataset": self.produced_dataset,
```

`from_dict` 的构造参数中，在 `result_fingerprint=value.get("result_fingerprint"),` 之后加一行：

```python
            produced_dataset=(
                dict(value["produced_dataset"])
                if isinstance(value.get("produced_dataset"), Mapping)
                else None
            ),
```

在 `execute` 里，step 完成时的 `WorkflowStepState` 构造（约 265-272 行，
形如 `completed = WorkflowStepState(... result_fingerprint=(result.result_fingerprint or step.fingerprint))`）
末尾补一个参数：

```python
                    produced_dataset=(
                        dict(result.payload["produced_dataset"])
                        if isinstance(result.payload.get("produced_dataset"), Mapping)
                        else None
                    ),
```

最后，resume 时重建 `completed_results` 的字典推导（222-231 行）改为把它放回 payload：

```python
        completed_results: dict[str, WorkflowStepResult] = {
            step_id: WorkflowStepResult(
                artifact_ids=list(step_state.artifact_ids),
                row_counts=dict(step_state.row_counts),
                result_fingerprint=(
                    step_state.result_fingerprint or step_state.fingerprint
                ),
                # Rebuilt so a chained step can still resolve its input after a
                # resume. Only this one binding is restored: it is the only part
                # of a payload another step is allowed to depend on.
                payload=(
                    {"produced_dataset": dict(step_state.produced_dataset)}
                    if step_state.produced_dataset
                    else {}
                ),
            )
            for step_id, step_state in states.items()
            if step_state.status == "completed"
        }
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py ../tests/test_workflow_resume.py -v
```

Expected: 全 PASS

- [ ] **Step 5: 提交**

```bash
git add tests/test_workflow_seam.py backend/workbench/agent/workflow.py
git commit -m "fix(workflow): keep the produced binding across a resume

Resume rebuilds completed results from persisted step state, which carried no
payload, so a chained step resolving its upstream input would find nothing and
fail on the second pass. Only produced_dataset is persisted and restored --
it is the single part of a payload another step is permitted to depend on, so
widening it to the whole payload would invite exactly the coupling this seam
is meant to keep narrow."
```

---

## Task 6: 链首被改动使整条链失效

### 范围补充（Task 4 审查发现）：指纹要跟真正被消费的那份数据挂钩

`exploration_fingerprint(str(source_context["source_sha256"]), spec)`
（`workflow_runtime.py:673 / 781 / 803`）用的是**原始 target** 的 sha，
而此时 frame 已经是上游的了。

今天无害——`derive_numeric` 只加列，同 spec 的 explore 结果必然相同。
但 P3 有了 `subset` 之后，「在原表上 corr」与「在子集上 corr」会算出**同一个
fingerprint、同一个 artifact 路径**。`_ensure_exploration_artifact` 会以
`artifact binding is not deterministic` 拦下——fail-loud 不出错数，
但**合法计划会被莫名其妙地拒绝**。

所以本任务的「指纹传递性」不止是 step→step，还包括「指纹必须跟真正被消费的
那份数据挂钩」。

**Files:**
- Test only: `tests/test_workflow_seam.py`

这条守卫断言的是设计文档决策零的核心保证。计划期传递性由
`_make_step` 的 `dependency_fingerprints`（`workflow.py:373`）提供，本任务证明它对链式 step 确实成立。

- [ ] **Step 1: 写测试**

```python
def test_changing_the_chain_head_invalidates_every_downstream_step(tmp_path: Path) -> None:
    """Fingerprints hold transitively, so the chain cannot be partly stale."""

    second = {
        "step_id": "second",
        "operation_id": "statistical.derive_numeric",
        "spec": {
            "source": {"from_step": "first", "output": "produced_dataset"},
            "recipes": [
                {
                    "output_name": "quadrupled",
                    "operation": "multiply",
                    "source_column": "doubled",
                    "factor": 2.0,
                }
            ],
        },
    }
    _project, original = _compiled_chain(
        tmp_path / "a", [_numeric_step("first", "doubled"), second]
    )

    changed_head = _numeric_step("first", "doubled")
    changed_head["spec"]["recipes"][0]["factor"] = 3.0
    _project_b, altered = _compiled_chain(tmp_path / "b", [changed_head, second])

    by_id_original = {step.step_id: step for step in original.steps}
    by_id_altered = {step.step_id: step for step in altered.steps}

    assert by_id_original["first"].fingerprint != by_id_altered["first"].fingerprint
    # The downstream spec is byte-identical; only its dependency changed.
    assert by_id_original["second"].spec == by_id_altered["second"].spec
    assert by_id_original["second"].fingerprint != by_id_altered["second"].fingerprint
    assert original.plan_fingerprint != altered.plan_fingerprint
```

- [ ] **Step 2: 跑测试**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py::test_changing_the_chain_head_invalidates_every_downstream_step -v
```

Expected: PASS（无需改实现——这是对既有机制的守卫）。

> 若 FAIL，说明 `dependency_fingerprints` 没有覆盖 `source` 引入的依赖。
> 回到 Task 2 检查 `from_step` 是否真的进了 `depends_on`——那是让传递性成立的关键。

- [ ] **Step 3: 补一条失败阻断的守卫**

```python
def test_a_failed_upstream_blocks_the_chained_step(tmp_path: Path) -> None:
    """A chained step is blocked, never silently run on the original data."""

    broken = _numeric_step("first", "doubled")
    broken["spec"]["recipes"][0]["source_column"] = "size"
    second = {
        "step_id": "second",
        "operation_id": "statistical.derive_numeric",
        "spec": {
            "source": {"from_step": "first", "output": "produced_dataset"},
            "recipes": [
                {
                    "output_name": "quadrupled",
                    "operation": "multiply",
                    "source_column": "doubled",
                    "factor": 2.0,
                }
            ],
        },
    }
    project, draft = _compiled_chain(tmp_path, [broken, second])

    def failing_executor(step, previous):
        if step.step_id == "first":
            raise RuntimeError("upstream failed on purpose")
        return build_workflow_step_executor(project, draft)(step, previous)

    state = WorkflowExecutor(project).execute(draft, failing_executor)

    assert state.steps["first"].status == "failed"
    assert state.steps["second"].status == "blocked"
```

- [ ] **Step 4: 跑全部 seam 测试**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py -v
```

Expected: 十一条全 PASS

- [ ] **Step 5: 提交**

```bash
git add tests/test_workflow_seam.py
git commit -m "test(workflow): guard transitive fingerprints and fail-closed chaining

Changing the chain head has to invalidate every step below it even when their
specs are byte-identical, which is what makes composition safe to cache and
resume. A failed upstream must block its consumer rather than let it fall back
to the original data -- a silent fallback would produce a plausible result from
the wrong input, the failure mode this version exists to remove."
```

---

## Task 7: 把 `source` 公布给 planning agent

**Files:**
- Modify: `backend/workbench/agent/workflow_contracts.py:1745-1763`（`workflow_step_vocabulary`）
- Test: `tests/test_workflow_seam.py`

**为什么需要这个任务（计划初稿漏了它）**：`workflow_step_vocabulary()` 是「让 agent 按真实
字段名编排而不是猜」的**唯一出口**。它公布 `step_operations` / `step_id_pattern` / `ordering`，
但初稿七个任务里**没有任何一个**提到它。按初稿全部做完，后端具备了组合能力，而
**planning agent 根本不知道 `source` 存在**——「先转换再建模」在自然语言路径上依然做不出来。

这正是本版存在的理由那个毛病的翻版：**能力做出来了，Agent 够不着**。P0 自己不能犯同一个错。

**为什么排在 Task 4 之后**：Task 4 落地前，`source` 是一个「校验通过但执行期被完全忽略」的
字段——一份声明了 `source` 的计划会**静默读原始数据**。目前风险受控（全仓库没有任何地方
产生 `source`，vocabulary 也没公布它），但这条约束必须守住：

> **在 Task 4 与 Task 5 完成之前，不得把 `source` 写进任何 agent 可见的 schema 或 prompt。**

- [ ] **Step 1: 写失败测试**

```python
def test_the_step_vocabulary_publishes_the_source_commitment() -> None:
    """A field the planning agent cannot see is a field it will never use."""

    from workbench.agent.workflow_contracts import (
        SUPPORTED_STEP_OUTPUTS,
        workflow_step_vocabulary,
    )

    vocabulary = workflow_step_vocabulary()

    source = vocabulary["step_source"]
    assert set(source["outputs"]) == set(SUPPORTED_STEP_OUTPUTS)
    assert sorted(source["produced_by"]) == sorted(STEP_PRODUCES_DATASET)
    assert "from_step" in source["shape"]
    assert "output" in source["shape"]
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py::test_the_step_vocabulary_publishes_the_source_commitment -v
```

Expected: FAIL with `KeyError: 'step_source'`

- [ ] **Step 3: 公布**

在 `workflow_step_vocabulary()` 返回的字典里新增一项（与既有的 `ordering` 同级）：

```python
        "step_source": {
            "shape": {"from_step": "step_id", "output": "output key"},
            "outputs": sorted(SUPPORTED_STEP_OUTPUTS),
            "produced_by": sorted(STEP_PRODUCES_DATASET),
            "meaning": (
                "By default every step reads the workflow's own target dataset. "
                "A step that declares `source` reads the dataset produced by the "
                "named step instead, which is how a transform and the model that "
                "consumes it belong in one plan. Only the operations listed in "
                "`produced_by` can be named; declaring a source also declares a "
                "dependency, so it is ordered and fail-closed like `depends_on`."
            ),
        },
```

- [ ] **Step 4: 跑测试确认通过，并跑既有 vocabulary 消费方的测试**

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests/test_workflow_seam.py ../tests/test_notebook_planning_agent.py ../tests/test_agent_capabilities.py -v
```

Expected: 全 PASS

- [ ] **Step 5: 提交**

```bash
git add tests/test_workflow_seam.py backend/workbench/agent/workflow_contracts.py
git commit -m "feat(workflow): publish the source commitment to the planning agent

The step vocabulary is the only place a planning agent learns real field names
instead of guessing, so a seam absent from it is a seam that never gets used.
Publishing it last is deliberate: until the runtime resolves a commitment, a
plan declaring one would have silently read the original data instead."
```

---

## Task 8: 全量回归与收口

**Files:** 无改动，仅验证。

- [ ] **Step 0: 端到端确认输入帧是对的**

Task 2 的审查指出：本计划的测试大多止步于 `validate_workflow_steps` 与单个 step 的执行，
而「声明 source 的 step 拿到的输入帧究竟对不对」需要一条真正跑完整条链的用例来断言。

补一条端到端测试：`derive_numeric`（产出派生列）→ 声明 `source` 的第二个 step →
断言第二个 step 的输出**数值正确**，而不只是状态为 completed。

**断言数值，不要只断言状态。** 「跑完了」和「算对了」是两回事——本仓库出过一次
run 成功、报告正常、而标准误用了 0.7804 而非正确的 1.5955 的事故。

- [ ] **Step 1: 跑 backend 全量**

先确认没有 vite 预览在跑（gate 会拒绝启动），然后：

```bash
cd backend && PYTHONPATH=$PWD ../.venv/bin/python -m pytest ../tests -x -q
```

Expected: 全 PASS，且总数 ≥ 4750 + 本计划新增的测试。

- [ ] **Step 2: 跑 full gate**

**必须在宿主终端跑，不要在 agent 的 Seatbelt 沙箱里跑**（macOS 拒绝嵌套 `sandbox_apply`，会假报约 20 个 containment 失败）：

```bash
LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 bash scripts/gate.sh --full
```

Expected: PASSED，BE ≥ 4765 / FE ≥ 1648 / tsc 0

- [ ] **Step 3: 记录 P0 完成状态**

在 `docs/superpowers/specs/2026-08-07-v1.8.8-data-management-in-natural-language-design.md`
的 §6 分期表里，把 P0 一行标为已完成，并注明验证等级（单测 + 链式执行，尚无真机）。

- [ ] **Step 4: 提交**

```bash
git add docs/superpowers/specs/2026-08-07-v1.8.8-data-management-in-natural-language-design.md
git commit -m "docs(v1.8.8): record P0 as landed with its verification level"
```

- [ ] **Step 5: 停下来等三级审查**

按仓库约定，P0 是架构改动，进入 Implementer → Test & QA → Reviewer 三级审查再进 P1。
子 agent 用 **opus**（haiku/sonnet 在这台机器上会解析到 deepseek）。

**未经用户明确授权，不 push / 不建 PR / 不 merge / 不 tag。**

---

## 验收标准（P0）

- [ ] 一个 step 能声明 `source: {from_step, output}` 并通过编译
- [ ] 悬空引用、引用不产出数据集的 step、自引用、成环 —— 四种全部是**编译错误**
- [ ] `from_step` 自动成为 `depends_on`，复用既有拓扑排序与失败阻断
- [ ] 产出数据集的 step 发布形状统一的 `produced_dataset` binding
- [ ] **链式 step 读到的是上游的输出，不是原始数据**（P0 的存在理由）
- [ ] 上游 binding 的内容摘要与实际读到的字节不符时**拒绝执行**——验收方式是
      **把 binding 的 `content_sha256` 改错一位，Task 4 的执行期校验必须变红**。
      若不变红，落地的就是一条恒真校验，而字段名与注释都在宣称此处受保护，
      比没有校验更危险
- [ ] 改动链首使下游 step 指纹改变，即便其 spec 逐字节相同
- [ ] 上游失败使下游 **blocked**，绝不回退到原始数据
- [ ] `workflow_step_vocabulary()` 公布 `source` 的形状与可用取值——**否则 planning agent
      永远不会用它，P0 就只是一个够不着的后端能力**
- [ ] 链式 step 的**血缘**指向真实上游节点，而不是 workflow 的原始 target
- [ ] full gate PASSED，既有测试无一被打破

## 一处已评估并保留的形状

`source` 住在 `spec` 内部，而它是编排层字段（注释自己也这么说），于是每次都要
「剥离 → 校验 → 塞回」。放在 step entry 顶层、与 `depends_on` 平级会更自洽。

**仍然保留在 `spec` 里**，理由：`spec` 是被指纹化与持久化的那一份，`source` 属于
「这个 step 是什么」的一部分；污染 `_spec_columns` 的担心已核实不成立（各 column
extractor 都按固定键读取，不遍历 spec）；而改动形状要波及 Task 2–5 全部。
记录在此，供后续版本在扩展 seam 时重新评估。

## 不在 P0 范围

- 数据转换操作的注册（P3）
- `CapabilityContract` 与统一能力清单（P1）
- 能力级可达性守卫（P2）
- 让 `model.genesis` 消费链式输入 —— 它需要 upload 物化路径（`workflow_runtime.py:836`
  已有先例），属 P3 与转换一起做。**P0 只保证 seam 成立，不改 genesis。**
