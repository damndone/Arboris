# v1.7.3 Local Closeout Simplification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove unused C2 product code and close v1.7.3 as a tested local macOS Workbench release.

**Architecture:** Keep the existing explicit `local_contained` runtime path. Remove only standalone C2 prototype modules with no product imports, move their purpose to the roadmap, then validate the assembled LMM/Agent/UI product.

**Tech Stack:** Python, pytest, FastAPI, React/Vitest/TypeScript, macOS Seatbelt, in-app browser.

---

### Task 1: Remove unused C2 runtime prototype

**Files:**
- Delete: `backend/workbench/frozen_containment_c2.py`
- Delete: `backend/workbench/frozen_containment_adapters.py`
- Delete: `backend/workbench/frozen_containment_canary.py`
- Delete: `backend/workbench/frozen_containment_execution.py`
- Delete: `backend/workbench/frozen_containment_ingestion.py`
- Delete: matching `tests/test_frozen_containment_c2_*.py`, adapter, canary, execution and ingestion tests

- [ ] Verify no non-C2 runtime import references these modules with `rg`.
- [ ] Delete the modules and tests.
- [ ] Run import/reference scan and `git diff --check`.
- [ ] Run the existing local execution-profile, sandbox and LMM suites.

### Task 2: Replace C2 release gates with local evidence

**Files:**
- Modify: `docs/superpowers/release-trains/v1.7.3/release-ledger.json`
- Modify: `docs/superpowers/roadmap/2026-07-15-graph-scale-artifact-ai-terminal-directions.md`
- Modify: `docs/superpowers/followups/BACKLOG.md`

- [ ] Mark C2 as removed from v1.7.3 local-release scope and retain it as a future deployment/plugin-security project.
- [ ] Record existing Report/Operations completion evidence.
- [ ] Define a bounded known-truth LMM timing smoke instead of the unused strict collector.

### Task 3: Validate and close the local candidate

**Files:**
- Create: `docs/superpowers/release-trains/v1.7.3/evidence/local-release-acceptance.md`
- Modify: `docs/superpowers/release-trains/v1.7.3/release-ledger.json`

- [ ] Run focused local sandbox/LMM/Agent/report/export tests.
- [ ] Run the known-truth LMM smoke and record result correctness and duration.
- [ ] Run the complete browser LMM/Agent/export journey.
- [ ] Run `bash scripts/gate.sh --full` and `git diff --check`.
- [ ] Commit the local release candidate; do not push, PR, merge or tag without separate release-owner action.
