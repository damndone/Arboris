# Workbench v1.7.3 Report and Operations Closeout Plan

> **For agentic workers:** This plan is executed inline in the isolated `workbench-v1.7.3` worktree. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move the validated report, figure-context, run-operations, and CS-DiD lineage repairs onto a clean v1.7.3 worktree, add the missing timeout regression, and prove the R1 report path with one real DeepSeek end-to-end run.

**Architecture:** Workbench remains the deterministic owner of facts, figure artifacts, report markers, lifecycle state, and export bytes. DeepSeek writes only bounded Markdown prose and exact figure/citation markers. Long runs remain cooperative and release the single worker slot through the existing event manager.

**Tech Stack:** FastAPI/Python, pytest, React/Vitest/TypeScript, existing local LLM provider configuration, no new public dependencies.

---

### Task 1: Establish the clean v1.7.3 baseline

**Files:**
- Worktree: `/Users/jiayuanren/项目规划/.worktrees/workbench-v1.7.3`
- Plan: `docs/superpowers/plans/2026-07-18-v1.7.3-report-ops-closeout.md`

- [x] Verify branch, commit, clean status, v1.7.2 preservation, and protected tests.
- [x] Run the baseline targeted backend checks before applying v1.7.3 changes (`61 passed`).

### Task 2: Bring the validated A/B/C implementation into v1.7.3

**Files:**
- Modify: `backend/workbench/figure_context.py`
- Modify: `backend/workbench/http/llm_routes.py`
- Modify: `backend/workbench/http/runs_routes.py`
- Create: `backend/workbench/report_contract.py`
- Create: `backend/workbench/report_export.py`
- Modify: `frontend/src/report/ReportView.tsx`
- Modify: `frontend/src/report/factTable.ts`
- Modify: `frontend/src/report/reportClient.ts`
- Modify: `frontend/src/workbench/views/TableView.tsx`
- Test: `tests/test_figure_context.py`, `tests/test_report_contract.py`, `tests/test_report_export.py`, `frontend/src/report/ReportView.test.tsx`, `frontend/src/workbench/views/TableView.test.tsx`

- [x] Apply only the already-reviewed deterministic figure source, report contract, report export, and UI changes from the dirty v1.7.2 worktree.
- [x] Run the R3/R4/D1/R1/R2 focused tests and verify HTML, DOCX, LaTeX ZIP, and print-ready HTML boundaries.

### Task 3: Add the missing timeout regression first

**Files:**
- Modify: `tests/test_run_cancellation.py`
- Modify only if the RED test proves a defect: `backend/workbench/services/run_service.py`, `backend/workbench/config.py`

- [x] Add a failing test with a zero/expired deadline that asserts `interrupted`, `WORKFLOW_TIMEOUT`, and event-slot release.
- [x] Run the new test and observe the expected failure before changing production code.
- [x] No production fix was required: the existing cooperative deadline path already terminalises and releases the slot.
- [x] Re-run the timeout test and cancellation/listing tests.

### Task 4: Carry the validated D2/D3/D4 behavior

**Files:**
- Modify: `backend/workbench/events.py`
- Modify: `backend/workbench/engine/context.py`
- Modify: `backend/workbench/engine/honest_did.py`
- Modify: `backend/workbench/engine/honest_did_adapter.py`
- Modify: `backend/workbench/econometrics/runner.py`
- Modify: `backend/workbench/orchestrator/_column_checks.py`
- Modify: `backend/workbench/engine/stages/recording.py`
- Test: `tests/test_honest_did_adapter.py`, `tests/test_dropped_variable_contract.py`, `tests/test_runs_listing.py`, `tests/test_run_cancellation.py`

- [x] Verify cooperative heartbeat/cancel/timeout behavior, bounded process parallelism with serial equivalence, and CS/SA variable-declaration semantics.
- [x] Run the focused D2/D3/D4 suite. A clean `cs_did_staggered` run under `PYTHONPATH=backend` has one consistent `policy_intensity` declaration and no dropped-variable node; the real default honest-DiD benchmark completed successfully in `604.89s`.

### Task 5: Real DeepSeek R1 acceptance

**Files:**
- Evidence only: temporary run directory under `/private/tmp`
- Do not persist API keys or provider responses containing secrets.

- [x] Start the local Workbench from v1.7.3 with the existing configured provider, without changing shared dependencies.
- [x] Use a real `cs_did_staggered` example run and real DeepSeek report generation.
- [x] Verify the rendered report expands all 8 figure markers into figures and normalizes provider citation shorthand into verified cite chips.
- [x] Verify ReportView renders all 8 figures; the export route returned HTTP 200, and byte-level HTML/DOCX/LaTeX/print checks expand all 8 figures.
- [x] Stop local processes after evidence is recorded; temporary evidence remains under `/private/tmp` and contains no API key.

### Task 6: Release-readiness verification

**Files:**
- Verify: protected tests, v1.7.2 worktree, v1.7.3 worktree status.
- Update: this plan checkboxes and a concise closeout note if needed.

- [x] Run focused backend tests, frontend Vitest, TypeScript, and `git diff --check`.
- [x] Confirm no real API key is exposed, no protected test changed, v1.7.2 remains untouched, and no push/PR/merge/tag occurred.
- [x] Report implementation status separately from real-DeepSeek evidence and remaining non-blocking limits.

### Closeout evidence

- Focused backend suite: `91 passed, 3 warnings`; frontend: `132 files / 1168 tests passed`; TypeScript: exit 0.
- Full backend suite in a host where macOS seatbelt is permitted: `2126 passed, 8 skipped, 43 warnings`.
- Real honest-DiD benchmark: `cs_did_staggered --honest-did` completed with both `rm` and `sd` tracks `ok` in `604.89s` (about 10m05s). This is a recorded baseline, not a performance optimization; D3 remains a separate optimization project.
- The ordinary managed `scripts/gate.sh --full` run returned non-zero only because its 20 sandbox/code.execute tests received `sandbox-exec: sandbox_apply: Operation not permitted`. The same 33 sandbox/code.execute tests pass in the permitted host environment; production behavior remains fail-closed and was not weakened.
- Real DeepSeek report run: `cs_did_staggered`, 8 figures rendered, 39 verified cite chips, zero raw citation markers, and non-empty `event_study` / `time_trend` figure sources.
- CLI note: the synchronous CLI path does not register the background `EventManager`, so its `workflow_log.jsonl` is empty; API/UI background runs use the event manager and are covered by heartbeat/cancel/timeout tests.
