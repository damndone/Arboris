# Workbench v1.8.6 Predictive Research Integration Objective

从本地 checkpoint `bfeb346` 继续完成 Workbench v1.8.6 predictive-research correctness
foundation 的 integration 收口。保留原 foundation formal line 的冻结证据，不编辑其
manifest 或历史事件。

必须完成：

1. 未声明 prediction data structure 的新 run 在拟合前以
   `PREDICTION_DATA_STRUCTURE_UNKNOWN` 阻塞，不得进入 legacy random split；legacy
   prediction 仅保留给历史 run 重放。
2. Temporal 与 panel 识别并校验必填列，但在 M0 通用 prediction 路径
   `PREDICTION_SPLIT_PROFILE_NOT_SUPPORTED` fail-closed，不得执行随机或 temporal ML。
3. 仅为 `frequency_weight` 实现明确的 sklearn `sample_weight` 正例；sampling_weight、
   analysis_weight、OLS、panel 与 econometric estimator 权重继续 fail-closed。
4. 缺少 payload contract 的历史 prediction 显示
   `legacy_random_split_v0`、`payload_not_evaluated`、`legacy_only`，使用规定英文 UI 文案，
   legacy 与 v1.8.6 Compare 以 `PREDICTION_LEGACY_RESULT_INCOMPARABLE` 拒绝。
5. MICE 与 prediction 同 run 时必须折内拟合，或在全表路径 fail-closed，并有诱饵测试。
6. Graph、Report、Compare、Table、Run、Agent 只读取同一经过 schema validation 的 typed
   evidence；Graph 至少显示 Dataset Snapshot → Prediction Task → Split Plan → Baseline +
   Candidate Models → Evaluation → Negative Controls → Result，Report 分区显示 development /
   final holdout / negative-control / limitations，Compare 只接受兼容 SplitPlan。
7. 完成 focused tests、formal verification、baseline `7e257f2` 对照归因、quick/full gate
   与 native browser evidence；不得 push、PR、merge、tag。

明确不做：PIT/Quant、实盘、自动调参、深度学习、任意 code.execute、reshape/merge、
temporal/panel prediction execution、OLS/Panel/econometric weights。

## 收口验收与交接（2026-08-02）

### 已完成能力

- 新 run 未声明 `prediction_data_structure` 时，在拟合前以
  `PREDICTION_DATA_STRUCTURE_UNKNOWN` 阻塞，不再进入 legacy random split。
- temporal / panel 会校验必填列，然后以 `PREDICTION_SPLIT_PROFILE_NOT_SUPPORTED`
  fail-closed；本版没有对外交付 temporal/panel ML 执行。
- `frequency_weight` 仅走 sklearn `sample_weight` 正例；`sampling_weight`、
  `analysis_weight`、OLS、panel 与 econometric estimator 权重继续拒绝。
- 历史缺少 `payload_contract` 的 prediction artifact 通过 legacy-only projection
  展示，legacy 与 typed Compare 明确拒绝数值比较。
- prediction + MICE 同 run 当前在全表插补路径 fail-closed，并有诱饵测试。
- Run / Graph / Table / Report / Compare / Agent 读取同一份 schema-validated typed
  evidence；Graph 主链为 Dataset Snapshot → Prediction Task → Split Plan → Baseline +
  Candidate Models → Evaluation → Negative Controls → Result。

### 验证证据

- focused backend：`95 passed, 2 skipped`；前端 focused：`19 passed`；typecheck 通过。
- current quick gate（支持 containment 的宿主）：backend `4456 passed, 2 skipped,
  1 xfailed`；frontend `174 files / 1549 tests passed`；typecheck 通过。
- current full gate：同上 backend；golden / invariants / snapshot `23 passed`；frontend
  `174 files / 1549 tests passed`；typecheck 通过；`GATE PASSED`。
- exact baseline `7e257f2` full gate（先按仓库规则链接共享 `.venv` 与 frontend
  dependencies）：backend `4406 passed, 2 skipped, 1 xfailed`；golden `23 passed`；
  frontend `172 files / 1541 tests passed`；typecheck 通过；`GATE PASSED`。
- golden 23 保持 0-drift；没有新增估计器数值漂移证据。
- native browser 已验证真实 local run 的 Graph、Table、Report：typed SplitPlan、OOS
  指标、baseline、两项 negative controls、四个 Report sections 与 known limitation
  均可见；浏览器 console 无 error。

### 已知限制与边界

- Graph 中的 typed prediction evidence 节点是只读、非物化节点，没有可执行数据节点的
  `node_hash`；点击其通用详情抽屉会显示 `missing_node_hash`。这不影响 Graph 主链、
  Table 或 Report 的 evidence 展示，本版不伪造 `node_hash`，该观察项保留给后续详情
  抽屉专门化工作。
- temporal / panel execution、sampling / analysis weights、PIT、Quant、实盘、自动
  调参、深度学习、reshape/merge 和任意 `code.execute` 均未交付。
- full gate 必须在支持本地 containment 的宿主上运行；agent Seatbelt 中的
  `sandbox-exec: sandbox_apply: Operation not permitted` 只能记录为宿主能力 GAP，不能
  用 skip/xfail 掩盖。

### Formal / Git 状态

- integration devline：`v1-8-6-predictive-research-integration`；最终 manifest 为
  `96515a20a8f5ee363d197ccae43a4f0947ba08f62a8dfeb2d4c7647e2dcb4bbb`。
- formal verifier 已通过；所有 scope 修正均通过 `devline_control.py` CLI 完成。
- 当前 branch 为 `workbench-v1.8.6`，尚未 push、PR、merge 或 tag；提交仍需本地显式
  closeout，发布动作保持授权门控。
