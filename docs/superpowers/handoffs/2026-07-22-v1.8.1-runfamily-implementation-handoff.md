# v1.8.1 交接 — RunFamily 实施起点

日期：2026-07-22
分支 / worktree：`workbench-v1.8.1` / `.worktrees/workbench-v1.8.1`
分支 HEAD：`3f3e54c`（已推送，**未合并**）
基线：`origin/main` = `be0699f`

---

## 0. 一句话

v1.8.0 已发版（tag `v1.8.0` = `63f2fa1`）。v1.8.1 的 **spec、plan、roadmap、backlog
四份文档已就位且已推送，代码一行未写**。下一步是按 plan 的 Task 1 开始实现 RunFamily。

---

## 1. 新对话这样开场

> 任务：Workbench v1.8.1，实现前置项 1（RunFamily 升为一等身份）。
> 工作区 `.worktrees/workbench-v1.8.1`（分支 `workbench-v1.8.1`，HEAD = 本分支最新提交，用 `git log -1` 确认，不要照抄本文档写死的哈希）。
> 先读这三份，顺序不要换：
> 1. `docs/superpowers/plans/2026-07-22-v1.8.1-runfamily-first-class-identity-plan.md`（要执行的）
> 2. `docs/superpowers/specs/2026-07-22-v1.8.1-agent-notebook-analysis-option.md` §2.3、DEC-NB-002（契约）
> 3. `docs/superpowers/roadmap/2026-07-22-agent-analysis-system-roadmap.md` §1（三条不变量）
>
> 读完先跟我确认你理解了 §2 的两条硬约束和 §4 的两条裁定（Trace 最小采集进本版、
> Context Compiler 是本版第二条轨道但另写 plan），再动手。

**环境**：worktree 已建、共享依赖已链接（`frontend/node_modules` 是符号链接）。
**不要**在 worktree 里 `npm install`——`gate.sh` 的 preflight 会 `exit 3` 拒绝。

---

## 2. 动手前必须理解的两条硬约束

这两条是 plan 里最容易被"顺手绕过"的地方。

### 2.1 fallback 不得成为新写入路径

读取优先级：

```
persisted run_family_id 存在   → 用持久化值
persisted 缺失                 → legacy_family_anchor() 计算，记兼容性告警
新建 run 但无 persisted family → 拒绝（RUN_FAMILY_REQUIRED）
```

第三行是命门。写代码时最自然的做法是"没有就算一个"，那会让派生逻辑永久留在新路径上，
本次改造等于白做。plan Task 2 Step 5 专门为此写了一条测试。

### 2.2 验收步骤必须断言内容，不能只断言存在

plan 开头写了这条约定，理由是实测教训：上一份
`plans/2026-07-21-v1.8-deliverables-closeout-plan.md` 的 Task 1 Step 1 要求
"an XLSX workbook with the eight named non-empty sheets"——**这条通过了**，
而三张表仍不可用（`Rolling forecasts` 只有 18 列里的 2 列）。

所以：迁移测试用 `assert before == after` 逐字节比对（不是 `startswith`），
fixture 从**真实 run 目录**复制（不手写）。

---

## 3. 已完成的四个 commit

| commit | 内容 |
|---|---|
| `c66f50a` | 清理已发版的 backlog；写 roadmap；spec 初稿 |
| `4bf2ec0` | 折入四条决议（DEC-NB-001 / DEC-OPT-001 / DEC-OPT-002 / DEC-ART-001） |
| `baae557` | 严格版 B：去掉幻影 `role`；`schema_ref` 改为拒绝而非忽略；RunFamily 一等对象 |
| `3f3e54c` | RunFamily 实施计划（6 Task，TDD 顺序） |

四份文档的位置：

```
docs/superpowers/plans/2026-07-22-v1.8.1-runfamily-first-class-identity-plan.md
docs/superpowers/specs/2026-07-22-v1.8.1-agent-notebook-analysis-option.md
docs/superpowers/roadmap/2026-07-22-agent-analysis-system-roadmap.md
docs/superpowers/followups/BACKLOG.md
```

---

## 4. 两处缺口 — 已于 2026-07-22 拍板（不要再当作开放问题）

这两条是 2026-07-22 review 时发现的，当时 spec 和 plan 都没有覆盖。用户已拍板，
结论写在每条末尾的「**裁定**」里。**v1.8.1 因此是两条轨道，不是一件事。**

### 4.1 Trace 采集无人认领

roadmap §3.1 写着"现在不训练 ≠ 现在不采集"，要求从 v1.8.1 起记录：
Agent 看到什么上下文、生成哪些候选、proposal 是否通过 schema validation、
用户选择/修改/拒绝了什么、执行是否成功、诊断是否通过、结果是否被 Compare 推翻、
结论是否忠于 Artifact。

**但 spec 和 plan 里都没有它。** 若 v1.8.1 就这样发版，v1.8.2 的评测工装开局
零历史。成本在 v1.8.1 很低（Agent 已在写 JSONL），拖到 v1.8.2 就变成
"我们没有过去的数据"。

**裁定（2026-07-22）：进 v1.8.1，只做最小采集。** 不新建 trace 子系统——
`backend/workbench/agent/events.py` 的 `AgentEventStream` 已经是 per-session
的 durable + replayable JSONL 流（`AgentEvent`：`seq` / `event_type` / `payload` /
`command_id` / `created_at`，幂等 `event_id` + 原子追加）。本版只补两件事：

1. **保留策略**——保证 `agent-events/*.jsonl` 不随项目清理被删；
2. **结果标注**——proposal 的终局（`confirmed` / `rejected` / `stale` / `failed`）
   与其触发上下文一并落成事件字段，使单条 trace 可直接充当评测样本。

不做评测工装、不做导出接口、不定义独立 trace schema——那些是 v1.8.2 的事。
验收照 §2.2：断言某条具体事件的字段值，不得只断言"事件已写入"。

### 4.2 Context Compiler 的排期自相矛盾

roadmap §5 判定"RAG 可以延期，**有界上下文编译不能延期**——Notebook Agent
本身就依赖它"。但 v1.8.1 spec 里没有它。

**裁定（2026-07-22）：它是隐藏的第二个前置项，排进 v1.8.1。**

因此 v1.8.1 = 两个前置项，不是一个：

- **轨道 A**：RunFamily 升一等身份 —— 即本交接指向的
  `plans/2026-07-22-v1.8.1-runfamily-first-class-identity-plan.md`，Task 1–6 不变。
- **轨道 B**：有界上下文编译（Context Compiler）—— **计划尚未撰写**。

轨道 B 是设计任务不是机械任务，不得塞进轨道 A 的 Task 列表里凑数。新会话的
正确做法：**先把轨道 A 做完**（它不依赖 B，纯后端身份改造，全程不过 Agent），
再基于 roadmap §5 的六层分级单独写轨道 B 的 plan。若先动 B，A 的迁移顺序
（Task 3）会和 B 的上下文读取路径相互纠缠。

轨道 B 要解决的具体问题有真机证据：v1.8.0 验收时的 `tool_output_budget_exceeded`
（Agent 摘要三度超预算）。正解是**有界投影**，不是向量库。

### 4.3 三条不变量没有执行点（提醒，非阻塞）

roadmap §1 的三条不变量目前只存在于文档。参照：共享依赖规矩在进 `gate.sh`
preflight 之前也只存在于文档，结果三个 worktree 各装一份、浪费 431 MB。
**没有可执行判据的不变量是愿望。** 值得在某个版本里为它们找强制点。

---

## 5. 实施顺序（plan 的 6 个 Task）

```
Task 1  RunFamily 持久化实体（id 不编码 run，run-family:<uuid>）
Task 2  run↔family 成员关系持久化 + 三级读取优先级  ← 含 §2.1 的命门测试
Task 3  既有数据迁移（legacy-family:<root_run_id> 字符串逐字节不变）+ 幂等
Task 4  冲突语义（persisted 优先且报错，不静默重写）+ RUN_FAMILY_MISMATCH
Task 5  rerun/fork 继承来源 run 的 family
Task 6  收口：跑 spec §9.1 六条验收、grep 残留调用点、full gate（golden 须 0-drift）
```

按 §4.1 裁定，Task 6 之后追加一件小事（**不进 plan 的 Task 编号，独立提交**）：

```
Trace 最小采集  agent-events 保留策略 + proposal 终局标注（见 §4.1 裁定）
```

按 §4.2 裁定，轨道 B（Context Compiler）在轨道 A 收口后**另写 plan**，不在本计划内。

**Task 6 Step 1 的第 1 条验收（无 Run 的 Notebook）依赖本计划范围外的 Notebook
对象，标为 deferred——不得因"暂时无法验证"而声称通过。** 其余五条必须闭合。

---

## 6. 关键代码位置

| 什么 | 在哪 |
|---|---|
| 现有 family 派生逻辑 | `backend/workbench/agent/chains.py: legacy_family_anchor()` |
| 家族扫描 | `scan_family()`（同文件引用） |
| proposal 的 stale 判定（读时求值要与它一致） | `backend/workbench/agent/proposals.py` 约 300–340 行 |
| context fingerprint 计算 | `backend/workbench/lineage/node_write_validation.py: compute_context_fingerprint()`（产出 `nocv1:xxxxxxxx`） |
| OperationRecord 状态机（已含 `stale`） | `backend/workbench/agent/operations.py` 约 822、1112 行 |
| artifact 注册（只有 id/type/step，无 role 无 schema_ref） | `backend/workbench/artifacts.py: register_artifact()` |
| 节点 Merkle identity | `backend/workbench/data_operations.py: _ensure_node_index_entry()` |

---

## 7. 环境与命令

```bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.1

# 聚焦测试
LANG=en_US.UTF-8 ../../.venv/bin/python -m pytest tests/<file> -q

# full gate（发版/交接前必跑；会拒绝与 dev server 并存）
LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 bash scripts/gate.sh --full
```

gate 基线（v1.8.0 发版时）：backend 2960 passed / 8 skipped · golden 23 **0-drift** ·
frontend 1294 / 150 files · tsc clean。**family 改造不得影响任何既有数值，
golden 必须保持 0-drift。**

---

## 8. 发版状态

v1.8.1 **未发版**，分支未合并。不得推断任何发布事实。
v1.8.0 已发版：PR #26 merged，`origin/main` = `63f2fa1`，tag `v1.8.0` = `63f2fa1`
（仓库已更名 `Arboris`，旧 URL 仍重定向）。
