# v1.6.5 变量角色层 — 测试反馈梳理 & 完成度核对（不含代码，先捋清楚）

> 来源：2026-06-30 本地 live 测试（后端 8010 = v1.6.5 worktree，前端 5174）。
> 任务：把测试发现的问题汇总成 to-do，对照 spec / plan double-check 哪些没完成。
> 呈现取向（已与用户确认）：**变量节点加角色徽章 + 按角色染色 + 外框容器**。

参考：
- spec `docs/superpowers/specs/2026-06-30-workbench-v1.6.5-variable-role-layer-design.md`
- plan `docs/superpowers/plans/2026-06-30-variable-role-layer.md`

---

## 0. 一句话结论

后端（A/B/C）按 spec 完成且 gate 绿（+ 本次修了一个 POST /runs 的 focal_x 接线漏洞）。
**前端 D3 没按 spec/plan 完成**：spec §8 / plan D3 要求**画布上**的角色分组 + solid/dashed 角色边，
我实现成了**详情抽屉里的 section**（RoleGroupsSection），画布上 x/y/z 角色完全没体现。
这是用户问题 1/2/3/4 的根因。问题 5/6（Pipeline 空、Draft Graph 无返回+丑）**spec §2 明确标为 v1.6.4 范围、不在 v1.6.5**。

---

## 1. 完成度核对（对照 spec / plan）

| 项 | spec/plan 要求 | 实际状态 | 判定 |
|---|---|---|---|
| 后端 A 角色引擎 | `role_layer.py` derive_roles/冲突策略/dedup/canonicalize | 完成，26 测试绿 | ✅ 达标 |
| 后端 B focal_x 持久化 | 解析→canonical→写 run_inputs；结构族清空 | 完成 **但 POST /runs 端点漏接 focal_x 字段**（已在本次修复 + 加 HTTP 回归测试） | ✅ 已修达标 |
| 后端 C recording 角色边 | `enters_as_*`/`offsets_as_exposure`/`identifies_as_instruments`/`configures_*` + dropped 保留角色 | 完成，live 验证 outcome/focal/covariates 边正确 | ✅ 达标 |
| D1 角色常量 | `roles.ts` | 完成 | ✅ |
| D2 groupVariablesByRole | adapter 纯函数分组 | 完成 | ✅ |
| **D3 画布角色渲染** | **spec §8 + plan D3**：在 **GraphView/GraphCanvas** 的变量渲染路径里按角色分组；**实体/识别角色 solid 边、Unit/Time/Cluster 用 dashed `configures_` 边**；model 节点 `roles: unspecified` tag | **只做了详情抽屉 RoleGroupsSection**；画布上：变量节点无角色徽章/染色、角色边无 solid/dashed 样式、无容器外框、节点上无 tag | ❌ **偏离 / 未达标（核心缺口）** |
| D4 抽屉 spec blocks | 公式/识别/offset/推断四块 | 完成（本就属抽屉） | ✅ |
| E1 run-form focal 多选 | 复选框 + 结构族隐藏 + post focal_x | 完成（+ 修了后端接线） | ✅ |
| E2 draft inspector focal_x | multiselect 控件 + 后端 editable_schema 注入 + execute 透传 | 完成 | ✅ |
| F1 forest 身份徽标 | run·hash·source/rerun | 完成 | ✅ |
| 估计不变性 | 系数 golden 0-drift | 23 golden 0-drift | ✅ |

**结论：声称「全 A–G 完成」中，D3 实际未按 spec/plan 落到画布。** 这是要补的主要工作。

---

## 2. 本次测试发现的问题清单（含根因 + 是否在 v1.6.5 范围）

### A 组 — 画布角色可视化（v1.6.5 角色层应落地、属本版本补完）

- **P1 双路径冗余**：model 同时有 `stage:cleaned --ols_robust.fit--> model`（原聚合边）和 4 条 `var:* --enters_as_*--> model`（角色边）。
  根因：角色层之前变量节点不连 model；我新增的角色边制造了第二条平行路径。
  方向：变量可见（展开/角色化）时，**隐藏聚合边 `cleaned→model`**，由角色边承担「变量进入模型」语义；折叠时反之。
- **P2 展开 marker 位置错乱**：`▼ Variables (expanded)` 只有 `cleaned→marker` 一条虚线边，dagre 把它排进 4 个变量中间（第 4 位）。
  方向：marker 不进 dagre 自由布局，固定到组容器标题位（或并入 P3 容器，取消独立 marker 节点）。
- **P3 变量无外框容器**：展开后是 4 个独立节点，看不出「同属一个变量集合」。
  方向：用 React Flow group/parent 节点把这组变量框进一个带标题的容器（用户选定）。
- **P4 x/y/z 角色没上画布（spec §8 核心缺口）**：画布变量节点是裸 `wage (cleaned)`，看不出 Outcome(Y)/Focal(X)/Covariate(Z)。
  方向：① 变量节点加角色徽章（Y/X/Z…）+ 按角色染色/排序；② 角色边按 spec §8 用 solid（实体/识别/exposure）vs dashed（unit/time/cluster）；③ model 节点 `roles: unspecified` tag 上画布。
  注：抽屉里的 RoleGroupsSection / ModelSpecBlocks 可保留为「读法」，但画布必须自身可读。

### B 组 — 独立老问题（**spec §2 明确不在 v1.6.5 范围**，是 v1.6.4 polish 欠项）

- **P5 Pipeline 标签页是空的**：`workbench/views/PipelineView.tsx` 自 V1.5.2 就是 placeholder，从未实现，未排进任何已发版本。
  说明：用户记忆的「这个版本做 pipeline MVP」实为 **v1.6.4 的 Pipeline *Draft* MVP**（从 model 节点 Open as Draft Graph 的草稿编辑链路），与「Pipeline 视图标签页」是两个不同东西。
- **P6 Draft Graph 无法返回上一级**：打开 Draft Graph 后回不到 History-Lineage-Graph。spec §2 列为 v1.6.4「Draft Graph return/layout」polish，未做。
- **P7 Draft Graph 布局丑、内容挤在一起**：同上，v1.6.4 layout polish 欠项。

---

## 3. To-Do List（按优先级 + 范围分组）

### 第一优先：补完 v1.6.5 画布角色可视化（A 组，本版本应落地）
- [ ] **T1** 画布角色边样式：GraphCanvas 边构造读 `edge.op`/`params.role`，实体/识别/exposure = solid、unit/time/cluster = dashed；角色边可着色。（对应 spec §8、plan D3「dashed edges for DASHED_ROLES」）
- [ ] **T2** 消除双路径（P1）：变量展开/角色可见时隐藏 `cleaned→model` 聚合边。
- [ ] **T3** 变量节点角色徽章 + 染色（P4）：GraphNode 对 `var:*:cleaned` 显示其角色（来自 var→model 角色边的 op），Outcome/Focal/Covariate… 着色 + 排序。
- [ ] **T4** 外框容器（P3）：React Flow group 节点把同组变量框起，标题如「Variables（4）· wage~education+age+exper」。
- [ ] **T5** 展开 marker 定位（P2）：marker 并入容器标题/固定位，不参与自由 dagre。
- [ ] **T6** model 节点 `roles: unspecified` / `legacy_unspecified` tag 上画布（目前只在抽屉）。
- [ ] **T7** 回归保障：GraphCanvas/GraphNode/folding 既有测试不破；新增角色画布测试；gate 全绿。

### 第二优先：确认并修 Draft Graph（B 组，超 spec，但用户痛点强）
- [ ] **T8** Draft Graph 返回上一级入口（P6）。
- [ ] **T9** Draft Graph 布局/样式整治（P7）。
- [ ] （需用户补充 Draft Graph 具体表现：是布局挤、边不对、还是编辑不生效？）

### 第三优先 / 单独排版本：Pipeline 视图（P5）
- [ ] **T10** `PipelineView` 从占位符做成真视图——工作量大，建议单开版本（v1.6.6?），不混进角色层。

---

## 4. 需要和用户确认的点
1. A 组（T1–T7）是否本轮就做完整（画布重做 GraphCanvas 变量渲染）？— 用户已倾向「是」+ 选了节点徽章+染色+外框容器方案。
2. B 组 Draft Graph（T8–T9）是否纳入本轮，还是先只做 A 组？（spec 把它划在 v1.6.4 范围之外）
3. Pipeline 视图（T10）确认单独排版本，不在 v1.6.5。
4. Draft Graph「奇丑、挤一起」的具体表现，便于精准修。

---

## 5. 本次已落地的修复（非计划内、测试中发现）
- `fix(roles): wire focal_x form field through POST /runs endpoint`：补 POST /runs 的 `focal_x` Form 参数 + form 字典透传 + HTTP 级回归测试。修复前 run-form 的 focal 声明端到端失效（所有 RHS 落到 explanatory_unspecified）。已提交。

---

## 6. v1.6.4（Pipeline Draft MVP，codex 执行）返工评估

> 背景：v1.6.5 从 `codex/workbench-v1.6.4`@`ddf58c9` 拉分支，v1.6.4 的代码已全部继承在当前 worktree，可直接审查（无需碰 v1.6.4 worktree）。

### 6.1 与 v1.6.5 的冲突/重叠 — **几乎为零**
- **Draft Graph 渲染独立**：`pipelineDrafts/DraftGraphCanvas.tsx` 是裸的「两个按钮 + 一行 `from -> to` 文本」，**不复用 `GraphCanvas`/`GraphNode`**。v1.6.5 的画布角色重做（T1–T7）改的是主 lineage 图的 `GraphCanvas`/`GraphNode`。两套渲染完全独立 → **互不冲突**。
- **唯一交集 = `ModelNodeInspector`**：v1.6.5 E2 已在它上面叠加 focal_x multiselect + 后端 editable_schema 注入 + execute 透传。是**协作叠加**，不是冲突，已绿。
- **后端 `pipeline_drafts.py`**：v1.6.5 仅追加 focal_x 相关逻辑，未改 v1.6.4 的草稿契约/校验/dedupe。

### 6.2 v1.6.4 哪些「没做完 / 做得差」
| 项 | spec 怎么说 | 实际 | 判定 |
|---|---|---|---|
| 草稿后端链路（create/patch/validate/execute/dedupe/hash-conflict/rollback） | §6–§8 完整契约 | 实现 + 测试覆盖 | ✅ 扎实，**不需返工** |
| Draft Graph 画布 | §6.3 定为「fixed InputNode → ModelNode」极简固定视图（有意 thin MVP，非 DAG builder，§6.8 明确 out-of-scope drag-drop 等） | 两按钮 + 文本边 | ⚠️ **符合 MVP 范围**，但毫无视觉打磨 |
| Draft Graph 页面布局 | §6.3 画的是 Canvas / Inspector / Validation **分区** | 全部竖直堆在一个 panel 里 → 「挤在一起」(P7) | ❌ 实现未按 §6.3 分区 |
| **返回上一级导航** | §6.3 toolbar 列了 Draft title / Validate / Execute，**没列 back**；§6.8 out-of-scope 也**没列**它 | `DraftGraphRoute` 无任何返回入口，打开后只能 Execute 或卡住 (P6) | ❌ **spec 遗漏 + 实现缺失**（不是有意 defer） |

结论：v1.6.4 的**后端与数据契约不需要返工**（thin slice 完整、有测试）。需要返工的只是 **Draft Graph 的两个前端 UX 缺口**，且因组件独立，可与 v1.6.5 画布重做并行做、不会撞车。

### 6.3 v1.6.4 返工 To-Do（= 本文档 §3 的 T8/T9 细化）
- [ ] **T8 返回导航**：`DraftGraphRoute` 顶部加 “← Back to Lineage”，`navigate(-1)` 或回 `/runs/{source_run_id}?tab=lineage&project_root=…`（draft 的 `created_from.source_run_id` 已有）。spec 遗漏项，应补。
- [ ] **T9 布局分区**：按 v1.6.4 spec §6.3 把 Toolbar / Canvas / Inspector / Validation 分区（左图右 inspector 或上下分栏），别全堆一个 panel。Canvas 维持「固定两节点」语义即可，但要有像样的节点卡片 + 连线，而非纯按钮堆叠。
- [ ] （可选）**Pipeline 标签页**（P5 / T10）与 v1.6.4 无关：是 V1.5.2 `PipelineView` 占位符，独立单排版本。

### 6.4 版本归属 — **决策：方案 A（2026-06-30 用户拍板）**
- **T1–T7（画布角色重做）= v1.6.5 本轮主线**，在 `codex/workbench-v1.6.5` 上做。
- **T8/T9（Draft Graph 返回 + 布局）= v1.6.4 的 follow-up**，单独在 v1.6.4 上下文做，**不混进 v1.6.5 分支**。本轮不碰它们，也不碰 v1.6.4 worktree。
- **T10（Pipeline 标签页视图）= 独立单排版本**（v1.6.6?），与角色层、v1.6.4 均无关。
- 符合版本隔离 [[feedback-version-isolation]]。spec §2 已把 T8/T9/T10 划在 v1.6.5 范围外。

---

## 7. 全项目技术债登记表（2026-06-30 全面复盘）

> 扫描来源：`frontend/src` + `backend/workbench` 的 placeholder/TODO/stub/disabled 标记、各版本 spec 的 out-of-scope、memory 里的 NEXT/deferred。分优先级。状态：📋 未做 / 🔧 本会话已修 / ⚠️ 有意 deferred。

### A. Draft Graph / Pipeline（用户已撞到，最高优先）
- 🔧 **P6 Draft Graph 无返回上一级(2026-06-30 已修)** — `DraftGraphRoute.tsx` 全组件无 back 入口;spec §6.3 toolbar 漏列(不是有意 defer)。→ T8。
- 🔧 **P7 Draft Graph 布局全堆一起(2026-06-30 已修:分区 + 节点卡片)** — `DraftGraphCanvas.tsx` = 两按钮+一行文本;`DraftGraphRoute` 没按 spec §6.3 的 Canvas/Inspector/Validation 分区。→ T9。
- 📋 **P5 Pipeline 标签页空** — `views/PipelineView.tsx` 自 V1.5.2 起是 placeholder,从未实现。→ T10(单独版本)。
- ⚠️ **草稿执行只支持 rerun_child** — `new_run` 模式后端返回 `NEW_RUN_EXECUTION_NOT_ENABLED`(api.py:1241, pipeline_drafts.py:541);有意 gated,若要"从草稿建全新 run"需实现。

### B. Workbench 视图 / 面板（占位符,整块功能欠做）
- 📋 **Table 视图** — `views/TableView.tsx` = "coming soon" placeholder(V1.5.2 P3)。
- 📋 **底部面板 3 个占位** — `bottomPanelRegistry.tsx`:Shell / Pending / Timeline 都是 `PlaceholderPanel`;只有 Logs 是真的。

### C. 节点 / 顶栏动作（disabled 占位)
- 📋 **"Ask AI about this node"(右键菜单)** — `actionRegistry.ts:176` disabled,无 `/llm/chat`。
- 📋 **抽屉 AskAISection** — feature-flag 默认关;`askAiForNode` 未接真 LLM(memory v1.6.2:只读 packet,NEXT 候选=接真 /llm/chat)。
- 📋 **"Mark needs review"** — `actionRegistry.ts:197` disabled,review 工作流从未实现。
- 📋 **顶栏 "Rerun"** — `actionRegistry.ts:213` disabled 占位(注意:节点级 rerun 已通过 OperationSection 走 POST /runs/<id>/rerun 是活的;顶栏这个是另一个死按钮)。
- ⚠️ **顶栏 "Generate report"** — `actionRegistry.ts:224` disabled,标注 V2.0。

### D. 可编辑控件(read-only 占位,限制草稿/操作编辑能力)
- 📋 **controlFactory 5 种控件只读** — `controlFactory.tsx`:`radio / slider / text / textarea / toggle` 都是 `makePlaceholder`(渲染但不可交互);只有 `select / columns / multiselect` 可用。任何 editable_schema 用到那 5 种 kind 的参数在草稿/操作面板里改不了。

### E. 计量估计器 deferred(有意,文档已记,冷门)
- ⚠️ **dCDH** — 连续处理强度 deferred(`dcdh_spec.py:53`);down-switch-first / baseline=1 deferred(`dcdh_spec.py:88`)。
- ⚠️ **CS/SA** — out-of-core 影响函数 deferred(`cs_attgt.py:365`);稀疏 SA 指标 deferred(`sa_attgt.py:143`)。
- ⚠️ **engine/pack.py:70** — 一批 declared-not-implemented(标注 V1.5.6+/agent)。

### F. 本会话已修(登记备查)
- 🔧 **D3 角色没上画布** — 原只在抽屉;已补画布角色徽章/染色/边样式/容器(T1–T7)。
- 🔧 **POST /runs 丢 focal_x** — 端点没接 focal_x 字段;已补 + HTTP 回归测试。
- 🔧 **forest(headset)角色 op/params 被抹** — 默认 Graph 视图走 forest,后端投影丢了 op;已在 headset.py + 前端 adapter 透传 + suppressAggregate 兼容 hash id。

### G. 跨切面 / 流程
- 🔧 **History `PROJECT_NOT_FOUND` 直接抛红错(2026-06-30 已修:友好空态)** — 旧 localStorage `lastProjectRoot` 指向不存在项目时,没有友好空态(应提示"选择/新建项目")。
- 📋 **`npx vitest` 解析错版本**(装到 `~/node_modules/vitest` 无 jsdom)— dev-env papercut,用 `frontend/node_modules/.bin/vitest` 或 gate 规避;可在文档/脚本固化。
- 📋 **v1.6.5 尚未 push/merge/tag**;发版后按流程清 dev 缓存([[feedback-version-cleanup]])。

### 优先级建议
1. **高(用户已撞)**:A 组 Draft Graph P6/P7(v1.6.4 follow-up),History 友好空态(G)。
2. **中(整块功能)**:B 组 Table/Pipeline 视图、D 组只读控件(限制草稿编辑深度)。
3. **中低(功能扩展)**:C 组 Ask AI 接真 LLM、review 工作流、new_run 草稿执行。
4. **低(有意 defer,冷门)**:E 组估计器扩展。
