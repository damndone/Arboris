# v1.8.4 remediation handoff

## 当前状态

工作目录为 `.worktrees/workbench-v1.8.4`，branch 为 `workbench-v1.8.4`，本轮
formal line 为 `v1-8-4-open-incidents-remediation`，baseline 为
`fdf47a407e137004d242bae9228e65b8456bd76d`。本 handoff 记录 remediation 收尾；
旧的 v1.8.4 release handoff 已归档。提交后仍**未 push、未 PR、未 merge、未 tag**。

## 本轮已完成

- Panel OLS 已走通 Notebook option → confirmation → materialization → execution：
  entity/time 字段有显式 schema 校验，非法或缺失时 fail-closed。
- `operation.multi_step` 的 panel-FE 与同设定 dummy-FE 在持久化结果上逐系数一致到
  `1e-8`；robust dummy branch 的正确持久化类型是 `ols_robust`，不可被误写成普通 OLS。
- Global Agent 的 committed workflow receipt reader 支持八个候选 run 的单次有界
  lookup；模型图和 predictor-residual 图暴露分箱聚合事实及 artifact 引用，而非行级数据。
- proposal-ready、covariance 描述边界、Notebook dependency recovery、workflow
  transcript 导航、blocked-run 视觉状态均有契约回归。
- in-app browser 用真实 DeepSeek Flash 完整运行了 Panel FE：run `94471f01` 的
  Table、Report 和 Global Agent 读取持久化系数/置信区间均可见，导航至 Table 后
  transcript 保留。

## 原 12 条 incident 的结论

| 结论 | incident |
|---|---|
| resolved | `ac7b9d42` panel workflow model composition；`33c552d6` Global workflow answer path；`64ec6d96` predictor-residual evidence；`8ff5b466` model figure evidence |
| resolved | `9aac0586` proposal ready terminal；`2c20a717` covariance interpretation；`11a22915` dependency shape；`0a41f2a3` composition transcript/browser partial |
| resolved | `e3fd7954` blocked run visual state；`df709bbd` browser controller policy（控制器规程，不是产品 bug） |
| open | `15b227d6` legacy Notebook load: fixture 加载与编译未复现；不凭猜测改代码 |
| open | `ddcc9408` DeepSeek V4 Pro upstream: UI 已验证 provider/tool 两种失败分流，但本轮未取得原 provider 故障的归因复现 |

每条结论都在本 formal line 以**原 incident_id**追加事件；不要只看本表判断状态。

## 验证

- 聚焦 backend 回归：`181 passed, 1 skipped`，覆盖 workflow、panel covariance、
  Agent evidence/routes/tools、Notebook planning/routes、context compiler。
- 前端 targeted：blocked/legacy 路由、AgentPanel upstream/tool 分流与 TypeScript 均通过。
- full gate：golden `23 passed`；frontend `171 files / 1513 tests`；TypeScript 通过。
  backend 为 `4229 passed, 20 failed, 11 skipped, 1 xfailed`。20 个失败全部来自本机
  `sandbox-exec: sandbox_apply: Operation not permitted`，故 full gate 未绿。
- 浏览器验收限 Workbench 内置浏览器，未用 HTTP 或外部浏览器替代。服务重启后需要
  建立新验收 tab，而不要复用旧 tab 导航。

## 未完成与边界

1. `15b227d6` 与 `ddcc9408` 保持 open，原因见上表。
2. 计时器跨组件重挂只有 deterministic test，没有浏览器中的在途重挂证据。
3. 当前 Mac 的 native containment 不可用；不得将 full-gate 的 sandbox 失败叙述为
   已通过隔离，也不得通过弱化 sandbox 取得绿色。
4. 没有引入任意代码执行、原始数据浏览或自动执行；workflow 仍经 typed validation、
   Proposal/Risk confirmation 与持久化 provenance。

## 下一位接手者

先读：

1. [`v1.8.4 release notes`](../../releases/v1.8.4-release-notes.md)
2. `docs/superpowers/plans/2026-07-31-v1.8.4-open-incidents-codex-brief.md`
3. `.agent/devlines/v1-8-4-open-incidents-remediation/context-pack.md` 与 events
4. 本文的 open 边界

首次检查：

```bash
cd "/Users/jiayuanren/项目规划/.worktrees/workbench-v1.8.4"
git status --short
LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 PYTHONPATH=backend \
  .venv/bin/python scripts/devline_control.py verify --all
```

不要 push、PR、merge 或 tag，除非用户再次明确授权。若需要重启 UI，保留 backend，
只在 `frontend/` 运行 `npm run dev -- --port 5177`，确认端口后在内置浏览器新开验收 tab。
