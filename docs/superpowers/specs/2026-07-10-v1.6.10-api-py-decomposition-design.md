# v1.6.10 — api.py 分层拆分（D1）+ P1 draft-editability 修复 · 设计 spec

> 日期：2026-07-10 · 分支/worktree：`workbench-v1.6.10`（从 main `b6d7098` 起）
> 决策依据：`roadmap/architecture-debt.md` D1（最高架构优先，且还在涨）
> + `followups/v1.6.9-followups.md` §2-P1（🔴 draft 存不了修改）。
> 用户拍板：**不开 v1.6.9.1 小版本；P1 折进 v1.6.10；本版核心 = D1 拆 api.py，
> 一切以「为后期拓展性打好基础」为前提；北极星主线本版酌情、非承诺。**

---

## 0. 一句话目标

把 2206 行的 `workbench/api.py`（FastAPI 承重容器）拆成**路由 / service / repository 三层**，
让"每加一个端点就往大文件叠"的累积模式结构性终结；顺带把 🔴 P1（auto-mode draft 不可编辑）
在服务层修掉。**纯搬迁 + 一个契约修复，行为对外不变——契约测试 + golden 0-drift 是硬不变量。**

## 1. 为什么这是拓展性打底（与北极星的关系）

roadmap 北极星 = 一张 lineage graph 完成所有操作（编辑/rerun/对比/AI/报告）。这些新能力
（v1.6.10+ 的图内对比 / 节点 AskAI / 引用报告）**每一个都要加 2-4 个新端点**。今天它们只能
往 api.py 叠 → 大文件继续涨、契约越糊越死。先立三层 + "不许再往大文件加"的规矩，之后每个北极星
功能都是**新 router + 新 service**，api.py 不再动。**这就是"为后期拓展性打好基础"的具体兑现。**

## 2. 现状盘点（2026-07-10 worktree 核实）

- `api.py` 2206 行：23 路由 + ~40 私有辅助，混装 6 类职责：路由定义、请求解析（Form/JSON）、
  文件上传、后台任务调度（`_bg_run`）、SSE 帧、结果汇总（`_model_results`/`_normalize_issue_stream`）、
  run 目录/artifacts 仓储读写、错误归一化、项目创建。
- **已有的层（复用，不重造）**：
  - `orchestrator/`（子包）= workflow 执行层，api.py 经 `_run_workflow`/`run_workflow`/`run_batch_y_workflow` 调它。
  - `events.py` = `EventManager`（SSE 基础设施 + 线程池 slot 生命周期）已抽出。
  - `lineage/`（19 文件）、`engine/`、`econometrics/` 等领域逻辑已在子包。
  - `projects.py`（`create_project`/`create_run`）、`artifacts.py`（`read_json`/`write_json`）已抽。
  - `api_errors.py` = `WorkbenchAPIError` + `register_error_handlers`。
- **护栏（D1 前置条件，已满足）**：`tests/test_api.py`、`test_api_run_params`、`test_api_capabilities`、
  `test_api_errors`、`test_api_runs_imputation`、`test_graph_api`、`test_pipeline_drafts_api`、
  `test_project_forest_api`、`test_uploads_api`、`test_batch_y_runner_api` + `tests/contracts/`
  + `test_behavior_snapshot.py` + golden 23。**端点级契约齐全，拆分有护栏。**
- **命名坑**：`router.py` 是**数据集分类**逻辑（`classify_dataset`），与 HTTP 路由无关——不要碰、
  新路由层不要叫 router.py 以免混淆。

## 3. 目标分层（拆完的形状）

```
workbench/
  app.py            # 新：FastAPI 实例 + register_error_handlers + include_router×N。~60 行。
  api.py            # 保留为兼容 shim：`from .app import app`（uvicorn/测试 import 路径不破）。
  http/             # 新：路由层（薄）——route def + pydantic 请求/响应模型 + 调 service
    __init__.py
    form_parsing.py     # _parse_focal_x / _inject_focal_x_control / _safe_int / _parse_json_str_array
    projects_routes.py  # /capabilities /projects /uploads
    runs_routes.py      # POST /runs /runs/batch · GET /runs /runs/{id} /artifacts /report /events
    graph_routes.py     # /graph /runs/{id}/graph（+ _annotate_editable_node(s) / _backfill_schema_values）
    drafts_routes.py    # /pipeline-drafts/*（from-node/genesis/list/get/patch/patch-node/delete/validate/execute）
    rerun_routes.py     # /runs/{id}/rerun
  services/         # 新：编排逻辑，不 import fastapi
    __init__.py
    run_service.py      # _submit_run · _bg_run 调度 · _write_upload · _mark_interrupted_if_dead
    results_service.py  # _model_results · _normalize_issue_stream · _dummy_coded_columns · _auto_dummy_coded_issue
    draft_service.py    # _execute_genesis_draft · _source_params_from_schema · _read_indexed_node_hash · P1 修复点
    rerun_service.py    # rerun 编排 · _has_context_target_fields · _manual_patch_idempotency_*
  repository/       # 新：纯文件系统读（无业务判断）
    __init__.py
    run_repository.py   # _resolve_project_runs_dir/_resolve_run_root/_resolve_project_root
                        # _read_manifest/_summarize_manifest/_artifact_counts
                        # _read_artifacts_index/_read_artifact_records/_group_artifacts/_resolve_artifact_path
                        # _list_existing_artifacts/_read_errors/_detect_failed_stage
```

依赖方向（单向，禁回环）：`http → services → repository → (orchestrator/lineage/engine 领域层)`。
路由层只做：解析请求 → 调 service → 包装响应 / 抛 `WorkbenchAPIError`。业务判断一律下沉 service。

## 4. P1 修复（折进本版，服务层落地）

**病**：auto-mode base run → from-node draft 的 `params.model_type="auto"`；`editable_schema` 的
model_type options 只含具体家族（无 `auto`）；DraftEditorSection Save 全量回传 → `PATCH
/pipeline-drafts/{id}` 逐参数校验 → 422 `INVALID_PARAM_OPTION`。任何从 auto-mode run
（旧 Submit / API / CLI 默认）fork 的 draft 完全不可编辑。

**选定修法 = ②+① 彻底修**（用户拍板"要修就修彻底，为拓展性打底"）：

**② 后端 PATCH 对「值未变的参数」跳过 option 校验（契约层兜底，主修）**：
- 在**契约执行点**修，与客户端无关——FE/CLI/API 任一发全量参数都不再误伤。这是根治。
- 语义正确——`auto` 不是可执行家族，不该进 editable_schema 合法项（排除 ③）。
- 规则：PATCH 校验时，若某参数的新值 `==` 该 draft 当前 stored 值（未改动），跳过其 option 白名单
  校验；只对**真正改动**的参数做 option 校验。改动 auto→具体家族仍正常校验、仍会拒绝非法家族。
- 落点：`draft_service`（Phase 3/5），PATCH 校验逻辑（现 `patch_pipeline_draft` @ api.py:1395）。

**① 前端 DraftEditorSection 只回传 changed fields（客户端卫生，同步做）**：
- UI 已经算出并高亮 "Changed fields" → 让 PATCH 只发这些，而非全量 params。
- 减少无谓回传、与后端 ② 双保险；也让请求语义诚实（"我只改了 covariance"）。
- 落点：`frontend/` DraftEditorSection Save 逻辑 + 补/改前端测试。

- 涉 v1.6.4 draft 契约 → 本 spec 覆盖之。补测试（后端）：auto-mode fork → 只改 covariance →
  PATCH 200；改 model_type 到非法值仍 422；改到合法家族 200；**值未变的 model_type=auto 全量回传也 200**。
  补测试（前端）：Save 只发改动字段。
- 真机复现验收：`draft_82b538d59b5f4d0687d326842f0ccb2a` 场景 Save 成功。

> 决策记录：用户明确"不怕工程量、修彻底"。故不取"只后端②"的最小方案，② 治本 + ① 客户端也一并做干净。

## 5. 分阶段执行（阶段间过 gate，纯搬迁零行为漂移）

> 纪律：每阶段**只搬不改逻辑**（P1 除外，单列 Phase 5）；每阶段后 worktree 内跑 gate
> （`LANG=LC_ALL=en_US.UTF-8 ./scripts/gate.sh`）确认 BE 全绿 / golden 23 0-drift / FE / tsc。

- **Phase 0 baseline**：worktree 内先跑一次 full gate 存基线（证明起点绿）；确认契约 + behavior_snapshot 覆盖 23 路由的响应形状。若有端点无契约测试 → 先补最小快照再拆（护栏优先）。
- **Phase 1 repository 层**：抽 `repository/run_repository.py`（最独立、纯函数、低风险）。api.py 改为 import。gate。
- **Phase 2 results service**：抽 `services/results_service.py`（`_model_results`/issue 归一化）。gate。
- **Phase 3 run service（后台调度+上传）**：抽 `services/run_service.py`（`_submit_run`/`_bg_run`/`_write_upload`）。SSE 复用 `events.py`。gate。
- **Phase 4 router 化**：逐簇把 route def 搬进 `http/*_routes.py`，api.py→app.py 只留 `include_router`。**一次一簇**（projects → runs → graph → drafts → rerun），每簇后 gate。`api.py` 留 `from .app import app` shim 保 import 路径。
- **Phase 5 P1 修复**：在 `draft_service`/PATCH 校验点落 ② + 补 3 个测试。gate。
- **Phase 6 收尾**：full gate + 三级评审（Implementer→Test&QA→Reviewer，子代理用 opus）+ 真机 smoke（含从 auto-mode run fork draft 改 covariance 存成功）。

## 6. 不变量 / 验收

- **对外行为零变**（除 P1 从"422 失败"变"200 成功"）：所有契约测试、golden 23、behavior_snapshot 0-drift。
- `api.py` 行数：2206 → app.py <~80 + 各 http/services/repository 文件均 < ~400 行；**无单文件 > 500**。
- import 路径不破：`workbench.api:app`（uvicorn target）、测试里的 `from workbench.api import ...` 仍工作（经 shim）。
- 依赖单向：`http→services→repository→domain`，无回环（可加一个 import-lint 断言）。
- P1：auto-mode fork draft 改 covariance PATCH 200；改 model_type 非法仍 422。

## 7. 非目标（本版不做）

- 不动 `orchestrator/` 内部结构（它已是包，本版只是让 service 层更清晰地调它）。
- 不做 D2（GLM 注册表）/ D6（OpenAPI codegen）/ D4（CSS）——architecture-debt 明确"单版本别贪多，D1 一个就够预算"。
- 北极星主线（图内对比/AskAI/报告）**本版不承诺**；D1 落稳、gate 绿、评审过后**再酌情**决定是否附带一小步（需先定 roadmap 开放问题 #3 = Ask AI 的 `/llm/chat` 模型选型）。
- 前端仅动 P1 的 ①（DraftEditorSection Save 只发 changed fields）；D1 拆分是纯后端重构，
  前端其余不动（`WorkbenchRouteContainer` 前端拆分是独立债，本版不含）。

## 8. 风险

- **router 化易漏隐式副作用**：FastAPI route 顺序（如 `/runs/{id}` vs `/runs/batch` 的路径优先级）、
  依赖注入、`Form(...)` 解析细节。缓解：一次一簇 + 每簇 gate + 契约测试钉响应形状。
- **循环 import**：service 调 orchestrator、orchestrator 可能反向引用 → 抽层时以依赖方向为准，
  必要时用 TYPE_CHECKING / 局部 import。
- **behavior_snapshot 覆盖盲区**：若某端点响应未被快照钉住，纯搬迁可能悄悄改形状。Phase 0 先补盲区。
```
