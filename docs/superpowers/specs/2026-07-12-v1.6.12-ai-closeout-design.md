# v1.6.12 — AI 收尾 + 兼容（设计文档）

> 2026-07-12 起版。worktree `.worktrees/workbench-v1.6.12`,branch `workbench-v1.6.12`(base = main `f1fdb09` = v1.6.11+反馈批3留痕)。
> 定位:**v1.7 Agent Harness 的前置集**——把 AskAI/报告线收干净,清掉两个用户可感 bug。
> 范围来源:followups §1 的 V1/V2/V3/V4/V5/V6/V8/V9/V10(§0 有圈定说明与排除项)。

## 0. 为什么先 v1.6.12 再 v1.7

v1.7 的「AI 操作 diff 留痕」依赖**带类型的可检查操作记录**契约(对抗审查 2026-07-10 战略结论)。
本版 V3 留痕 tab 就是这份契约的第一个消费者——typed record 先在只读场景把形状定下来,
v1.7 的"提议→确认→执行→留痕"操作环直接续写同一记录流。V6(AskAI 历史)与 V3 同源同素材。

## 1. 任务切片(按依赖排序)

### T1 · V9 折叠框回归(bug,先查后修)
- 现象:变量多时无外层折叠框,纵向爆屏。
- 代码面预查(2026-07-12):`folding.ts` 对 forest key 兼容(`variantOf` 用 `endsWith(":cleaned")`,
  headset 已把 `parent_stage_id` remap 成 dedup key,`GraphCanvas` L433 确在调 `foldVariableClusters`)。
  阈值=严格 `>3` 才折。
- 假设:①用户场景恰好 ≤3 变量(本来就不折,是设计不是 bug→改成 ≥2 或加手动折叠?拍板)
  ②forest 投影下 `kind !== "variable"` 或 parent key 不一致(真回归)。
- 步骤:先造 5+ 变量真机项目复现定性,再修/拍板。

### T2 · V3 AI 交互留痕 tab(契约打底,本版核心)
- 定义 typed 操作记录:`AiInteractionRecord`(kind: ask_ai | report_generate;时间戳/节点 key/
  question/model/context_fingerprint/答案摘要或全文/事实表快照 ref)。
- 存储:先 localStorage per-project(照 `reportHistory.ts` cap 模式);后端化留给 v1.7。
- UI:底部面板三个 stale tab("lands in V1.5.3")一并处置——第一个做实为「AI activity」只读时间线;
  另两个(Shell/Timeline)文案改 honest(v1.7 方向)或移除,死按钮不留。

### T3 · V6 AskAI 历史 + V5 md 渲染(与 T2 同素材)
- V6:AskAI 面板内 per-node 问答历史(读写 T2 的记录流,不再覆盖)。
- V5:答案用 Report 视图既有 md 渲染管线(citeMarkup 同源)渲染,不再裸 `###`/`**`。

### T4 · V1 A3 逐 artifact「Explain」
- drawer artifact 行加 Explain → 走 askAiAboutNode 通道,packet 增加 `focus_artifact`
  字段(name+preview),system prompt 声明聚焦该 artifact。

### T5 · V2 A4 LLM 设置面板
- BE:`GET /llm/config`(脱敏:base_url/model/key 是否已配,**绝不回 key**)。
- FE:设置区块只读展示 + 未配置时的引导文案(指向 `~/.config/econometrics-workbench/llm.env`)。

### T6 · V4 legacy /graph 挂住(兼容 bug)
- v1.5.x 老项目 scanner hang。定位挂点(疑 runs 扫描对旧布局死循环/慢 IO),
  修成 degrade(legacy 标记/跳过),不 hang。复现样本:`~/Desktop/安全边际筛选`。

### T7 · V8 画布复位键(小)
- GraphCanvas 控件区加「Reset layout」:清 layout seed + 清拖拽偏移 + fit view。

### T8 · V10 Home 整合(拍板后做)
- 方向已获用户认可(反馈批3)。实现细节需拍板:topbar 项目切换器带「最近项目+新建」,
  Home 路由退役 or 重定向 `/`→上次项目。localStorage lastProjectRoot 已有。

## 2. Gate 与流程

- 三级评审(Implementer→QA→Reviewer)每任务;gate = BE 全量+golden 0-drift+FE 全量+tsc 0;
  LANG/LC_ALL=UTF-8;gate 前停 preview/杀残留 vitest;真机 smoke 走 launch.json
  `backend/frontend-v1611-c2`(8014/5176,worktree 路径进版后改指 v1.6.12)。
- ⚠️ cwd 陷阱:每条 Bash 显式 cd 本 worktree;提交前查主 checkout 污染。

## 3. 排除项(勿蔓延)

V7③ 工具式 drill-down、V11 数据层图内化 → v1.7;二类工程债(N2/S/W/U/D)非阻塞,
只在顺手时清;M6 统计方法四大类独立穿插另排。
