# V1.5.1 — Visual Alignment & Live Run Design Spec

| | |
|---|---|
| **Spec ID** | `v1.5.1-visual-alignment-design` |
| **Date** | 2026-05-25 |
| **Status** | Draft for three-party review |
| **Parent branch** | `workbench-v1.5.0` (commit `6a7bc5e`, V1.5.0 PR [#6](https://github.com/damndone/Econometrics-Workbench/pull/6) pending merge) |
| **Impl. worktree** | `.worktrees/workbench-v1.5.1` (created from `workbench-v1.5.0`) |
| **Rebase plan** | Rebase onto `main` after V1.5.0 PR merges |
| **Predecessors** | V1.5.0 lineage UI/UX integration |
| **Design package** | `/Users/jiayuanren/项目规划/uiux/` (panels.jsx · app.jsx · styles.css · screenshots/initial.png) |
| **Baseline** | FE 333 / BE 618 passing at V1.5.0 sign-off (commit `6a7bc5e`) |

---

## 1. Goals and non-goals

### 1.1 Goals

Close the visual gap between V1.5.0 (lineage canvas + drawer interior) and the design prototype's outer shell, and replace the run-progress polling fallback with real-time event streaming. Specifically:

1. **SSE-driven run progress** — replace `waitForRunTerminal` polling with `connectRunEvents`, surface step events to the Submit page, retain polling only as a fallback when EventSource is unavailable.
2. **First-review-node auto-selection** — on entering `/runs/<id>`, default `?node=` to the first node flagged `trust=review` (then `caution`), mirroring the editorial prototype's entry state.
3. **LeftRail** — fixed 8-stage navigation sidebar with per-stage node counts and review-needed indicators; clicking a stage focuses (fitView) the canvas to its bounding box and selects the first node in that stage.
4. **Top breadcrumb** — workspace label (from `WORKBENCH_PROJECT_ROOT` basename) + run pill + tab switcher (Graph active; Table & Pipeline disabled with "Coming in V2.0" tooltips).
5. **Polish bundle** — `lastRun` state persists across navigation, `/runs` history list adopts the dark chrome consistent with `/runs/<id>`, `WORKBENCH_UI_ORIGIN` falls back to `:8000` when `:5173` is dead.

### 1.2 Explicit non-goals (deferred to V1.5.2+)

- Multi-tab DetailDrawer + compare mode (spec §17 Q1; refactor of `useSelectedNode` → `useTabs`)
- `useGraphKeyboard` multi-tab singleton guard (tied to multi-tab landing)
- AskAI section with SSE LLM backend (spec §17 Q2)
- Artifact / Coefficient sections (spec §17 Q3 — wait for backend fields)
- Bottom terminal panel
- Trust banner inline action buttons (rerun-to-here, mark-bad-decision)
- Drawer header breadcrumb chip path (cosmetic)
- Real-world large-dataset performance smoke (separate release-gate task)
- `getDPDisplay` → `getDecisionDisplay` symbol rename (codebase-wide cosmetic)
- 798 KB chunk code-split

### 1.3 Success criteria

- V1.5.0 FE 333 + BE 618 tests stay green; **V1.5.1 targets FE 354 / BE 621** (new tests for SSE wiring, auto-select, LeftRail counts, Topbar route binding, UI origin resolver)
- Browser smoke checklist gains L21–L29 covering: SSE step display, auto-select fixture, LeftRail click → fitView, Topbar tab disabled tooltips, `/runs` dark chrome, lastRun persistence
- 5-minute polling timeout vulnerability (V1.5.0 handoff §3.1) closed
- React Flow v11 class lock vulnerability (V1.5.0 handoff §3.4) **acknowledged but not closed** — abstracting Controls to native HTML is a V1.5.2 task

---

## 2. Locked-in consensus

| Topic | Decision |
|---|---|
| SSE depth | **Full** — expose step events to Submit UI; `waitForRunTerminal` keeps external signature, internals composed of `connectRunEvents` |
| SSE fallback | Polling stays as fallback when `EventSource === undefined` (e.g., jsdom tests, ancient browsers) |
| Auto-select priority | `trust=review` first, then `trust=caution`; if none, no selection (don't fabricate) |
| Auto-select URL | `replaceState` (no history pollution) |
| LeftRail click | **Focus + select first node** — `useReactFlow().fitView({nodes: stageNodes, padding: 0.2})` + `setSelected(stageNodes[0])` |
| LeftRail counts | Total nodes per stage + red dot when any node in stage has `reviewStatus="needed"` or `trust ∈ {review, caution}` |
| LeftRail collapse | **Not collapsible in V1.5.1** — fixed 200 px width |
| Topbar tabs | Graph active; Table / Pipeline disabled with `aria-disabled="true"` and tooltip "Coming in V2.0" |
| Topbar workspace | Derived from `runDetail.project_root` basename (e.g., `/tmp/wb-smoke` → "wb-smoke") |
| Branch strategy | `workbench-v1.5.1` off `workbench-v1.5.0`; rebase onto `main` after V1.5.0 PR merges |
| Worktree isolation | `.worktrees/workbench-v1.5.1` — all V1.5.1 artifacts live here, **never** in V1.5.0 worktree |
| Execution order | **Serial** — T5 polish → T1 SSE → T2 auto-select → T3 LeftRail → T4 Topbar |
| Per-track gate | Implementer → Test & QA → Reviewer → commit + push (per memory `feedback_review_workflow.md`) |
| Hooks shape | **Unchanged in V1.5.1** — multi-tab refactor of `useSelectedNode` → `useTabs` is V1.5.2 |
| `LineageContext` | **Unchanged in V1.5.1** — no new fields, no new consumers beyond LeftRail |
| `sectionRegistry` | **Unchanged in V1.5.1** — no new sections |
| `graphAdapter` | **Unchanged in V1.5.1** — no new backend fields |

---

## 3. Spec §17 Extension Self-Check — Official Answers (Archive)

The V1.5.0 spec §17 required six design-integrity questions to be answered before sign-off. These are the official answers, archived here to close the V1.5.0 design gate. Each answer was verified against the post-V1.5.0 codebase.

### Q1. V1.5.1 adds multi-tab + compare. Which V1.5.0 files change?

**Affected files**:
1. `frontend/src/lineage/hooks/useSelectedNode.ts` (replaced by / upgraded to `hooks/useTabs.ts`)
2. `frontend/src/lineage/LineageRouteContainer.tsx` (binds active tab to route param structure)
3. `frontend/src/lineage/detail/DetailDrawer.tsx` (wraps content in multi-tab layout shell)

**Unchanged**: `sectionRegistry.ts` and all interior section components. The tab controller handles wrapping; sections only demand a node view model.

> Note: V1.5.1 does **not** land multi-tab. This answer documents the V1.5.2 surface. The `useGraphKeyboard` singleton must also be guarded in V1.5.2 — design note already at `docs/superpowers/followups/v1.5.1-multi-tab-keyboard.md`.

### Q2. V1.5.2 adds AskAI with a real LLM backend. Which V1.5.0 files change?

**Affected files**:
1. `frontend/src/lineage/detail/sections/sectionRegistry.ts` (append `AskAISection` entry)
2. `frontend/src/lineage/detail/sections/AskAISection.tsx` **[NEW]** — SSE chat renderer

**Unchanged**: `DetailDrawer.tsx` auto-mounts via registry. The graph adapter is untouched — AI audits use a dedicated SSE chat endpoint, not the run-graph payload.

### Q3. V1.5.x backend adds `artifacts` and `coefficients` to the graph response. Which V1.5.0 files change?

**Affected files**:
1. `frontend/src/lineage/api/graphAdapter.ts` (map incoming JSON to already-declared optional view-model fields)
2. `frontend/src/lineage/detail/sections/sectionRegistry.ts` (append two entries)
3. `frontend/src/lineage/detail/sections/ArtifactSection.tsx` **[NEW]**
4. `frontend/src/lineage/detail/sections/CoefficientSection.tsx` **[NEW]**

**Unchanged**: `graphViewTypes.ts` already pre-declares `artifacts?: ArtifactRef[]` and `coefficients?: Array<{...}>`. Forward-compat slots in V1.5.0 were designed for exactly this case.

### Q4. V2.0 switches backend to Postgres + Pipeline/Run separation. Which V1.5.0 files change?

**Affected files**:
1. `frontend/src/lineage/api/graphAdapter.ts` (expose new `adaptPipelineGraph` alongside `adaptRunGraph`)
2. `frontend/src/lineage/LineageRouteContainer.tsx` (dispatch adapter by URL context)

**Unchanged**: Entire component catalog (`GraphCanvas`, `DetailDrawer`, etc.). They consume `GraphViewModel`, which is invariant to database backing or entity hierarchy.

### Q5. V2.0 splits physical UUID from logical key. Which V1.5.0 files change?

**Affected files**:
1. `frontend/src/lineage/api/graphAdapter.ts` (map UUIDs to `id`, logical keys to `nodeKey`)
2. `frontend/src/lineage/hooks/useSelectedNode.ts` (filter by `nodeKey` instead of physical `id`)

**Unchanged**: All components — V1.5.0 enforces identity comparisons via `refOf()` / `refEquals()` in `frontend/src/lineage/api/nodeRef.ts`. No raw `node.id` comparisons exist in high-level components.

### Q6. V1.6 adds a new `decision_id` (e.g., panel-FE Hausman). Which V1.5.0 files change?

**Affected files**:
1. `frontend/src/lineage/decisions/decisionRegistry.ts` (append the localised metadata template)

**Unchanged**: Components, adapter, and controllers. If an older frontend runs against a backend emitting an unmapped `decision_id`, the system gracefully falls back to `humanize()`.

---

## 4. Track 1 — SSE + Progress

### 4.1 Backend contract (already exists, no BE changes)

```
GET /runs/{run_id}/events?project_root=...
```

Emits SSE frames with the following event names and JSON body shape (`RunProgressEvent`):

| event | step | message | status | terminal? |
|---|---|---|---|---|
| `step_start` | string | "Stage <name> starting" | null | no |
| `step_complete` | string | "Stage <name> done" | null | no |
| `step_blocked` | string | "<reason>" | "blocked" | no |
| `workflow_completed` | null | "Workflow completed" | "completed" | yes |
| `workflow_blocked` | null | "Workflow blocked" | "blocked" | yes |
| `workflow_failed` | null | "Workflow failed: <err>" | "failed" | yes |
| `workflow_interrupted` | null | "Workflow interrupted" | "interrupted" | yes |

All shape facts asserted by `tests/test_event_manager.py` + `frontend/src/api.test.ts:257 connectRunEvents wires step events and terminal close`. **No backend changes needed for T1.**

### 4.2 Frontend changes

#### 4.2.1 `waitForRunTerminal` internals

External signature stays:
```ts
waitForRunTerminal(projectRoot, runId, { intervalMs?, maxMs?, onTick?, signal? })
  : Promise<RunDetail>
```

New behaviour:
1. If `typeof EventSource === "undefined"` → fall through to the V1.5.0 polling path unchanged.
2. Otherwise, open `connectRunEvents(...)`:
   - On every event, call `opts.onTick?.(detail)` — but `detail` must include the latest event payload. Extend `onTick` shape: `onTick?: (detail: RunDetail, lastEvent?: RunProgressEvent) => void`.
   - On terminal event, `await fetchRunDetail(...)` once and resolve with it.
   - On `EventSource.onerror` (network drop, server gone), close the SSE handle and fall through to polling for the remainder of the wait.
3. `maxMs` cap still applies (now defaults to **30 min** since SSE doesn't burn HTTP roundtrips). Polling-fallback path keeps the 5-min default.
4. `signal` aborts both SSE and any in-flight fetch.

#### 4.2.2 Submit page progress UI

In `App.tsx`, replace the static "Running workflow — waiting for completion" string with:

```
Running workflow…
└─ <step_name>: <message>     ← from latest step_start / step_complete
```

If three seconds pass without any event, show "Connecting to event stream…" to confirm the channel is live. If the run reaches a terminal status, the existing auto-nav to `/runs/<id>?tab=lineage` fires unchanged.

#### 4.2.3 Optional: lineage page banner during in-progress run

If a user enters `/runs/<id>` before the run terminates (rare but possible via deep-link), show a thin top banner: "Run in progress · current step: <name>". Source: the same `connectRunEvents` subscription, owned by `LineageRouteContainer` (cleaned up on unmount).

### 4.3 Files touched

| Path | Change |
|---|---|
| `frontend/src/api.ts` | `waitForRunTerminal` internals; extend `onTick` shape; new `_subscribeRun` helper composing `connectRunEvents` |
| `frontend/src/App.tsx` | Submit-region progress text bound to latest event; `onTick` callback wired |
| `frontend/src/lineage/LineageRouteContainer.tsx` | Optional in-progress banner subscription |
| `frontend/src/api.test.ts` | Update 4 existing `waitForRunTerminal` tests + add 3 new (EventSource happy path, EventSource error → polling fallback, terminal resolution) |
| `frontend/src/App.test.tsx` | Update Submit progress text expectations |

**No new files. No backend changes.**

### 4.4 Failure modes covered

| Scenario | Behaviour |
|---|---|
| `EventSource` undefined (jsdom, IE) | Polling path unchanged (V1.5.0 contract preserved) |
| SSE 404 / network drop mid-run | Close handle, fall through to polling for remaining time |
| Run already terminal at subscription | `fetchRunDetail` resolves immediately, SSE never opens |
| `signal.abort()` | SSE closed + fetch aborted + reject with `AbortError` |
| 30-min `maxMs` exceeded | Reject with descriptive `Error` (same as V1.5.0) |

### 4.5 DoD

- All 4 existing `api.test.ts` `waitForRunTerminal` tests still pass with new internals
- 3 new tests cover SSE happy path, fallback, terminal-at-subscribe
- Manual browser smoke: trigger `workbench run`, watch Submit page show "Running… step: regression" within < 1 s
- `App.test.tsx` Submit progress test asserts step text appears after `connectRunEvents` callback fires
- No regression in FE 333 baseline

---

## 5. Track 2 — Auto-select first review node

### 5.1 Behaviour

On `LineageRouteContainer` mount **only when `?node=` is absent**:

1. Iterate `viewModel.nodes` in the order they appear in the response.
2. First pass: pick first node with `trust === "review"`.
3. If none, second pass: pick first node with `trust === "caution"`.
4. If still none: leave selection empty (drawer shows V1.5.0 placeholder).
5. When picked, call `navigate(..., { replace: true })` with `?node=<nodeKey>` appended. **Never use `push`** — this would create a useless history entry that breaks the back button.

### 5.2 Why this order

The editorial prototype enters with a review-needed card already open (per `uiux/screenshots/initial.png`). `review` is the higher-attention state (hard block); `caution` is softer (advisory). Selecting `caution` only when no `review` exists matches "open the most pressing review first."

### 5.3 Files touched

| Path | Change |
|---|---|
| `frontend/src/lineage/hooks/useSelectedNode.ts` | New init effect that fills default node from view model when `?node=` is absent |
| `frontend/src/lineage/hooks/useSelectedNode.test.ts` | New tests: mixed-trust fixture → review wins; all-caution → first caution; all-ok → no selection; explicit `?node=` wins; back-button doesn't re-select |

### 5.4 DoD

- 5 unit tests pass
- Manual: fresh v3 smoke run shows drawer opens on the first regression decision card (assuming it's flagged review/caution)
- Back button after auto-select does **not** strand user on a blank URL — verified via test using `MemoryRouter` history inspection

---

## 6. Track 3 — LeftRail

### 6.1 Visual

Fixed 200 px sidebar mounted inside `/runs/<id>` route, left of `GraphWorkbench`. 8 rows, one per stage, in spec §19 order: `source · eda · clean · transform · model · diag · viz · report`.

Each row:

```
┌──────────────────────────┐
│ ●  Source            3 ·│   ← coloured dot (OKLCH stage token) · label · count · review-needed dot
│ ●  EDA               5  │
│ ●  Clean             2  │
│ ●  Transform         4 ·│
│ ●  Model             1 ·│
│ ●  Diag              3  │
│ ●  Viz               0  │
│ ●  Report            1  │
└──────────────────────────┘
```

Stage with `count === 0` is **disabled** (50% opacity, no click). The trailing `·` is a red 4 px dot (`var(--review-dot)`, OKLCH derived from `--trust-review`) when any node in that stage has `trust ∈ {review, caution}` or any decision has `reviewStatus === "needed"`.

Active state: the row corresponding to the selected node's stage gets `background: var(--rail-active-bg)` + left border `var(--<stage>)` 3 px.

### 6.2 Behaviour

- Click enabled row → `useReactFlow().fitView({nodes: stageNodes, padding: 0.2, duration: 400})` + `setSelected(stageNodes[0])` (which propagates to drawer + URL via existing `useSelectedNode`).
- Hover row → tooltip "<stage label> · <count> nodes · <n> review-needed".
- Keyboard: rail is `tabindex=0` per row; Enter activates same as click. (Arrow-key navigation between rows: V1.5.2.)

### 6.3 New component contract

```tsx
// frontend/src/lineage/leftRail/LeftRail.tsx
export function LeftRail(): JSX.Element {
  const { viewModel, selectedNode } = useLineage();
  const counts = useStageCounts(viewModel);   // { [stage]: { total, needsReview } }
  const rf = useReactFlow();
  // …
}
```

```tsx
// frontend/src/lineage/leftRail/useStageCounts.ts
export type StageCount = { total: number; needsReview: number; nodes: GraphViewNode[] };
export function useStageCounts(vm: GraphViewModel): Record<Stage, StageCount>;
```

`useStageCounts` is a `useMemo` over `vm.nodes` keyed by stage; pure function, no side effects, fully unit-testable.

### 6.4 Mount point

LeftRail mounts in `GraphWorkbench.tsx` layout, **not** in `LineageRouteContainer.tsx`:

```tsx
<div className="workbench-layout">
  <LeftRail />
  <GraphCanvas />
  <DetailDrawer />
</div>
```

CSS: `.workbench-layout` is a CSS Grid `grid-template-columns: 200px 1fr 360px` with `gap: 0`.

### 6.5 Tokens

New tokens in `lineage/tokens/lineage.css`:

```css
--rail-bg: oklch(0.14 0.01 240);
--rail-active-bg: oklch(0.18 0.02 240);
--rail-text: oklch(0.78 0.02 240);
--rail-text-muted: oklch(0.50 0.02 240);
--rail-border: oklch(0.20 0.01 240);
--review-dot: var(--trust-review);
```

### 6.6 Files touched

| Path | Change |
|---|---|
| `frontend/src/lineage/leftRail/LeftRail.tsx` | **NEW** |
| `frontend/src/lineage/leftRail/LeftRail.test.tsx` | **NEW** — 5 tests (renders 8 rows, count accuracy, disabled state, click → fitView mock, review dot visibility) |
| `frontend/src/lineage/leftRail/useStageCounts.ts` | **NEW** |
| `frontend/src/lineage/leftRail/useStageCounts.test.ts` | **NEW** — 4 tests (empty vm, all stages populated, partial stages, review-needed derivation) |
| `frontend/src/lineage/GraphWorkbench.tsx` | Add LeftRail to layout grid |
| `frontend/src/lineage/GraphWorkbench.test.tsx` | Update layout assertion |
| `frontend/src/lineage/tokens/lineage.css` | Add `--rail-*` tokens |
| `frontend/src/styles.css` | Add `--rail-*` mirror for route-shell scope |
| `docs/dev-browser-smoke.md` | Add L21–L23 (LeftRail visible, click focuses canvas, review dot lights up) |

### 6.7 DoD

- 9 new unit tests pass
- Browser smoke L21–L23 pass
- Click on disabled stage does nothing (no fitView, no console error)
- Selected node's stage row has active styling
- Visual: matches `uiux/screenshots/initial.png` left rail region within reasonable tolerance

---

## 7. Track 4 — Top breadcrumb

### 7.1 Visual

Fixed 56 px header below AppShell tabs, only on `/runs/<id>` route:

```
┌────────────────────────────────────────────────────────────────┐
│ wb-smoke  ›  Run · 2026-05-25-abc123    [Graph] Table  Pipeline │
└────────────────────────────────────────────────────────────────┘
   workspace      run pill                  tab switcher
```

- **Workspace**: `runDetail.project_root` basename. Click → no-op in V1.5.1 (future: workspace switcher).
- **Run pill**: shortened `run_id` (last 8 chars after final `-`). Click → no-op in V1.5.1 (future: run picker).
- **Tabs**: Graph is active (route-bound, currently the only thing rendered). Table & Pipeline are `<button aria-disabled="true" title="Coming in V2.0">` with `opacity: 0.4` and `cursor: not-allowed`.

### 7.2 Why disabled tabs at all

Two reasons:
1. **Layout commitment** — the prototype puts tabs here; building the empty slot now means V2.0 can drop in real components without re-flowing the header.
2. **User expectation signal** — labelling Table/Pipeline as "Coming in V2.0" telegraphs the roadmap without requiring a separate announcement.

### 7.3 Mount point

`Topbar.tsx` mounts in `App.tsx` inside the `/runs/:id` route guard, **above** `LineageRouteContainer`:

```tsx
<Route path="/runs/:runId" element={
  <>
    <Topbar />
    <LineageRouteContainer />
  </>
} />
```

This keeps the topbar route-scoped (Submit `/` and History `/runs` stay V1.4 light shell).

### 7.4 Files touched

| Path | Change |
|---|---|
| `frontend/src/shell/Topbar.tsx` | **NEW** |
| `frontend/src/shell/Topbar.test.tsx` | **NEW** — 5 tests (workspace label, run pill format, Graph active, Table/Pipeline disabled, tooltip presence) |
| `frontend/src/App.tsx` | Wrap `/runs/:runId` route with Topbar |
| `frontend/src/App.test.tsx` | Add route-binding test (Topbar visible on `/runs/x`, hidden on `/` and `/runs`) |
| `frontend/src/styles.css` | `.workbench-topbar` + token mirror |
| `docs/dev-browser-smoke.md` | Add L24–L26 (Topbar visible on /runs/:id, disabled tabs have tooltip, /runs and / lack Topbar) |

### 7.5 Workspace derivation rules

- If `runDetail.project_root` is `/tmp/wb-smoke` → label "wb-smoke"
- If it's `/Users/.../项目规划/.worktrees/workbench-v1.5.0` → label "workbench-v1.5.0"
- If basename is empty (e.g., `/`) → label "(workspace)" placeholder
- If `runDetail` not yet loaded → render skeleton (gray pulse, same height)

### 7.6 DoD

- 6 new unit tests pass
- Browser smoke L24–L26 pass
- Topbar present on `/runs/<id>`, absent on `/` and `/runs` (verified by App.test.tsx route assertions)
- Hovering disabled tab shows tooltip; clicking does nothing (no nav, no console error)

---

## 8. Track 5 — Polish bundle

### 8.1 `lastRun` persistence

**Problem**: In V1.5.0, `App.tsx` holds `lastRun` in `useState`. Navigating away (e.g., `/` → `/runs/x` → back to `/`) wipes the "your last run was…" hint card.

**Fix**: Move `lastRun` into a new `AppShellContext` (or reuse the existing one if it's already a context — check during impl), backed by `sessionStorage["workbench:lastRun"]`. Restored on mount, cleared on explicit user dismissal only.

**Files**:
- `frontend/src/AppShell.tsx` (or new `frontend/src/AppShellContext.tsx` if missing)
- `frontend/src/App.tsx` (read from context instead of local state)
- `frontend/src/App.test.tsx` (test: trigger run → navigate away → back → hint card still visible)

### 8.2 `/runs` history list dark chrome

**Problem**: `/runs/<id>` is dark (V1.5.0 P1), but `/runs` history list is still V1.4 light. Jumping from lineage to history is visually jarring.

**Fix**: Extend the `workbench-shell--lineage` modifier (currently route-pattern `/^\/runs\/[^/?#]+$/`) to also match `/^\/runs(?:\/[^/?#]+)?$/`. Update OKLCH token mirrors as needed.

**Files**:
- `frontend/src/App.tsx` (regex update + code comment explanation)
- `frontend/src/App.test.tsx` (assert dark class on `/runs` route)
- `frontend/src/styles.css` (verify no breaking selectors)

### 8.3 `WORKBENCH_UI_ORIGIN` fallback

**Problem**: CLI prints `Lineage: http://localhost:5173/...` by default. If user runs CLI without `npm run dev`, the link is dead.

**Fix**: When env var unset, CLI tries to detect whether `:5173` is live (quick TCP probe with 100 ms timeout). If not, fall back to `http://localhost:8000` (BE serves the static SPA from `frontend/dist/` in production; ditto if user is running `uvicorn` alone).

**Files**:
- `backend/workbench/cli.py` (new `_resolve_ui_origin()` helper)
- `tests/test_cli.py` (3 tests: env var set wins, :5173 live wins, fallback to :8000)

**Risk**: TCP probe adds ~50–200 ms to CLI start. Mitigation: cache result in `~/.workbench/ui_origin_cache.json` with 1 h TTL.

### 8.4 DoD (Track 5 combined)

- All new tests pass (~6 added)
- Browser smoke L27–L29 (lastRun persistence, /runs dark, CLI lineage URL valid)
- No regression in baseline counts

---

## 9. File change matrix — consolidated

| Path | Track | Change type |
|---|---|---|
| `frontend/src/api.ts` | T1 | edit (`waitForRunTerminal` internals) |
| `frontend/src/api.test.ts` | T1 | edit (4 updated + 3 new) |
| `frontend/src/App.tsx` | T1, T5.1, T5.2 | edit (progress text, context, route regex) |
| `frontend/src/App.test.tsx` | T1, T4, T5.1, T5.2 | edit (multiple new assertions) |
| `frontend/src/AppShellContext.tsx` | T5.1 | new (if missing) |
| `frontend/src/lineage/LineageRouteContainer.tsx` | T1 | edit (in-progress banner) |
| `frontend/src/lineage/hooks/useSelectedNode.ts` | T2 | edit (default-node effect) |
| `frontend/src/lineage/hooks/useSelectedNode.test.ts` | T2 | edit (5 new) |
| `frontend/src/lineage/leftRail/LeftRail.tsx` | T3 | new |
| `frontend/src/lineage/leftRail/LeftRail.test.tsx` | T3 | new (5 tests) |
| `frontend/src/lineage/leftRail/useStageCounts.ts` | T3 | new |
| `frontend/src/lineage/leftRail/useStageCounts.test.ts` | T3 | new (4 tests) |
| `frontend/src/lineage/GraphWorkbench.tsx` | T3 | edit (layout) |
| `frontend/src/lineage/GraphWorkbench.test.tsx` | T3 | edit |
| `frontend/src/lineage/tokens/lineage.css` | T3 | edit (rail tokens) |
| `frontend/src/styles.css` | T3, T4, T5.2 | edit (mirrors + topbar styles + `/runs` dark) |
| `frontend/src/shell/Topbar.tsx` | T4 | new |
| `frontend/src/shell/Topbar.test.tsx` | T4 | new (5 tests) |
| `backend/workbench/cli.py` | T5.3 | edit (UI origin resolver) |
| `tests/test_cli.py` | T5.3 | edit (3 new) |
| `docs/dev-browser-smoke.md` | T1, T3, T4, T5 | edit (L21–L29) |

**No changes to**: `graphAdapter.ts`, `graphViewTypes.ts`, `sectionRegistry.ts`, `LineageContext.tsx`, `nodeRef.ts`, `pathBuilder.ts`, `decisionRegistry.ts`, `GraphNode.tsx`, `GraphCanvas.tsx`, `GraphTooltip.tsx`, `DetailDrawer.tsx`, any section component, any backend graph/orchestrator code.

The V1.5.0 boundaries hold.

---

## 10. Test matrix

| Layer | V1.5.0 baseline | V1.5.1 added | V1.5.1 target |
|---|---|---|---|
| FE unit | 333 | +21 (T1: 3, T2: 5, T3: 9, T4: 5, T5: 1 lastRun) | **354** |
| BE unit | 618 | +3 (T5.3 only) | **621** |
| Browser smoke | L1–L20 (V1.5.0) | L21–L29 (V1.5.1) | **L1–L29** |

New browser smoke items:

- **L21**: LeftRail renders 8 rows in stage order with correct counts
- **L22**: Clicking a stage row fitViews the canvas to that stage's bbox
- **L23**: Stage row shows red dot when any node in stage is review/caution
- **L24**: Topbar visible on `/runs/<id>`, hidden on `/` and `/runs`
- **L25**: Topbar Table/Pipeline tabs show "Coming in V2.0" tooltip on hover
- **L26**: Topbar workspace label matches project_root basename
- **L27**: `lastRun` hint card survives navigation (`/` → `/runs/x` → `/`)
- **L28**: `/runs` history list adopts dark chrome (matches `/runs/<id>` palette)
- **L29**: Submit page shows live "Step: <name>" within 1 s of `workbench run`

---

## 11. Acceptance gates per track

Per memory `feedback_review_workflow.md` (three-level review):

For each Track:
1. **Implementer**: Code complete, all tests in track green locally, commit with descriptive message
2. **Test & QA**: Run full FE + BE suites, browser smoke if visual, verify DoD checklist items
3. **Reviewer**: Read diff, check for hidden coupling, run smoke independently, sign off
4. **Push**: Only after reviewer signs off

Tracks ship sequentially. Each push gates the next track's start.

---

## 12. Vulnerability mitigation map

From V1.5.0 handoff §3:

| Vulnerability | V1.5.1 action | Closed? |
|---|---|---|
| #1 Polling 5-min cap | T1 SSE upgrade | ✅ closed |
| #2 Visual overlap (badge × group nodes on narrow viewport) | Not addressed | ❌ deferred to V1.5.2 |
| #3 `useGraphKeyboard` singleton | Not addressed (no multi-tab in V1.5.1) | ❌ deferred to V1.5.2 (tied to multi-tab) |
| #4 React Flow v11 class lock | Not addressed | ❌ deferred to V1.5.2 |

Additional vulnerabilities surfaced **in** V1.5.1:

| New risk | Mitigation |
|---|---|
| LeftRail click → `fitView` requires React Flow instance; if mounted before RF ready, click is no-op | `useReactFlow()` returns null until ready; LeftRail rows are disabled until RF instance available |
| Topbar workspace label could expose `/Users/<name>/…` paths to screenshots | Use basename only, never full path |
| `_resolve_ui_origin` TCP probe could hang CLI if `:5173` is firewalled (not refused) | Hard 100 ms timeout + cached result |

---

## 13. Open questions for V1.5.2

These do **not** block V1.5.1 sign-off, but should be answered before V1.5.2 planning:

1. Multi-tab DetailDrawer keyboard arbitration — design note in `docs/superpowers/followups/v1.5.1-multi-tab-keyboard.md` proposes a context-based focus controller. Validate before T-X.1.
2. React Flow v11 class abstraction strategy — keep RF Controls or replace with custom HTML buttons?
3. AskAI SSE endpoint contract — Anthropic API direct vs backend proxy with audit log?
4. Real-world dataset performance — what's the right large-dataset fixture? Production panel data export, or synthetic 1000-node graph?

---

## 14. Sign-off

V1.5.1 closes the largest user-visible gap between V1.5.0 and the design prototype (outer shell), eliminates the run-progress polling timeout vulnerability, and lands a small UX polish bundle. It does **not** touch the V1.5.0 boundaries (adapter, view model, context, section registry, graph components), so the forward-compatibility contracts established in V1.5.0 remain intact.

V1.5.1 is **3 weeks of work** by the V1.5.0 ship-readiness baseline. Plan doc: [`2026-05-25-v1.5.1-visual-alignment-plan.md`](../plans/2026-05-25-v1.5.1-visual-alignment-plan.md).
