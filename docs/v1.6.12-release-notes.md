# V1.6.12 — AI 收尾与 legacy `/graph` 兼容

**Theme:** Finish the AI/report interaction loop, make the typed activity record usable as the v1.7 foundation, and keep legacy project graph loading responsive.

**Branch:** `workbench-v1.6.12` (base: `main` at v1.6.11)
**Gate:** Backend `1389 passed, 8 skipped` · golden/invariants `23 passed (0-drift)` · Frontend `1014 passed` · `tsc --noEmit` 0 errors · `./scripts/gate.sh` → `GATE PASSED`

## What shipped

### AI/report closeout

- **AI activity record and timeline:** Ask AI and report-generation interactions use the typed local activity log and appear in the bottom-panel AI activity view. Stale placeholder tabs were removed.
- **Ask AI history and Markdown:** Per-node Ask AI history keeps previous answers, and Ask AI uses the shared report Markdown renderer with citation-chip support.
- **Artifact Explain:** Each artifact row can open Ask AI with an artifact-focused context packet.
- **LLM configuration visibility:** The read-only configuration endpoint and provider badge expose base URL/model/key presence without returning the API key.
- **Canvas reset and Home integration:** Reset restores the automatic graph layout; the bare Home route returns to the latest project while `?home=1` opens the launcher.

### Legacy `/graph` compatibility

The old sample project at `/Users/jiayuanren/Desktop/安全边际筛选` contains pipeline run directories but no Workbench `run_manifest.json`, `graph.json`, `run_inputs.json`, or `node_index.json`. The project forest now reaches the response without importing Matplotlib during app startup; manifest-less legacy directories are skipped and the endpoint returns an empty forest instead of hanging on plotting/font initialization.

Measured smoke result on 2026-07-12: `GET /graph?project_root=/Users/jiayuanren/Desktop/安全边际筛选` → HTTP `200`, `0.014s`, `nodes=0`, `heads=0`.

### Deep-link behavior

`/runs/:id?project_root=...` intentionally resolves into the project forest and focuses the target run rather than showing only that run's family headset. This is the v1.6.8 unified-graph behavior, not a regression.

## Backward compatibility

The per-run graph contract and project-forest response shape remain unchanged. Plotting is still imported and used when a real run reaches diagnostics; only application-startup import timing changed. Golden snapshots and existing visualization/orchestrator seams remain protected by tests.

## Deferred

- v1.7 Agent Harness: propose → confirm → execute → typed operation record, diff history, and sandbox.
- V7 drill-down and V11 data-layer editing.
- Non-blocking backlog items and the independent statistical-method roadmap.
