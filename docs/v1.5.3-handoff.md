# V1.5.3 交接文档（地基版，全部完成）

> 交接时间：2026-05-31（初始），2026-06-01（F2/F3），2026-06-02（F5/F6/F7 收官）
> 分支：`workbench-v1.5.3`（基于 `workbench-v1.5.2`），已 push 到 origin，与远端同步
> Worktree：`.worktrees/workbench-v1.5.3`，**有独立 `.venv`（python3.11）**
> 当前 HEAD：`2b31a73`（F7）

---

## 0. 一句话状态

V1.5.3 计划 9 项（F1–F9）**全部完成并 push、测试全绿**。详见 `docs/v1.5.3-release-notes.md`。

测试基线（独立 venv + npm ci 后）：
- 后端：`./.venv/bin/python -m pytest -q` → **621 passed**
- 前端：`cd frontend && npx vitest run` → **549 passed**

下文 §2/§B 的 F2/F3 内容保留为方法论与历史记录；F5/F6/F7 已按相同纪律完成（结构化 keys、复用 F4 guard、disabled 双层防护、关键逻辑变异测试验证）。

---

## 1. 已完成（已 commit + push）

| 项 | commit | 内容 | 测试 |
|---|---|---|---|
| **F9** 独立 venv | `5a27d81`（说明合并在 F8 commit body） | `.worktrees/workbench-v1.5.3/.venv` 用 python3.11 重建，不再共用 v1.5.1 venv | BE 621 在新 venv 通过 |
| **F8** sessionStorage 命名空间统一 | `5a27d81` | `GraphView.tsx`：`lineage:legacy-hint-dismissed:<runId>` → `workbench:`，带一次性迁移读取（读旧 key→写新 key→删旧 key）。审计确认全 codebase 仅此一处用 `lineage:` 前缀，其余（layout/theme/splitter/viewport/lastRun）早已是 `workbench:` | `GraphWorkbench.test.tsx` +1 迁移测试，14 passed |
| **F4** ⌘K focus guard | `8bae1e6` | 新建 `workbench/keyboard.ts` 的 `isEditableTarget()`（input/textarea/select/contenteditable，jsdom 兼容用 `closest("[contenteditable]")` 读属性而非 `isContentEditable`）。`SearchPalette.tsx` 在 palette 未开且焦点在可编辑元素时不抢 ⌘K；palette 已开则仍允许 ⌘K 关闭 | 新建 `keyboard.test.ts`（7）+ SearchPalette +2 |
| **F1** setFocusOnly | `c627e33` + `1d741df`（topbar 补漏） + `cfb441d`（测试） | Provider 新增 `setFocusOnly(nodeKey)`：只写 `focusKey + pinned=0`，不动 tabs/active/selected。三处 action surface（ContextMenu、WorkbenchTopbar、NodeActionMenu）的 `focusUpstream` 全部从 `selectByCanvasClick` 改为 `setFocusOnly` | Provider +3 transition 测试，24 passed |
| **F2 + F3** 双 writer 消除 | `4894518` + `e3189ca` + `66a4028` + `66d0bdb` + `55571b8`（5 commits，2026-06-01 完成） | 彻底消除双 URL writer 架构：① `TabState` 类型迁至 `tabsSchema.ts`；② 新建 `LineageBridge` 组件，从 `useWorkbench()` 读取 state/dispatch 构造 `LineageContextValue`，取代 `useSelectedNode`/`useTabs` 作为选择源；③ 新增 8 个 lock-in 测试锁定 `selectedKey` 唯一源；④ 将 review/caution 首屏 auto-select 逻辑迁入 `LineageBridge`；⑤ 删除 `useTabs.ts/.test.tsx` + `useSelectedNode.ts/.test.tsx`（618 行代码/测试）。现在 `WorkbenchStateProvider.commit()` 是**唯一的 URL writer** | BE 621 passed，FE 515 passed（44 files，含 +8 lock-in 测试，删旧 hook 24 个测试）|

### ⚠️ F1 的一个教训（已修复，但要知道）
F1 第一次提交（`c627e33`）的 commit message **声称**带了 3 个测试 + 改了 WorkbenchTopbar，但因为工具调用被取消，实际：
- WorkbenchTopbar 的 wiring **漏了** → 用 `1d741df` 补上；
- 3 个测试**没进** → 用 `cfb441d` 补上。

现在 F1 三件套（code + wiring + tests）都齐了、已验证。**对接时不要再被那条 overclaim 的 commit message 误导。**

---

## 2. ✅ 已完成：F2 / F3（双 writer 消除）—— 2026-06-01

> **状态：已完成。** 以下保留原始分析文档供参考，实际实现细节见 commit history。

### 计划怎么写的（错的）
`docs/v1.5.3-plan.md` §2-F2 说："`useTabs()` 只有一个消费方 `useSelectedNode`，而 `useSelectedNode` 零生产消费方，都是 V1.5.0/1.5.1 死代码，直接删。"

### 真实代码（读出来的，推翻计划）
- `useSelectedNode` **是生产代码**，在 `WorkbenchRouteContainer.tsx:67` 被调用：
  ```
  const { selectedKey, select, tabs, activeTabId, setActiveTab, closeTab, lastEvictedTabId }
    = useSelectedNode(model?.nodes ?? null, model?.runId ?? null);
  ```
- 它内部用 `useTabs()`，而 `useTabs` 有**自己的 `setParams` writer**（`writeUrl`），写 `tabs`/`active`。
- 这个 `useSelectedNode` 调用发生在 `WorkbenchStateProvider`（`WorkbenchRouteContainer.tsx:105` 才 mount）**之外**；LineageContext 的 `{selectedKey, select, tabs...}`（line 106）全部来自 `useSelectedNode`，**不是** Provider。

### 结论：§3-① 是**真实存在的活 bug**，不是理论风险
production 里**确实有两个 URL writer 同时写 `tabs`/`active`**：
1. `useTabs.writeUrl`（经 useSelectedNode，喂 LineageContext）
2. `WorkbenchStateProvider.commit()`（经 dispatch.*）

它们同 tick 触发会互相覆盖。**F2 和 F3 因此是同一次重构**，不是两件事：
> 把选择源（selectedKey/select/tabs/activeTabId/closeTab）从 `useSelectedNode` 搬进 `WorkbenchStateProvider`，让 LineageContext 读 Provider，删掉 `useTabs` + `useSelectedNode`。

### 对接时必须保留的两个行为细节（坑）
1. **auto-select**：`useSelectedNode` 有首屏自动选中逻辑——`nodes` 加载后，若当前无选中，自动 openTab 第一个 `trust==="review"`（其次 `"caution"`）的节点。`useSelectedNode.test.tsx`（15 个测试）覆盖它。重构后必须等价保留。
2. **focus 语义差异**：旧 `useTabs.openTab(key)` 只设 tabs/active，**不碰 focus**；而 Provider 的 `selectByCanvasClick(key)` 在 `!pinned` 时**会顺带设 focus=key**。所以把 auto-select / select() 直接换成 `selectByCanvasClick` 会**改变 focus 行为**。需要确认期望语义：要么接受 focus 跟随，要么给 Provider 加一个"只开 tab 不设 focus"的路径。

### 受影响的测试（blast radius）
mount 容器或直接测这些 hook 的文件：
- `frontend/src/workbench/WorkbenchRouteContainer.test.tsx`（容器集成）
- `frontend/src/lineage/hooks/useSelectedNode.test.tsx`（15，删 hook 后要迁移到容器级或 Provider 级）
- `frontend/src/lineage/hooks/useTabs.test.tsx`（删 hook 后处理）
- `frontend/src/lineage/detail/DetailDrawerTabs.test.tsx`、`DetailDrawer.test.tsx`（只 import `TabState` 类型）
- `frontend/src/workbench/state/tabsSchema.test.ts`、`urlSchema.test.ts`（只 import `TabState` 类型，且注释里提到 useTabs）

### TabState 类型迁移（F2 的子任务）
`TabState` 当前定义在 `lineage/hooks/useTabs.ts`，被 6 处 import。删 useTabs 前，先把 `TabState` 定义搬到 `workbench/state/tabsSchema.ts`（它已是 tab parse/write/reduce 的纯 reducer，是类型的自然归属），再把 6 处 import 重定向：
- `workbench/state/tabsSchema.ts`（自身定义）
- `lineage/LineageContext.tsx`
- `lineage/detail/DetailDrawer.tsx`、`DetailDrawerTabs.tsx`、`DetailDrawerTabs.test.tsx`、`DetailDrawer.test.tsx`
- `workbench/WorkbenchStateProvider.tsx`

### 建议的安全步骤（给对接工程师）
1. 先把 `TabState` 搬到 tabsSchema + 重定向 import（纯类型移动，零行为变化），跑全量绿，单独 commit。
2. 在 Provider 里实现/确认 selected 单一源（F3），并加 lock-in 测试：selectedKey 严格 = active tab 的 nodeKey；无 active 时为 null；focus-only dispatch 不动 selected。
3. 在容器里加一个 bridge：LineageContext 的 `{selectedKey, select, tabs, activeTabId, setActiveTab, closeTab, lastEvictedTabId}` 全部从 `useWorkbench()` 派生；`select(null)` 对齐现有 deselect = closeTab(active) 语义。
4. 把 auto-select 逻辑搬到容器（消费 Provider dispatch），保留 review/caution 首屏选中 + focus 语义决策。
5. 删 `useTabs.ts/.test.tsx` + `useSelectedNode.ts/.test.tsx`，迁移其测试断言。
6. 每步全量 `cd frontend && npx vitest run`，绿了再下一步。F1→F2→F3 顺序里 F1 已完成，可直接做。

---

## 3. 未开工：F5 / F6 / F7（低风险加性项，可独立于 F2/F3）

这三项**不依赖** F2/F3，可以先做（如果想让本版有更多产出而暂不碰核心重构）。

| 项 | 规格出处 | 关键点 |
|---|---|---|
| **F5** search cursor 专属高亮 | plan §2-F5 | 新增 Tier3 `searchCursorKey`，SearchPalette 在 cursor 变化时 `dispatch.setSearchCursorKey(results[cursor]?.nodeKey)`；GraphView 透给 GraphCanvas 作第 6 级高亮（盖过普通命中）。GraphCanvas 加 `searchCursorKey?` prop + 样式 token。注意 F4 已经把 `dispatch.setSearchCursor(index)` 的 effect 改过，cursor 已在 Tier3，但只有 index 没有 nodeKey |
| **F6** shortcut 全局 dispatcher | plan §2-F6 | actionRegistry 的 `shortcut` 字段当前只显示。需先定**结构化键字段**（`keys: {key, meta?, ctrl?, shift?}`）再接全局 dispatcher，复用 F4 的 `isEditableTarget` guard，disabled action 不触发。评审键位与现有 ⌘J/⌘K/Escape 冲突 |
| **F7** Command Palette | plan §2-F7 | surface `command-palette` 已留，无组件。复用 SearchPalette 的 portal+↑↓+Enter 模式，但列 action（`actionsForSurface("command-palette", ctx)`）。依赖 F6。时间紧可降级为最小可用面板 |

---

## 4. 环境与命令速查

```bash
# 进 worktree
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.5.3

# 后端测试（用本 worktree 独立 venv）
./.venv/bin/python -m pytest -q                       # 621 passed

# 前端测试（必须先 cd frontend，否则 vitest cwd 漂移）
cd frontend && npx vitest run                          # 515 passed（F2/F3 后）
cd frontend && npx vitest run src/workbench/WorkbenchStateProvider.test.tsx  # 单文件
```

> ⚠️ **本项目没有 tsconfig.json，也没有独立 tsc 类型检查步骤。**
> TypeScript 仅作为 vite/esbuild 的转译依赖；类型错误不被任何 CI 捕获。
> **唯一真实的验证门槛就是 `npx vitest run`**（用 esbuild 转译，跑通即可）。
> 不要尝试 `tsc --noEmit -p .` —— 会因找不到 tsconfig 而报错，那不是你引入的 bug。
> （此前文档/commit message 里出现的 "tsc clean" 是错误说法，已在此更正。）

### 环境坑（已踩）
- 系统默认 `python3` 是 3.14（在 `/opt/homebrew/bin`），但科学计算包在 3.14 上 wheel 不全。**用 `~/.local/bin/python3.11`** 建 venv。
- `git worktree list` 里 v1.5.1 的 venv 是 3.14（能跑是因为包恰好有 3.14 wheel），本 worktree 用 3.11 独立 venv。
- vitest 跑到一半 cwd 会漂移，**每次显式 `cd frontend`**。

---

## 5. 流程教训（给所有对接者，也是给我自己）

1. **不要过度并行批处理工具调用**：我两次在一个回合里塞了几十个有依赖关系的调用（读→改→测→提交混在一起），结果中间一个失败触发整批 cancellation，造成 commit message 与实际落地不符（F1 的 wiring 和测试都掉了，事后才补）。**改代码—测试—提交要分回合、串行确认。**
2. **commit message 不能 overclaim**：F1 那条 message 写了"524 passed""改了三处 wiring""3 个测试"，但其中两件没真正落地。对接时**以代码和 `git show` 为准，不信 message**。
3. **Read 跨 worktree 副本会失配**：本机有 ~25 个 worktree，路径相似。编辑前确认 Read 的是 `workbench-v1.5.3` 那份。

---

## 6. 一个 untracked 文件

`docs/v1.5.3-project-structure-framework.md` 是另一进程生成的结构盘点（只覆盖到 F1/F4/F8，即我被打断时的状态），未 commit。对接时可参考或删除，不影响代码。

---

# 附录 A：五问盘点

## A.1 已实现 / 失败 / 遇到困难

### ✅ 已实现（已 push + 测试绿）
- **F9 独立 venv**：worktree 自带 `.venv`（python3.11），BE 621 通过，不再共用 v1.5.1 的 venv。
- **F8 sessionStorage 命名空间统一**：唯一残留的 `lineage:` key 迁移到 `workbench:`，带一次性迁移（读旧→写新→删旧），用户已 dismiss 的提示不会复活。
- **F4 ⌘K focus guard**：新增 `workbench/keyboard.ts` 的 `isEditableTarget()`，抽屉里打字按 ⌘K 不再被吞。这是个**可复用基础件**，F6/F7 都要用它。
- **F1 setFocusOnly**：真正实现了"看 A、只高亮 B 上游、不动 selected"。三处 surface 全部改用它。

### ❌ 失败 / 主动叫停
- **F2/F3 叫停**：不是技术失败，是**发现计划写错了**。计划假设 useTabs 是死代码可直删；真实代码里它是**活的生产选择源**（见 §2）。这是一次真正的双-writer 架构重构，决定交给对接工程师专门做，而不是在剩余预算里仓促硬上。

### ⚠️ 遇到的困难（已解决）
1. **python 版本陷阱**：系统默认 `python3` 是 3.14，科学计算包 wheel 不全，`/opt/homebrew/bin/python3.11` 不存在（真实路径 `~/.local/bin/python3.11`）。首次建 venv 因此 exit 127 失败，且失败后 BE 测试"假绿"（命令根本没跑起来）。**教训：建完 venv 必须实跑一次 `pytest` 确认非空输出。**
2. **过度并行批处理导致 cancellation 级联**：我两次在一个回合塞了几十个有依赖关系的工具调用，中间一个失败触发整批取消，造成 F1 的 wiring 和测试"以为提交了其实没有"。已用 `1d741df`/`cfb441d` 补齐。
3. **跨 worktree Read 失配**：本机 ~25 个相似 worktree，多次 Read 到 v1.5.2 副本再 Edit v1.5.3 导致 old_string 不匹配。**教训：编辑前确认 Read 的绝对路径含 `workbench-v1.5.3`。**

## A.2 后期维护需重点关注的关键组件

| 组件 | 路径 | 为何关键 |
|---|---|---|
| **`WorkbenchStateProvider`** | `workbench/WorkbenchStateProvider.tsx` | 跨视图状态中枢。`commit()` 是**唯一正确**的 URL writer。§7 transition 表是加新交互的契约，新 dispatch 必须配套加 transition 测试 |
| **`commit()` 内部函数** | 同上，~206 行 | 唯一 URL writer。任何想写 URL 的代码都必须经 dispatch；绕过它 = 复活 setParams 覆盖 bug |
| **`useSelectedNode` + `useTabs`** | `lineage/hooks/` | ⚠️ **当前最危险的活代码**：第二个 URL writer，喂 LineageContext。F2/F3 的全部目标就是消灭它（见 §2）。**删除前不要在它上面加任何新功能** |
| **`WorkbenchRouteContainer`** | `workbench/WorkbenchRouteContainer.tsx` | 两个 Provider 的挂载点 + LineageContext 桥接。F2/F3 重构的主战场（line 67 / 105 / 106）|
| **`tabsSchema.ts`** | `workbench/state/` | 纯 reducer。`TabState` 类型即将迁到这里。改动同时影响 Provider 和（待删的）useTabs |
| **`workbench/keyboard.ts`** | F4 新建 | `isEditableTarget()` 是 F6/F7 全局键盘监听的共享 guard。改它会影响所有键盘 surface |
| **三个 Registry** | `workbench/registry/` | 加 section/action/panel 都走这里，**永远不要**在组件里硬编码绕过 |

## A.3 目前最脆弱的部分

| 脆弱点 | 风险 | 状态 |
|---|---|---|
| **双 URL writer（useTabs vs commit）** | 同 tick 触发会互相覆盖 `tabs`/`active`。这是 §3-① 描述的 bug，**确认为生产活 bug，不是理论** | F2/F3 待修，**未修复前是头号风险** |
| **F1 已修 focusUpstream 语义** | 已从 `selectByCanvasClick` 改为 `setFocusOnly`，但 `selectByCanvasClick` 在 `!pinned` 时仍会顺带设 focus —— F2 把 auto-select 接到 Provider 时会再次踩到这个语义 | 见 §2 坑 #2 |
| **auto-select 跨 hook** | review/caution 首屏选中逻辑藏在 `useSelectedNode`，删 hook 时极易丢失 | 见 §2 坑 #1 |
| **shortcut 字段是显示文案** | `actionRegistry.shortcut` 是给人看的字符串，没有结构化键定义 | F6 要先补结构化字段才能接 dispatcher |
| **没跑过真浏览器 smoke** | 单测全绿不代表 React Flow 渲染 / Portal 层叠 / SSE 正常 | 本版结束前应做一次，参考 `docs/dev-browser-smoke.md` |

## A.4 可未来升级的部分

- **F5/F6/F7**（本版剩余）：cursor 专属高亮、快捷键 dispatcher、命令面板。低风险加性项，**不依赖 F2/F3**，可先做。
- **V1.5.4 AI 版**：`/llm/chat` 后端从零建 + AskAISection 接真接口（当前是 disabled 占位）。
- **V1.5.5 编辑+重跑版**：partial rerun 后端 + Editable Ops + Monaco 编辑器。
- 三版蓝图详见 `docs/v1.5.3-plan.md` 上游对话与本仓库 release notes。

## A.5 是否需要更多信息 / 补充

对接工程师建议先确认这几点（我不确定的）：
1. **focus 语义决策**（§2 坑 #2）：auto-select / 普通 select 时，focus 是否应该跟随 selected？这是产品语义问题，需要 owner 拍板，不是纯技术决定。
2. **真实大图性能**：所有测试用合成 fixture，>100 节点的真实 run 在 6 级高亮 + focus overlay 下的渲染性能未验证。
3. **F6 键位表**：哪些 action 该有快捷键、用什么键（避免与 ⌘J/⌘K/Escape 冲突），需要产品确认后再实现。

---

# 附录 B：给 Junior 工程师的反脆弱性开发指导

> 目标：即使你不熟悉这个 codebase，按下面的"门槛"逐关通过，也不会把现有功能改坏。
> **核心原则：每一步都必须能用一条命令证明"没坏"，证明不了就不准进下一步。**

## B.0 开工前必做（环境自检，5 分钟）

```bash
cd /Users/jiayuanren/项目规划/.worktrees/workbench-v1.5.3
./.venv/bin/python -m pytest -q          # 必须看到 "621 passed"，不是空输出
cd frontend && npx vitest run            # 必须看到 "515 passed"（F2/F3 后基线）
```
**这两条全绿 = 你的起跑线。** 之后任何一步让其中任何一条变红，就是你刚引入的回归，立刻停下回滚，不要继续往前堆。

> ⚠️ 本项目**没有独立类型检查**（无 tsconfig.json）。不要把 `tsc` 当门槛——它跑不起来。`vitest run` 用 esbuild 转译，是唯一真实门槛。

### 黄金规则（违反任何一条都会重蹈我的覆辙）
1. **一次只改一件事，改完立刻跑测试，绿了立刻 commit。** 不要攒着一起测。
2. **commit message 只写你亲眼验证过的事实。** 写"531 passed"前，必须真的看到 531 passed 的输出。
3. **Edit 前先 Read 同一条绝对路径**，确认路径含 `workbench-v1.5.3`。
4. **不要在一个回合里发几十个有依赖关系的工具调用**（这是我翻车的直接原因）。读→改→测→提交分开做。
5. **绝不为了"让测试通过"去删断言或改期望值**，除非你能解释清楚为什么旧断言本身是错的。

---

## B.1 F2/F3（双 writer 重构）—— 高风险，按关卡走

> 这是本版最难的部分。**不要试图一次性重写**。按下面 6 关，每关一个 commit，每关都先绿再进。

### 困难点（先理解，再动手）
- **D1 两个选择源并存**：`useSelectedNode`（line 67，自带 setParams）和 `WorkbenchStateProvider`（line 105，commit 写 URL）都在写 `tabs`/`active`。目标是只留 Provider。
- **D2 auto-select 会丢**：首屏自动选中 review/caution 节点的逻辑在 `useSelectedNode.ts:48-61`。删 hook 时必须搬走，否则首屏行为变了但**可能没有测试报警**（先确认有没有覆盖测试）。
- **D3 focus 语义差异**：旧 `openTab` 不设 focus，新 `selectByCanvasClick` 在 !pinned 时设 focus。直接替换会让"选中即聚焦"，可能不是期望行为 —— **开工前找 owner 确认（见 A.5 #1）**。
- **D4 LineageContext 是公共契约**：很多组件读 `useLineage().selectedKey/select`。桥接必须保持这个接口形状不变，否则连锁报错。

### ✅ 这套 Gate 已实际执行完成（2026-06-01）

> **F2/F3 已经按下面的 Gate 顺序做完了**，commit 链印证：
> `4894518`(step0 TabState 搬家) → `e3189ca`(F3 source LineageContext) →
> `66a4028`(F3 lock-in 测试) → `66d0bdb`(F2 auto-select 迁 LineageBridge) →
> `55571b8`(F2 删 useTabs/useSelectedNode) → `60ce9e3`(回归验证)。
>
> 成果落在 **`frontend/src/workbench/LineageBridge.tsx`**：单一 URL writer（commit），
> auto-select 已迁移，focus 语义明确决策（初始 load 用 selectByCanvasClick 让 focus 跟随，
> 注释说明理由）。`grep -rn "useTabs\|useSelectedNode" frontend/src` 仅剩注释。
> FE 515 passed，BE 621 passed。
>
> **下面的 Gate 描述保留作方法论参考**（给 F5/F6/F7 或未来类似重构借鉴），不是待办。

### 关卡（Gate）与验收门槛 — 方法论参考

**Gate 0 — 类型搬家（纯机械，零行为变化）**
- 做：把 `TabState` 定义从 `useTabs.ts` 搬到 `tabsSchema.ts`，重定向 6 处 import（清单见 §2）。
- 验收：`npx vitest run` 测试数不变（搬家前的基线）。
- 通过标准：**测试数一个不少、不多**。变了说明你顺手改了别的，回滚。
- commit：`refactor(workbench): F2 step0 — move TabState to tabsSchema`

**Gate 1 — Provider 成为选择单一源 + lock-in 测试**
- 做：确认/补全 Provider 已能提供 `selectedKey/select/tabs/activeTabId/closeTab/setActiveTab`；加 4 个 lock-in 测试（selectedKey 严格=active tab；无 active 时 null；setFocusOnly/pinFocus 不动 selected）。
- 验收：新增测试通过；总数 = 531 + 你加的数量。
- 通过标准：**先写测试，看它失败，再让它通过**（TDD）。如果测试一写就过，说明它没测到东西。
- commit：`test(workbench): F3 — lock selectedKey single-source`

**Gate 2 — 桥接 LineageContext 到 Provider（不删旧代码）**
- 做：在容器里让 LineageContext 的值从 `useWorkbench()` 派生，但**暂时保留** useSelectedNode 不删，先让两者并存跑通。
- 验收：`WorkbenchRouteContainer.test.tsx` 全绿；手动确认点击节点→抽屉切换仍工作。
- 通过标准：这一步如果有测试变红，说明桥接的接口形状不对，**修桥接，不要改测试**。
- commit：`refactor(workbench): F3 — source LineageContext from Provider`

**Gate 3 — 搬迁 auto-select**
- 做：把 review/caution 首屏选中逻辑搬到容器（消费 Provider dispatch），按 D3 的 focus 决策实现。
- 验收：`useSelectedNode.test.tsx` 里的 auto-select 断言要么迁移过来仍绿，要么你能证明等价。
- 通过标准：**auto-select 的行为必须有测试守住**。没有就先补一个再搬。
- commit：`refactor(workbench): F2 — move auto-select into container`

**Gate 4 — 删除死代码**
- 做：删 `useTabs.ts/.test.tsx` + `useSelectedNode.ts/.test.tsx`，迁移其仍有价值的断言。
- 验收：`grep -rn "useTabs\|useSelectedNode" frontend/src` 应只剩零（或纯注释）；全量 vitest 绿。
- 通过标准：删除后**唯一 URL writer 只剩 commit()**。验证：`grep -rn "setParams" frontend/src/lineage` 无结果。
- commit：`refactor(workbench): F2 — retire useTabs/useSelectedNode`

**Gate 5 — 全量回归 + 双 writer 消除验证**
- 验收：BE 621 + FE 全绿；人工 smoke：快速连续"点节点 + 切 view"不再丢状态。
- 通过标准：能在 PR 里**贴出 pytest + vitest 两条命令的真实输出**。
- commit / push。

### F2/F3 的"红线"（碰到就停下来问人）
- 任何一关让基线测试**减少**（不是增加）→ 你删到了不该删的东西。
- 需要修改 ≥3 个现有测试的**期望值**才能通过 → 大概率你的实现错了，不是测试错了。
- auto-select 行为变了但你不确定对不对 → 找 owner（A.5 #1）。

---

## B.2 F5 / F6 / F7（加性功能）—— 中低风险，但仍按门槛走

> 这三项不碰核心状态机，相对安全，**可以先于 F2/F3 做**。但"安全"不等于"随便"——同样每步先绿再进。

### F5 — search cursor 专属高亮
**困难点**：cursor 现在只有 index（在 Tier3），graph 需要的是 nodeKey；高亮有 6 个层级，新层级必须插在正确优先级（cursor 高于普通命中，低于 focus）。
**验收门槛**：
- [ ] Provider 新增 `searchCursorKey` 状态 + `setSearchCursorKey` dispatch，带单元测试。
- [ ] SearchPalette 在 ↑/↓ 改 cursor 时派发 cursorKey；测试断言派发值跟随。
- [ ] GraphCanvas 新增 `searchCursorKey?` prop，该节点拿到区别于普通命中的高亮 class；测试断言两者 class 不同。
- [ ] 全量测试绿，数字只增不减。
**红线**：改了 GraphCanvas 的现有高亮层级导致 focus/selected 高亮测试变红 → 你插错了优先级。

### F6 — shortcut 全局 dispatcher
**困难点**：`actionRegistry.shortcut` 现在是**显示文案**（如 "⌘P"），不能直接用来匹配键盘事件。必须先加结构化字段。键位会与现有 ⌘J/⌘K/Escape 冲突。
**验收门槛**：
- [ ] **先**给 registryTypes 加结构化 `keys` 字段（`{key, meta?, ctrl?, shift?}`），与显示文案分离；旧测试不受影响（绿）。
- [ ] dispatcher 复用 **F4 的 `isEditableTarget`**（不要重写一份判断逻辑）。
- [ ] 单元测试：注册的组合按下→对应 action invoke（mock dispatch）；输入框内按下→不触发；disabled action→不触发。
- [ ] 键位冲突表先列出来给 owner 看过（A.5 #3），再实现。
**红线**：自己新写一套"是否在输入框"判断，而不复用 `isEditableTarget` → 重复逻辑，必然两处不一致。

### F7 — Command Palette
**困难点**：依赖 F6（共享 ctx 构造 + guard）。容易和 SearchPalette 的 ⌘K 抢键。
**验收门槛**：
- [ ] 触发键与 ⌘K 区分（如 ⌘⇧P），复用 F4 guard。
- [ ] 列 `actionsForSurface("command-palette", ctx)`，disabled action 灰显不可 invoke。
- [ ] 单元测试：打开→列出 action；Enter→invoke read-only action；disabled→不触发。
- [ ] 全量绿。
**降级许可**：时间不够时，F7 可只做"最小可用面板"（能开、能列、能执行 read-only），把丰富度推迟。**但 F2/F3 的质量优先级高于 F7 的完整度** —— 不要为塞满 F7 牺牲核心重构。

---

## B.3 通用验收门槛（每个 F 项都适用）

提交任何一项前，逐条打勾：
- [ ] `cd frontend && npx vitest run` 全绿，且测试总数 **只增不减**（减了就是删了别人的测试或功能）。⚠️ 没有独立 tsc 步骤，vitest 是唯一前端门槛
- [ ] `./.venv/bin/python -m pytest -q` 仍 621（前端改动不应影响后端，影响了就是改错了文件）
- [ ] 新功能**有新测试**覆盖（不是只改实现不加测试）
- [ ] commit message 里的每个数字/事实都是你**亲眼看到输出**后写的
- [ ] commit 后**立即 push**（本仓库规则）
- [ ] `git status` 干净，没有意外的 untracked/modified 文件混进提交
