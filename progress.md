# Progress Log — v1.7.3 Integration

## 2026-07-19

- Approved offline native frontend dependency restoration completed without scripts or network.
- Native focused repeated-measures suite passed 38/38; complete frontend suite passed 1211/1211; typecheck passed.
- Corrected the stale FailureCard test assertion to preserve the current server-owned action contract.
- Began Failure Memory System and Development Efficiency Control System planning. No release or integration-assembly claim has been made.
- Implemented `scripts/devline_memory.py` with TDD: 7 focused tests cover append validation, promotion, metrics, Context Pack, line close, and startup instructions.
- Initialized `wo-a`, `wo-b`, `wo-c`, `wo-d`, and `integration-control` devlines. Recorded current WO-A persistence churn, WO-C native dependency gate, WO-D C1/C2 boundary, and WO-B inert runtime seam.
- Promoted the repeated WO-A persistence capability issue to a mandatory global rule. Independent review is pending.
# 2026-07-19 — formal control-plane checkpoint

- Replaced the unaccepted devline-memory prototype as the target architecture with the formal FMS contract.
- Formal event integrity module: 13 focused tests pass.
- Formal deterministic retrospective module: event plus retrospective suite has 17 focused tests pass.
- C2 containment contract received independent design approval after separating the C2 executor from C1, binding exact host-instance fingerprints, and correcting Seatbelt path semantics.
- These results are component evidence only. No candidate execution, browser acceptance, full release gate, merge, tag, or release is implied.

## 2026-07-19 — formal FMS Task 5 checkpoint

- Replaced the active entry rule with `scripts/devline_control.py`; legacy `scripts/devline_memory.py` and its five existing devline records remain quarantined and were not read, rewritten, or accepted as formal evidence.
- Created formal `wo-a-agent`, `wo-b-model-pack`, `wo-c-ui`, `wo-d-evaluation`, and `integration-v1-7-3` lines through the CLI. The normalized Integration ID uses hyphens because the formal line-id contract rejects dots.
- Appended bounded `historical-backfill` events using current reviewed designs, tests, and the release ledger as evidence references; no legacy event content, fabricated duration, or token measurement was copied.
- Generated all five formal retrospectives, ran formal promotion, and rebuilt the retrospective index. The index is empty because all formal lines remain in progress.
- Formal CLI `verify --all` verified the five formal lines and reported the five legacy lines as quarantined. The focused formal FMS suite passed; this is process-control evidence only, not an Integration candidate or release gate.

## 2026-07-21 — v1.8 VIX browser reproduction

- Read the in-app Browser operating contract and the complete 1013-line Stata Do-file.
- Confirmed the task requires a real visible Workbench flow, committed VIXCLS input, real DeepSeek Agent turns, screenshot capture, and a durable issue/evidence report.
- Created a bounded six-phase QA plan without changing product code.
- Started the backend on 127.0.0.1:8018 and frontend on 127.0.0.1:5188 after an initial sandbox bind denial; no heavy gate is running concurrently.
- In the visible in-app browser, created `/private/tmp/wb-v18-vix-browser-20260721`, uploaded the committed VIXCLS CSV via the real file chooser, observed 2,610 rows/two columns, selected ARMA-GARCH, and saved the table node.
- Paused before any real provider request because the active model is `deepseek-v4-flash`; the user requires DeepSeek V4 Pro. Next action is a visible UI model switch, then Graph-node form discovery.
- Switched the provider through the visible LLM Providers UI to `deepseek-v4-pro` and captured browser evidence.
- Used the supported `/submit` form to configure the exact Stata-aligned sequential baseline and submitted raw VIXCLS. Run `20260721_200829_505438_443bf21a` failed at estimation because 68 VIXCLS values were missing; its failure manifest and upstream artifacts remain inspectable.
- Sent one real natural-language DeepSeek V4 Pro Agent task. Session `agent_chain_9bdac9db230b4ac1873e4eb6495cfc4b` ended without mutation, explicitly identifying that no typed row-filter operation or usable ARMA-GARCH model node was available.
- Entered `result = df.dropna(subset=["VIXCLS"])` in the failed raw node's visible Run code editor, but preview/apply stayed disabled. Created the equivalent non-missing CSV mechanically and verified 2,542 data rows and zero blank VIXCLS cells for the continuation run.
- Completed visible manual run `20260721_201344_532869_f49478a8`; inspected the acceptance summary, parameters, diagnostics, rolling metrics, full-sample child, 17 chart artifacts, and all visible chart sections.
- Verified on real VIX numbers that all 2,520 finite manual-run pairs satisfy `conditional_variance == conditional_volatility^2` exactly.
- Completed visible automatic scan `20260721_201913_716716_77fe0555`; inspected all 26 ARMA and 12 variance candidates and confirmed validation was not used for selection.
- Exercised Graph Compare between manual and automatic ARMA-GARCH runs and saved compare node `1def99e...`. The packet contains detailed result differences but is visibly blocked by incorrect split/lineage integrity findings; the Compare action did not modify either run's `graph.json`.
- Audited generated stage-node drawers and Report facts; recorded missing node hashes/stage lineage and missing time-series report facts as product gaps.
- Wrote the final evidence and capability matrix to `docs/superpowers/handoffs/2026-07-21-v1.8-vix-browser-reproduction.md`. No product code, release state, protected path, or provider secret was changed.

## 2026-07-21 — v1.8 browser debug and optimization closure

- Implemented the approved minimal browser-debug waves with TDD; no estimator redesign or release action was added.
- Added an explicit confirmed missing-row analysis policy and structured blocking preflight, then preserved the failed-run model tombstone and terminal artifact truth.
- Completed real DeepSeek V4 Pro Report and Agent recovery flows. Fixed the nested `model_options` merge defect found by the first confirmed proposal; the second proposal completed a manual child.
- Fixed project-forest versus rerun-family write fingerprint drift; visible automatic bounded search then completed and selected ARMA(2,1) plus ARCH(4).
- Fixed read-only Compare owner resolution and persisted split-artifact loading. Recreated the durable Compare node with complete status and no integrity findings.
- Added missing time-series report facts, four Stata sequence charts, truthful empty/malformed chart states, and browser-safe SVG download behavior.
- Captured six durable screenshots under `/private/tmp/wb-v18-vix-fixed-20260721/browser-evidence/` and rewrote the VIX reproduction handoff to current code and run evidence.
- Appended seven formal FMS/DECS events to `v1-8-arma-garch-runtime`, including the persisted-fixture false-green waste event and the fresh full-gate receipt; no token total was fabricated.
- Stopped all development services and ran the final full gate without another heavy workload: backend 2940 passed / 8 skipped; golden 23 passed / zero drift; frontend 1288 passed; typecheck passed; `GATE PASSED`.

## 2026-07-21 — Table structured-chart visibility follow-up

- Reproduced the reported four-chart Table view against the persisted VIX auto run: the generic Table gallery accepted only `figure` PNG artifacts and treated all `time_series_json` charts as ordinary links.
- Added a red Table regression test, then reused `useArmaGarchCharts` and `ArmaGarchChartGallery` with the Table view's existing artifact listing; no backend, estimator, or chart-calculation path changed.
- Focused Table/gallery tests pass (26), TypeScript passes, and real in-app-browser QA shows 17 chart artifacts / 18 panels with zero browser console errors.
- Saved follow-up screenshots `07-table-time-series-charts.png` and `08-table-17-time-series-charts-visible.png` under the existing VIX browser-evidence directory.
- Fresh quick verification produced frontend 150 files / 1,289 tests passing and a clean TypeScript check. The sandboxed backend sweep passed 2,920 with 8 skips; its 20 failures all reported the host-denied nested `sandbox-exec`. Native-host rerun of the three affected test files passed 33/33, confirming an execution-environment limitation rather than a product regression.
